# Sourcing AI Agent Dev Progress

> Status: Living tracker. Use the latest entries as the source of truth, and assume older bullets may describe superseded intermediate states.


> Rotation contract (2026-06-11): this file keeps only the current and previous month of entries, newest first.
> Older entries are rotated into `docs/archive/progress/PROGRESS_<YYYY-MM>.md`. When a new month starts, move the
> month-before-last out before appending. Keep this file under ~300 lines.
> Archives: [2026-05](docs/archive/progress/PROGRESS_2026-05.md) · [2026-04 and earlier](docs/archive/progress/PROGRESS_2026-04_and_earlier.md)

## 2026-07-10 (Asia/Shanghai)

### CRM Public Web: gpt-5.6-sol product model and review-evidence hardening

- Per owner decision, the startup-scoped shared `model_provider` and immutable W7g product target moved from
  `gpt-5.5` to `gpt-5.6-sol`. OpenAI-compatible calls now retain requested/response/effective model identity and
  a bounded token-usage allowlist. Every generic business call and legacy string API rejects missing or
  mismatched provider response identity and opens the model circuit; Public Web and provider health retain the
  raw structured result only so they can publish precise fail-closed diagnostics. Concurrent first health probes
  for the same provider/base/model share one in-process flight.
- W7g has no Make/environment model downgrade. Real execution additionally requires exact
  `CRM_PUBLIC_WEB_LIVE_FORCE_REFRESH=1` / `--force-refresh` before any backend/provider request, sends
  `force_refresh=true`, and pins the returned batch plus exact record/run mapping through poll and detail before
  export. The dry run remains request-free and reports the missing live prerequisites.
- The Codex review runner now inherits the globally selected newest model/highest effort and records v2 evidence:
  zero exit, effective settings from the persisted rollout, prompt/events/raw-output/rollout hashes, exact
  prompt/final-output causal binding, and a pinned Git scope digest. The verifier independently recomputes those
  claims; missing/deleted required files, later scoped commits, staged/unstaged/dirty/committed cancellation,
  model reroute, nonzero/timeout, and old-session repacking all fail closed. This is local integrity evidence, not
  a cryptographic signature or remote attestation against a malicious workspace author.
- Two adversarial review rounds found and closed stale-scope replay, missing required-file, causal-repack,
  index/worktree cancellation, shared business-model identity, and stale W7g batch reuse defects. Final reference
  reviews returned GO. Validation: review-evidence group **124 passed**; model/settings/CRM/pre-agent group
  **144 passed + 11 subtests**; final `ci-pre-agent-contract` **223 + 2/11/1/2 = 239 passed** with
  `dry_run_ready failures=[]`; Ruff and compileall clean; mypy remains R-011's **87 errors / 4 files**. No live
  provider validation was run, so relay acceptance, returned model identity, price, and product quality remain a
  guarded post-review live proof rather than a checked-in claim.

### Track B ②.3c: serving_projection_members repository cutover + D-4(a) (`219f2e8`)

- Eight members methods plus their bespoke mapper/payload/readiness helpers moved to
  `store.repos.serving_projection`. All 56 external production calls and 39 test calls switched; the two
  Store-internal calls disappeared with the old facade. Retired Store methods, helpers, two native dispatch
  keys, and dynamic old-facade access are all zero; `storage.py` fell from 14,261 to 13,822 lines.
- The frozen pinned-HEAD A/B battery produced byte-identical 11,783-byte output on both implementations
  (SHA-256 `d139000b384d754d9d190193e83248b4a37c58799b1acef9b50d130dd2b33913`). Removing the rank clamp changed
  the output to 11,785 bytes / `f10aacd3c8d13d4223e6890b3285931b79771867eafc8acf22b774116cccbca0`, so the battery caught the mutation.
- D-4 option (a) is implemented: all four wrapper calls now pass `table_name=` and `rows=` explicitly, and a
  global AST guard rejects positional bulk writes in `src/` and `scripts/`; four synthetic mutations fail loudly.
- Focused validation passed 15 surface, 9 members, 57+4 live-PG, 7 writer, 22 asset, 20+4 projection CRM,
  17 person/CRM, 34 CRM boundary, and 4 operation tests. Final stable-tree contract CI passed 223 plus 2/11/1/2
  downstream gates (**239 total**) and a clean request-free dry run. Mypy remains at R-011's 87 errors. One
  results count failure reproduces exactly on pinned HEAD (R-001/R-007/D-3).

### Track B ②.3b: projection_manifest_shards repository cutover

- The three `projection_manifest_shards` methods and its bespoke mapper moved from `ControlPlaneStore` to
  `store.repos.serving_projection`; four direct test calls across two files switched in the same batch.
  Production callers were already zero. The old methods, mapper, and native dispatch key were deleted;
  `storage.py` fell from 14,355 to 14,261 lines.
- The mapper intentionally remains hand-written: its non-negative integer clamp and broad row-access fallback
  are stricter than the generic descriptor contract. Six old/new function bodies were AST-equivalent after the
  approved name/primitive mappings. The deletion-window battery passed 3 tests plus 9 subtests, including
  Tier-A/Tier-B read/write faults and a clamp mutation that changed negative values from 0 back to negative.
- A frozen real-PG snapshot compared 13 public return samples and a four-row raw table dump against pinned
  `d97d17c`; both 5,221-byte outputs were byte-identical with SHA-256
  `74cc0b13d342f857cfb49d3a97200bac69cd9dcb9b6e18bc8781561a9f0d0f00`.
- Validation: domain/surface/on-conflict 21 passed, live-PG foundation 2 passed, writer 7 passed, and the final
  contract lane passed 190/0 skip plus 2/11/1/2 downstream gates and a clean dry-run report. Ruff/format checked
  43 files, compileall passed, and mypy remained at R-011's 87 errors in the same four files.
- D-4 records a separate pre-existing positional bulk-write fail-closed defect affecting the next members/search
  batches plus two other tables. The owner selected option (a) on 2026-07-10: fix all four sites and add a
  global wrapper-call guard with ②.3c; ②.3b itself did not modify those write paths.
- The ②.3b independent review scope is pinned to `b3818f0..aaf13fb`. Telemetry does not support the earlier
  "5-hour quota exhausted" or "first reviewer capacity failure" wording: its 300-minute window reported
  `used_percent=0.0` / no rate-limit type and the first session ended task-complete after producing a chat-only
  GO body but no artifact; one later attempt reported model capacity and two were interrupted. A hardened-runner
  retry on 2026-07-10 produced no reviewer output and timed out after 900 seconds, so its artifact is explicitly
  invalid. None of the chat-only/interrupted/timeout results counts as a verdict.
  R-014 keeps the review pending and blocks only this scope's live/W6/manual/milestone signoff.

### Track B ②.3a: serving_projection catalog repository cutover

- `serving_projections`, `run_projection_links`, and `collection_authoritative_pointers` moved from nine
  `ControlPlaneStore` methods to `store.repos.serving_projection`; 69 production references (68 calls plus
  one callback) and 82 direct-test calls switched in the same batch. The nine methods, three store mappers,
  and three native dispatch keys were deleted; `storage.py` fell from 14,635 to 14,355 lines.
- The deletion-window battery passed 4 tests plus 18 fault subtests. A frozen real-PG script compared 33
  return samples and raw 3/2/2-row table dumps against both the coexisting old surface and pinned `9217350`;
  all normal outputs were byte-identical (SHA-256 `648efefe086ed7359386885eb212e3274a1d2589d54acdb0922868133b611276`).
  A shared-descriptor mutation stayed equal inside one worktree but changed the pinned hash to
  `8bef381ff56ef3adb7bd4cf8d1d1cc59e5055377bdf085cefdbbf11ab4c43c26`, proving the cross-worktree gate.
- Validation: permanent direct/guard group 23 passed, downstream domain group 39 passed, results API exact
  slice 6 passed, and the three relevant pipeline tests passed on both current and `9217350`. The contract
  lane passed 189/0 skip plus 2/11/1/2 downstream gates and a clean dry-run report. Ruff/format checked 43
  files; mypy stayed at R-011's 87 errors in the same four files after the new repository/base were added.
- This is the low-risk catalog sub-batch only. Manifest shards, members, and person search index remain in
  `ControlPlaneStore` as ②.3b/c/d; no schema, count/readiness, board, provider, or public API semantics changed.
  Independent non-author Codex review of `9217350..d97d17c` returned GO with no blocking finding
  (`runtime/reviews/20260710_async-reference-track-b-2-3a-codex-subagent.md`). Its two low durability findings
  were fixed forward: a permanent 18-outcome Tier-A/Tier-B battery plus dual-clock/zero-negative-limit assertions,
  and a receiver-aware AST caller/callback/getattr guard plus retired dispatch-key guard. The focused group passed
  23 tests; the strengthened contract lane passed 192/0 skip plus the unchanged 2/11/1/2 downstream gates.

### Track B ②.2: manual_review repository cutover

- The `manual_review_items` domain moved from seven `ControlPlaneStore` methods to
  `store.repos.manual_review`; all 14 production and 17 direct-test callers switched in the same
  batch, and the old facade was deleted. `storage.py` fell from 15,091 to 14,635 lines; the raw mapper
  remains intentional because nullable scalar and malformed-JSON behavior are not representable by
  the current descriptor kinds.
- Equivalence evidence: 14/14 mapped AST functions exact; temporary three-part A/B green, deliberately
  broken mapper made all three parts fail, then green after restore; a canonical snapshot cleanup
  script was byte-identical against pinned `c19c0fd` (six 16-column rows plus sequence state, SHA-256
  `8913ee82698af70ee0f1deb7c56307f605b60352e264deaa614963a3d755ecab`).
- Validation: `ci-pre-agent-contract` 187 passed / 0 skipped plus 2/11/1/2 downstream passes and a clean
  dry-run report; domain/guard group 69 passed; Ruff/format and compileall clean; mypy stayed at the
  R-011 baseline of 87 errors in four files. Five targeted pipeline tests had the exact same 3-pass/
  2-fail result on the migration and baseline worktrees (R-009, zero regression).
- The takeover routing table was initially corrected in `c19c0fd` to keep the reviewer outside the
  GPT/Codex author family. A host-CLI Claude Opus/xhigh
  review of pinned `c19c0fd..d0828e7` bypassed the owner's ReClaude container/daemon transport and
  returned only `ConnectionRefused`; it has no verdict and is recorded as deferred/invalid in R-013.
  Per owner direction it was not retried. The owner then updated the policy on 2026-07-10: a separate
  read-only Codex session is an accepted reviewer, and a recorded review request does not block the next
  module. The separate non-author Codex fallback subsequently reviewed pinned `c19c0fd..d0828e7` and returned
  GO (`runtime/reviews/20260710_async-reference-track-b-2-2-codex-subagent.md`; AST 14/14, fault probes 8/8,
  base/head pipeline result identical). R-013 is closed; the two failed transport artifacts remain invalid.

## 2026-07-09 (Asia/Shanghai)

### Governance batch: Track B ②.0+②.1 committed; provider fail-closed merged; CI lanes hardened; protocol upgrades; GPT-5.6 takeover prep

- **Track B ②.0+②.1 landed as one commit (`d5109f1`)**: the linkedin_profile_registry (②.0, 2026-07-02)
  and criteria/confidence (②.1, 2026-07-06) domains retired from `storage.py` into
  `store.repos.<domain>` repositories (storage.py 19,187 → 15,091 lines). The two verified batches had
  sat uncommitted in the working tree; batch details in `docs/TRACK_B_PG_PURE_STORE_DESIGN.md` §6,
  protocol in `docs/TRACK_B_REPOSITORY_MIGRATION_HANDBOOK.md`. Next batch: ②.2 manual_review.
- **`fix/provider-fail-closed-isolation` merged (`430a369`)** — the 2026-06-27 test-billing incident
  fix had been stranded on its branch for 12 days with every fail-open signature still live on main:
  unset `SOURCING_EXTERNAL_PROVIDER_MODE` now normalizes to `simulate` (never live), live access in all
  non-production environments requires the two-key confirm, detached subprocesses get live access
  disabled + tokens blanked, and `tests/conftest.py` points secrets at an empty temp file session-wide.
  Verified: 58 runtime/model/settings tests + provider connector suites; 7 full-suite failures proven
  pre-existing by a worktree control run at the pre-merge baseline (ledger R-010).
- **CI lanes hardened (`8b555b6`)**: `tests/test_pg_onconflict_guard.py` (extended in ②.1 to scan
  `repositories/*.py`) ran in NO lane — now in both the Makefile contract lane and backend-ci; and
  `SOURCING_REQUIRE_PG_STORE_TESTS=1` + `SOURCING_REQUIRE_PG_DURABLE_RUNTIME_TESTS=1` now cover every
  lane segment, so PG-availability skips fail closed (the Docker-daemon-down mass-skip false-green
  class). Mutation-proven: an unreachable DSN converts the skip into a failure. Lane: 185 passed 0 skip.
- **Protocol upgrades from the playbook round-2 mapping (`6e2526a`)**: NEW `docs/RESIDUAL_LEDGER.md`
  (unifies the two drifted known-failure budget copies; per-row id/tripwire/attribution; only-down
  ratchet; green-modulo-ledger acceptance); handbook §7 pending decisions → decision cards with
  deadlines and timeout defaults; handbook §4 gains the A/B mutation self-check and step 7 (async
  cross-model reference review per batch); `INDEPENDENT_REVIEW_GATE.md` gains a checked-in Model
  Routing Table (the original cross-family-only fallback was superseded by the 2026-07-10 owner decision);
  `INDEPENDENT_REVIEW_BRIEF.md` output format rewritten (exhaustive severity-ranked findings,
  new/re-raise/residual classification with ledger-id citations, termination rule).
- Playbook side: eight distilled experience items (worktree-not-stash baselines, verbatim-move byte
  gates, fail-closed lanes, guard scanning surfaces, generated dispatch tables, fan-out resume,
  double-run determinism, debt-baseline ratchets) written back to the ai-assisted-engineering-playbook
  sibling repo (round-2 distillation).

> **Tracker gap note**: 2026-06-14 → 2026-07-06 work (Track A Phase 4 close, M2 provider runtime,
> Track B B3/B4.x, ②.0/②.1) was recorded in `docs/TRACK_B_PG_PURE_STORE_DESIGN.md` §6,
> `docs/NEXT_TODO.md`, and the Track B handbook instead of this tracker. This tracker resumes today;
> consult those docs for that window.

## 2026-06-12 (Asia/Shanghai)

### Phase 3a extraction, FastAPI transport port, ON CONFLICT gap class — interrupted batch recovered and landed

- Three parallel implementation workflows (Phase 3a `crm_public_web` extraction; FastAPI transport-equivalent rewrite of `api.py`; PG ON CONFLICT unique-index gap fix) were killed mid-flight by an AI-session limit, leaving partial uncommitted edits. Recovery was probe-based rather than rollback: all three states imported cleanly and their primary suites passed, showing the agents died during final verification, not mid-edit. One real cross-workflow breakage was found and repaired — a `crm_public_web` boundary test pinned the old hand-rolled `api.py` route text that the FastAPI rewrite removed; it was re-pointed to the new `post_crm_public_web_export` handler body with strengthened ordering asserts. The originally planned independent adversarial verifications were then re-run as a consolidated read-only workflow before anything was committed.
- Phase 3a (Track A) landed: the `crm_public_web` domain moved verbatim into `src/sourcing_agent/crm_public_web_owner.py`; `SourcingOrchestrator` keeps signature-identical delegating wrappers for every public and private method (zero call-site changes); `regression_matrix` mappings updated; verified per-method AST-identical against git HEAD by an independent agent.
- Track C landed: `api.py` rewritten on FastAPI + uvicorn as a strict transport-equivalent port — same routes, payloads, status codes, and headers; CORS allowlist with localhost auto-allow; Apify webhook token check preserved; the two-lane semaphore became middleware (kept until M2 per-provider budgets exist, per the HarvestAPI constraint); a `create_server` compatibility shim wraps uvicorn so `cli.py` and tests keep `serve_forever`/`shutdown`/port-0 semantics. Route parity was independently verified against a pre-rewrite route inventory reconstructed from `__pycache__` bytecode (the old `api.py` was never committed). pydantic-modeled handlers → OpenAPI-as-frontend-contract is the explicit next step, not part of this port.
- Track B follow-through: the PG fixture pilot (committed the previous evening as `e7a8be2`) immediately caught a production defect class — SQLite `UNIQUE` constraints were never mirrored into the PG bootstrap DDL, so `ON CONFLICT` targets like `upsert_criteria_pattern` raise `InvalidColumnReference` in postgres_only mode. Fixed by extending `_CONTROL_PLANE_UNIQUE_INDEXES` with idempotent unique indexes plus dedupe-before-index (keep newest by recency column; loud failure when none exists), guarded by `tests/test_pg_onconflict_guard.py`, which mechanically diffs every ON CONFLICT target in the codebase against the index inventory so the gap class cannot recur silently.
- Shipped as three themed file-scoped checkpoint commits on PR #15 (FastAPI transport → extraction → ON CONFLICT), with disclosure that `api.py`, `cli.py`, and `control_plane_postgres.py` were last committed 2026-04-26 and therefore also carry pre-handoff uncommitted changes in their checkpoint diffs.

### Failure triage: 8 flagged failures attributed and fixed; results_api budget recorded

- A verifier had attributed 8 suite failures to the extraction workstream. Control experiments (same suites with only the extraction diff reverted against pinned `e7a8be2`) refuted that for all of them — every failure reproduced bit-identically without the extraction. Root-cause diagnosis then found and fixed: (1) three frontend-history tests stubbed `threading.Thread.start` globally to suppress the excel-intake runner, which also strangled psycopg_pool's worker threads so the pool could never populate (first `getconn` → 30s `PoolTimeout`); the stub is now targeted at `_run_excel_intake_workflow_command_thread`. (2) `test_projection_crm_api_contracts` predated the 2026-06-07 workspace fail-closed contract; it now sends `workspace_id` explicitly and additionally asserts the 400 `crm_public_web_workspace_id_required` contract at the HTTP layer. (3) The hosted-smoke `runtime_tuning_profile` "loss" was a read-path bug: the `get_job_api` include_details compaction carried in c942874 (pre-handoff Codex-era state) dropped `request.execution_preferences`; the full stored request is now restored under include_details while events/summary stay compacted.
- Three further frontend-history failures (separate cause) were drift against c942874-checkpointed pre-handoff state: the completion-policy gate now requires a durable `serving_finalized` proof plus an agent-runtime-session row (test seeds both, preserving the fail-closed contract); the W6 cutover made snapshot materialization command-owned (test now asserts the durable `snapshot.compaction.run` command succeeded and that the legacy items adapter stays disabled); and the canonical serving-projection read path never called `_annotate_job_scoped_candidate_markers_on_records`, so job-scoped 本次Excel导入 markers never reached `matched_keywords` — a real product gap breaking the frontend recall filter, fixed by annotating on that path too. Open owner question recorded: should completion promotion exempt legacy/recovered jobs that have no durable workflow run at all (current behavior blocks them permanently, fail-closed).
- A first-ever full run of `tests/test_results_api.py` surfaced 36 pre-existing failures (control-verified independent of all three landed commits; buckets: PG-only durable-runtime guards, HTTP 410 Gone, completion/promotion gates, projection-link assertions). Recorded as the known-failure budget in `docs/NEXT_TODO.md` for themed repair batches; budget growth now requires control-environment attribution first.

### Phase 4 Step 5e read-path takeover: durable recovery-takeover-intent (Option B)

- The read-path takeover conversion went through two async-Codex NO-GOs that proved a real design smell: cramming the read path's "immediately take over THIS classified dead-runner job" intent (scope + zero stale) into the shared daemon's coalesced scalar wake file is structurally broken — F1, the forwarded controls were injectable from the untrusted /api/workflows body; F2, the scalar coalescing let a scoped takeover and a generic global-nudge clobber each other. Rather than patch a third time, I surfaced the design choice; the owner chose Option B (separate the notification from the intent) and stated a durable-foundation principle (decoupled, fine-grained, build the correct foundation, no band-aids on unreasonable designs). A two-audit investigation chose the substrate (a new control-plane table `workflow_recovery_intents`, job_id PK — clean per-job grain, latest-wins, single-winner claim, and a single-column PK that rides the onconflict-guard carry path so it needs no unique-index registration), captured in `docs/RECOVERY_TAKEOVER_INTENT_DESIGN.md`. Implemented (`ebad57d`): the read path writes the intent with server-side literal params (F1 gone — no request field reaches recovery controls), `_signal_shared_recovery_wakeup` reverts to a pure `{source}` nudge, and a new `workflow_takeover_intent_drain` recovery phase claims intents single-winner (PG FOR UPDATE SKIP LOCKED / SQLite conditional-UPDATE) and runs a stale=0 scoped resume per job (F2 gone — per-job durable rows + scopeless nudge are two non-interfering mechanisms). The Step 1 oracle got an additive-only update (one new pinned phase, nothing weakened) and stays 10/10; dual-path DDL + the onconflict guard are clean; ci-pre-agent-contract green. Forward-compatible with the Track C worker/API split. The async lane's 8th and 9th catches drove this to a durable design instead of a third patch; two more review rounds then hardened the claim/consume lifecycle (10th: consume must be claim-identity-scoped so a stale claim can't clobber a newer same-job intent; 11th: a takeover_failed resume must leave the intent for retry, not consume it; 12th: the claimer must reclaim expired-lease claimed rows, or a failed/crashed claim would strand the intent forever). The final reclaim fix passed GO. Net: the read-path takeover is now a durable per-job intent with a complete, reviewed claim → resume → consume/retry → reclaim lifecycle (single-winner throughout), and Phase 4 Step 5e is closed. The async Codex reference lane caught 12 real defects across Phase 4 — every one fixed forward — which is the strongest evidence yet for the owner-proposed non-blocking heterogeneous-review mechanism.

### Phase 4 Step 5c+5e: poll-as-backstop, then request/read triggers signal-only — Phase 4 complete

- 5c demoted the shared daemon poll to a 30s backstop (within the stale/lease windows; ~6x idle-scan reduction) now that 5b drives latency-sensitive work; the default is characterization-pinned to stay inside those windows. 5e then made the request and read paths signal-only: the request path's vestigial ensure_*_recovery driver calls and the read path's inline tick (a spawned server thread running run_worker_recovery_once) were replaced by `_signal_shared_recovery_wakeup`, leaving recovery driven solely by the serve-guaranteed daemon (5a) + event wakeup (5b) + backstop poll (5c). Response contracts preserved; the tick body and the ensure_* methods (still used by the provider-event path and serve startup) are untouched; the recovery-tick oracle stays 10/10. The critical fallback property was proven: the guaranteed daemon's tick defaults auto-resume/queue-takeover on, so a queued workflow recovers with no client polling. Characterize-first throughout; 7 test_pipeline guardrails were re-pointed from "the tick runs inline" to the strictly stronger "the tick is NOT called and the contract is preserved". 5e's async Codex review caught one more real defect (the lane's 7th): the signal helper dropped the read-path takeover's bounded-recovery contract (workflow_stale_scope_job_id + zero stale thresholds) — the daemon consumes the wakeup callback_payload, not the helper's arg, so a classified dead-runner job would have waited on the default stale window instead of immediate takeover. (My earlier 5b "scope doesn't reach the global tick" reasoning was right for 5b's non-invertible workflow_run_id but over-generalized to 5e, where the read path has the real job_id.) Fixed forward (`35d147f`): the helper forwards the eight consumed bounded-recovery fields into the callback_payload (only present-and-non-None keys, so the request path stays an unscoped nudge); tests strengthened to assert the wakeup callback_payload persists the scope and to replay the actual persisted payload into the tick. This completes Phase 4 (orchestrator ~87k → ~74k lines): command layer in four domain owners + CommandKernel + the CommandTypeSpec/cancel-resume/recovery-drain registries; the recovery tick partially phase-objectified and now event-driven; the serving mesh boundary frozen. Explicitly deferred: 5d (durable runtime_outbox consumer → Track C worker/API split), Step 2b (the dense cascade-cluster phase migration → future milestone), and Option 3 (full event-stream driver → Track D, agent_events/SSE not yet built). The async Codex lane caught 7 real defects across Phase 4 (clean-checkout import closure, the acquisition blocked-summary merger, the recovery-tick oracle routing gap, wakeup concurrency-safety, misleading wakeup scope fields, and the 5e read-path takeover-contract drop), every one fixed forward.

### Phase 4 Step 5a+5b: event-signaled recovery wakeup + daemon-guarantee invariant

- After the recovery-driving study (owner approved Option 2), the additive core landed: 5a makes "recovery is driven" a code invariant — `serve` now refuses to start (RecoveryCoverageError, after a bounded boot-race re-check) if neither an in-process shared recovery thread nor a fresh external daemon covers recovery, with a loud `--allow-uncovered-recovery` opt-out; this closes the gap the study exposed (the production guarantee was an implicit deployment convention, not enforced). 5b wires `_signal_recovery_wakeup` into `reduce_and_persist` so a durable transition that commits new recovery work (command enqueue / runtime_outbox row) signals the daemon sub-second via the existing wake-file primitive — closing the invariant-3 latency gap (ready downstream work no longer waits up to a 5s poll). Purely additive (poll, inline triggers, provider-event caller all unchanged; no LISTEN/NOTIFY, no Redis), the signal can't break the durable write, and fan-out coalesces to one wake file; `request_service_wakeup` hardened to an atomic temp+replace write now that the path is hot. Characterize-first with mutation-proven load-bearing invariant 1 (event-tick + backstop-poll-tick claim the same worker once via the DB lease) and invariant 3 (signal emitted at event-production time) regressions; recovery tick oracle stays 10/10. The remaining Step 5 sub-steps (5c poll→backstop, 5d optional outbox consumer, 5e remove inline triggers) are sequenced behind this.
- The async Codex review then returned NO-GO with two HIGH findings (the lane's 5th and 6th real catches), both fixed forward (`2000dfb`): (1) wakeup coalescing was not concurrency-safe — the read-merge-replace was unlocked and the temp file PID-only, so concurrent same-process writers (the multi-threaded API) could clobber each other's merged callback or collide on the temp path (my own atomic-write hardening had introduced the PID-only collision); now serialized by a module-level threading.Lock (fcntl.flock across same-process threads is unreliable on Darwin) plus the flock for cross-process, with per-(pid,thread,counter) temp files, proven by a 16-thread merge-preservation test that fails flock-only and passes with the lock. (2) the wakeup payload carried scope fields that recovery consumes nowhere — verified repo-wide — and could never scope a global daemon (workflow_run_id is a one-way hash, and real scoping would change the oracle-pinned tick body, so it is deferred to Step 2b); the signal is now an honest documented global-sweep accelerator with a minimal payload. Oracle stays 10/10; 162 passed across the affected suites.

### Phase 4 Step 3: cancel/resume dispatch via CommandTypeSpec slots

- The two hand-maintained running-command dispatch ladders became CommandTypeSpec slot lookups (additive cancel_handler/resume_handler string fields, getattr resolution like the drain registry). The faithful subtlety: the old owner-mismatch fall-through (known command type + non-canonical owner) is preserved via a sibling owner-agnostic registry rather than distorting the spec; equivalence was proven exhaustively across all 1025 (owner, command_type) combinations — 0 cancel/0 resume mismatches, confirmed independently by the verifier's own simulator. Characterize-first (the contract test is green on the 344598c control ladders and after), API-layer state invariants byte-identical, no handler body changed, command_type_manifest() now exports cancel/resume semantics (Track E). Recovery tick oracle stays 10/10; operation_runtime cancel/resume E2E 93/93; ci green. The async Codex reference review returned GO with zero blocking findings, independently re-deriving the 1025-combination equivalence — the lane's first clean GO after four consecutive real catches, cross-confirming the result rather than finding a gap.

### Phase 4 Step 4: serving mesh ownership boundary frozen (doc, no code moved)

- `docs/SERVING_MESH_OWNERSHIP_BOUNDARY.md` freezes the projection/candidate_source/asset_population mesh into four blocks (CandidateSourceResolver, ServingReadModel, ProjectionCommandOwner + the 9-method read family, fast-path split) with per-method file:line, natural owners, the frozen resolver interface signature, and — most importantly — the two precise edges of the candidate_source↔reader bidirectional loop with the B2 break (paging consumes the resolver's output instead of re-resolving, collapsing the cycle to an acyclic `paging → resolver → reader`). Migration order set (resolver first, because it breaks the loop). The map corrected a premise from the earlier audit: the 9 read-family `*_api` methods are NOT consumed by ProfileFetchOwner (the orch:979 injected callable is the admission write-side command); their natural owner is the read-model surface. No code moved — extraction lands with M3-M5 per decision #5.

### Phase 4 Step 2 (partial): recovery tick phase-object registry

- `recovery_phases.py` introduces RecoveryPhase + TickContext + a loud-failure registry; the 8 registry-shaped phases plus the 14-uniform-drain group now run through the registry seam. The Step 1 oracle was the hard gate — verified byte-identical (never edited) and 10/10 throughout, so every migrated body is verbatim modulo the nonlocal→ctx rewrite and the phase order equals the base call order. Discipline held: the densely-threaded cascade clusters (per-branch owner divergence, result-vs-metrics divergence, the 4-7-way skip ladders that thread *_work_observed state, workflow_resume multi-wave, remote_event_followup's 4-tuple rebind) were deliberately left inline — forcing them through TickContext would change the pinned phase records, so they stay until their coupling can be expressed faithfully (same partial-adoption discipline as the 2 CRM pinned drains). TickContext declares only the fields the migrated phases use; I trimmed ~25 pre-declared-but-unreferenced fields the implementer left as scaffolding so the context does not over-promise. Independent verification PASS; the import-coherence guard correctly flagged the new module until it was committed. The async Codex review then returned NO-GO with its 4th real lane catch: the regression matrix routed the tick oracle only for recovery_phases.py changes, but the tick body still lives in orchestrator.py — so the safety net was not connected to the file it guards (a future tick edit could drift the pinned sequence without the oracle running). Fixed forward (`043898a`): orchestrator.py now additively selects the recovery suites, with two regression-matrix guards locking that orchestrator.py and recovery_phases.py each route to the oracle.

### Phase 4 Step 1: recovery tick characterized (Step 2 safety net)

- `tests/test_recovery_tick_characterization.py` (549 lines, 10 tests) golden-snapshots `run_worker_recovery_once` with zero product-code changes: the full ordered 49-row phase sequence (not a count), per-phase gating in both directions (explicit-job scope, payload-disable reasons, and the threaded cross-phase ladders — handoff truncation, refill-submit suppression, search-seed resume-skip, budget-exhaustion → next_tick_requested), and each phase→summary-slot mapping. Independently verified as a STRONG characterization via a mutation battery on /tmp control copies: a phase reorder, a gate flip, and a summary-key drop are each caught, none slip, control restores to green — so Step 2's phase-object extraction will be provably behavior-preserving. The implementing agent's final report stream died after the work completed (same died-after-finishing pattern seen before); the file was confirmed present, green (10/10), and ci-clean before committing.

### Phase 4 Step 0: profile-prefetch scheduler contract formalized + 15-failure budget cleared

- Owner batch-approved the Phase 4 proposal (execute per recommendations). Step 0 set the baseline per the E2 decision — formalize the scheduler contract BEFORE characterizing, so the frozen behavior is M2-correct, not the half-finished rework. A three-audit workflow mapped the scheduler surface, classified all 15 budget failures (test expects OLD vs new contract), and recovered the intended contract; I ratified the per-test dispositions and the three open rulings (D1 fix code + keep the 50/55 floor; D2 fix the test stub; D3 the 3 results_api failures are projection-count, not scheduler — moved to the B-band). The contract is `docs/PROFILE_PREFETCH_SCHEDULER_CONTRACT.md` (R1-R8 + storeless §3 + dispositions).
- Implementation (`ef74475`): the regression cluster had ONE root cause — the actor-slot repack and roster split were gated on live/scripted provider mode, so the refill/canonical hot paths (which run under simulate) skipped repacking and fragmented tiny waves; removing the gate behind a grow-only write-guard fixed all 5 fragmentation regressions without weakening the larger-envelope initial-wave behavior. Storeless fail-open became fail-closed (decision #1): `_owned_result` stopped hardcoding status=queued, `self.store is None` now bubbles a distinct blocked terminal the consumer never renders as satisfied (invariant 7). enrichment.py budget 12 → 0; new contract-guard test file pins R1-R7 + invariant-1/7. The 3 results_api failures stay (B-band); a 4th full-run failure was control-attributed to a pre-existing teardown-race flake (passes in isolation, results_api files empty-diff). Independent adversarial verification PASS.
- The async Codex reference review then returned NO-GO on Step 0 with a genuine catch the same-worktree verification missed (its third real find on the lane): a SECOND consumer one layer up — `acquisition.py:_merge_profile_prefetch_dispatch_summaries` — still derived `merged_status = completed` when `queued_worker_count==0`, so a blocked summary merged to completed, re-opening the invariant-7 hole above the layer Step 0 fixed. acquisition.py was outside the Step 0 file scope, which is exactly the heterogeneous-channel blind spot. Fixed forward (`67539c6`): blocked wins over every merged status, two-signal shape preserved, with a regression test (storeless-blocked alone and mixed with a completed sibling both stay blocked). 62/62 across the enrichment prefetch/refill slice; no collateral.

### Async review lane catches a clean-checkout coherence gap; import closure committed + guarded

- The drain-registry Codex reference review returned NO-GO with one major finding: the new regression-matrix entry referenced `tests/test_durable_runtime.py`, which was still untracked. This exposed the blind spot of in-worktree verification (untracked files exist locally, so every Claude verifier saw them) — exactly the heterogeneous-channel value the lane was built for. Generalizing the finding into its class with a fixpoint sweep revealed the true magnitude: 38 locally-present files imported/referenced by tracked code (incl. `operation_runtime`, the serving-projection reader/writer, `crm_writer`, `person_asset_writer`, `runtime_lease_utils` — modules the orchestrator itself imports), meaning a clean checkout could not import the package at all. Fixed forward per the lane discipline: the 3-file fixture chain (`293eeba`), the 38-file import closure (`05a07f0`, recorded as-is with disclosure — it is the exact code every green suite ran against), and a recurrence guard (`tests/test_repo_import_coherence.py`, `9bef76d`) that fails on any new tracked→untracked import or matrix reference.

### Phase 3 tail: recovery drain bindings registry-ized — Track A Phases 0–3 fully complete

- `run_worker_recovery_once`'s 14 uniform flag-gated domain-drain call sites (a 202-line block) became a 16-line loop over frozen `DEFAULT_RECOVERY_DRAIN_BINDINGS` (`recovery_drain_registry.py`), with the one audited non-uniformity (crm_writer's write-only result) encoded as binding data and getattr-at-call-time preserving wrapper/monkeypatch interception. The structural-refactor discipline differed from the verbatim-move playbook: a characterization test pinning exact call order, payload identity, summary aggregation, skip dicts, and exception propagation was written FIRST and verified green on a pre-refactor control copy by the independent verifier. Two CRM drains stay as named calls (a boundary guardrail pins their literal source text); bespoke cascade drains stay for Phase 4. Owner modules zero diff. Verification PASS (orchestrator diff exactly 5 hunks, binding fields byte-equal, order mechanically proven; ci gate green); async Codex reference review dispatched per the new lane.

### Phase 3d: acquisition command layer extracted — Phase 3 COMPLETE; async Codex reference lane established

- Final Phase 3 domain landed deliberately narrow: 43 methods covering all 8 `acquisition.*` command types moved verbatim into `AcquisitionCommandOwner`; the independently verified scope guard confirmed the Phase-4 reserved core (run_worker_recovery_once, worker/lease spine, candidate_source/projection/asset_population mesh, stage-1 discovery lifecycle, resume threads) stayed byte-identical. Orchestrator is now ~74k lines, down from the inherited ~87k; the command layer lives in CommandKernel + the CommandTypeSpec registry + four domain owners. The first verifier run was interrupted mid-flight and a full replacement verification was re-run from scratch (CONFIRMED; enrichment budget id-identical to a reverted-extraction control; ci gate green).
- Owner approved a dual-channel review pattern and it ran end-to-end on this very change: a NON-BLOCKING Codex (GPT-5.5 xhigh) reference review fired in parallel with the Claude adversarial verification against the same pinned-base diff, returned GO with zero blocking findings, and independently cross-confirmed the AST results. Convention recorded in `docs/INDEPENDENT_REVIEW_GATE.md` (async lane never applies to destructive ops on non-rebuildable assets, which keep the blocking gate).

### Phase 3c: profile_fetch extracted into ProfileFetchOwner

- Third Phase 3 domain: the orchestrator-side profile-fetch command band (18 methods) moved verbatim into `ProfileFetchOwner`; orchestrator down another ~2,450 lines (now ~76.6k from the original ~87k). The stay-behind discipline mattered more than the move here — 13 of 31 mechanical candidates stayed: a 9-method projection/asset-population read family whose callers are all projection-side, two instance-patch hazards kept interceptable via re-resolving lambdas (~23 patch sites in test_pipeline), and two statics whose bodies reference SourcingOrchestrator. Adversarial verification CONFIRMED against pinned 0f9db3d with whole-module sweep; both failure budgets (enrichment 12, results_api 3) id-identical to a reverted-extraction control; `make ci-pre-agent-contract` all green. Side finding for the test_pipeline redesign: a 25-test profile slice has 15 pre-existing failures (un-migrated PG-only storage drift, control-attributed).

### Phase 3b: excel_intake extracted into ExcelIntakeOwner

- Second Phase 3 domain landed with the established playbook: 35 of 36 mechanical candidates moved verbatim (independently verified per-method AST-identical against pinned 2fc3018; single declared kernel-wrapper rewrite across 36 call sites), 35 signature-identical facade wrappers, zero call-site changes, orchestrator down ~1,800 lines. The two deliberate stay-behinds validate the playbook's shared-method discipline: the snapshot-full-materialization infrastructure serves both workflow and excel_intake job types, and `_run_excel_intake_workflow` carries six shared spine dependencies plus instance-level patches in test_operation_runtime — the owner reaches both through injected callables (a re-resolving lambda keeps those patches intercepting). Adversarial verification PASS; `make ci-pre-agent-contract` exit 0; one intermittent thread-vs-schema-teardown warning control-attributed pre-existing.

### Track B batch 5: the three giants — SQLite-direct migration COMPLETE, budget 74 → 15

- `test_candidate_artifacts` (58/58): its one pre-existing failure was a real product defect — `prefer_hot_cache=False` was overridden by the resolver's serving-preference sort key, so an explicit canonical request re-resolved into a broken hot-cache view and the authoritative loader's documented canonical fallback ladder never engaged; fixed by mirroring the existing canonical re-pin into the snapshot_id fast path (5th product defect caught by the migration protocol). `test_enrichment` 38 → 12 (21 cleared by the migration itself — PG-only guard trips; 4 were a fixture artifact worth remembering: the PG fixture pins provider_mode=simulate while the unset default is live; 5 needed only a durable store provided). `test_results_api` 36 → 3 (its agent died on a socket error AFTER completing the work; edits audited — no weakened assertions, anti-spoofing guards untouched — and verified by two independent full-file runs). Remaining 15 budget entries are all one family: pre-handoff scheduler/projection rework drift whose contracts exist only in untracked docs — to be repaired together with that rework's reconciliation, plus one owner decision (storeless enricher fail-open reporting completed with zero dispatch).
- Direct-SQLite `ControlPlaneStore` instantiation in tests is now fully migrated (pilot + 5 batches, 37 files). Left by design: 6 files on the durable-runtime mixin, 3 PG self-tests, `test_pipeline` bespoke.

### Track B: PG fixture migration batch 4 (4 server/daemon files, completed inline after weekly agent quota)

- The agent fleet for batch 4 was killed by the weekly usage quota two minutes in; one agent had already produced a complete canonical migration of `test_workflow_explain` (verified and kept), and the remaining three files were migrated inline to the same baseline-first standard. `test_cli` (55/55, CLI-constructed stores resolve to the per-class schema via the mixin env patch), `test_worker_recovery_daemon` (21/21, with two PG-mode test repairs: stubs of the dead-local-process helper must also patch the live adapter module's own binding, and staleness backdating must update the authoritative PG row rather than the SQLite shadow), `test_crm_public_web_runtime_boundary` (32/32, function-style test moved to the per-test context manager). Consolidated: 124 passed + 23 subtests, exactly the one budget failure. Remaining migration targets are now only the three giants (enrichment, candidate_artifacts, results_api — the first and last folded into their failure-budget repair batches) plus test_pipeline's bespoke design.

### Track B: PG fixture migration batch 3 (10 files) — two more PG defects caught and fixed

- Ten medium test files migrated with the same baseline-first per-file agent protocol; consolidated verification 143 passed + 11 intentional retired-feature skips + exactly 1 pre-existing baseline failure. The `storage_profile_registry` migration flushed out two more real PG product defects (the agent bailed out per protocol with precise diagnoses; fixes landed first, then the migration): the missing-lease read returned `None` under PG where SQLite returns the `{}` sentinel, and the refill-plan PG bulk path dropped the source_jobs merge for terminal rows (SQLite persists it before reporting terminal_skipped) — in postgres_only production that under-counts terminal rows in per-job scope summaries. These are the 3rd and 4th instances of the dual-path row-semantics divergence family tracked by guardrail invariant 7. Notable migrations: runtime_rebuild's seed/rebuild store pairs use per-test fresh schemas; cloud_asset_import's no-DSN rejection test moved to a non-mixin class; an env-driven post-import refresh default flip (DSN present → background) was diagnosed as intentional behavior, not a defect.

### Track B: PG fixture migration batch 2 (12 files)

- Twelve store-level test files migrated off direct-SQLite `ControlPlaneStore` onto the per-class `PGControlPlaneStoreTestMixin` (one agent per file, strict file ownership; baseline-first protocol so pre-existing failures are recorded by id before migration and the post-migration failing set must be a subset). 12/12 migrated with zero new failures and zero skips under `SOURCING_REQUIRE_PG_STORE_TESTS=1`; consolidated verification 52 passed plus exactly the 3 pre-existing baseline failures. Unlike the pilot (which flushed out the ON CONFLICT defect), this batch surfaced no new PG product defects. Remaining inventory reclassified in `docs/NEXT_TODO.md`: ~9 large SIMPLE files next, 5 server/daemon files need the per-test context-manager variant, 6 already PG-backed via the durable-runtime mixin, `test_pipeline` last, 3 PG self-tests exempt.

### Owner decision applied: advisory lock keys schema-namespaced

- Owner approved landing the previously deferred schema-namespacing now, while production deployment is still a single-server systemd full restart (no old/new process overlap, so the rolling-deploy lock-identity hazard that caused the deferral does not exist yet). All 7 advisory-lock sites in the live PG adapter route through `_advisory_lock_key()` (schema prefix, deterministic `public` fallback for unset schema). A live test proves cross-schema independence and same-schema mutual exclusion. Deployment constraint recorded in code and tracker: rollout requires a full process stop; do not hot-deploy alongside old processes. Evidence payloads keep the logical key, so lock analytics are unaffected. Regression sweep surfaced `test_enrichment.py`'s 38 pre-existing failures (control-verified identical without the change) — added to the failure budget.

### Owner decision applied: legacy jobs exempt from the serving_finalized gate

- Owner approved exempting legacy/recovered jobs (no durable workflow run) from the fail-closed completion gate, with the explicit instruction to institutionalize the lesson. Implemented as an explicit `workflow_run_absent` observation gated by a per-entry-point opt-in (`allow_legacy_run_absent_exemption`): only the final-results reconciliation path (which holds independent completion evidence — stage_2_final + persisted results) opts in; `_job_is_terminal`, the worker supervisor, deferred promotion, and the fast path stay strictly fail-closed, preserving the anti-spoofing guards (a first, evaluator-global version of the exemption broke exactly those two guard tests in `test_results_api` — caught by the budget-comparison run and scoped down). Lookup failures stay fail-closed (separate `workflow_state_unavailable` signal); a recorded proof still wins over the absence flag. The PG-only `update_agent_runtime_session_status` also raised on missing session rows where the SQLite fallback silently no-ops — fixed to parity (same SQLite/PG divergence family as the ON CONFLICT gap). Lesson recorded as `docs/WORKFLOW_BEHAVIOR_GUARDRAILS.md` invariant 7: fail-closed gates must enumerate exempt populations, attach exemptions to evidence-holding entry points (never the evaluator globally), distinguish lookup-failure from confirmed absence, ship both-direction tests, and keep dual-path row-absence semantics identical.

## 2026-06-11 (Asia/Shanghai)

### Claude Code deep audit, plan restructure, doc governance, and Phase 0 kickoff

- Completed a multi-agent deep audit of the decomposition surface with adversarial verification (9 analysis agents + 6 verifiers + 4 serving/agent/test-env auditors). Key verified facts: `SourcingOrchestrator` is 1,099 methods with only ~156 externally reachable methods and zero dynamic self-dispatch; command-layer domains interact via store + durable enqueue only; the entangled core is `run_worker_recovery_once`, the cancel/resume control band, and the projection/candidate_source/asset_population mesh; the ~30-method command kernel is store-only and mechanically extractable; `storage.py` has 292 dual-path methods and the default test suite runs on SQLite (so "delete SQLite first" was rejected); one workflow command's semantics span ~14 definition sites; serving currently runs on ThreadingHTTPServer with an 8-slot semaphore, per-query psycopg connects, no pool, no auth.
- Owner approved the restructured plan: five tracks (A decomposition Phases 0–4, B storage/test infra with PG schema-per-run test-env contract v2 and Docker PG on Mac, C Serving Runtime for ~20 concurrent users incl. approved FastAPI rewrite and explicit no-Redis decision, D strong agent-ification starting with ModelClient streaming/tool-calling upgrade and a company-identity self-verification loop, E governance). M1 redefined as CommandSpec registry in `durable_runtime` with manifest as export. M0.9 standalone review-gated apply cancelled (cold bundles sha256-verified; sources reclaimed via TTL prune). Recorded HarvestAPI's implicit ~8-actor concurrency limit as an M2 Provider Task Runtime design constraint (per-provider budgets, key pooling, batch-size governance).
- Doc governance executed: 21 historical/session-scoped docs moved to `docs/archive/` with archived banners and repo-wide link fixes; `PROGRESS.md` rotated (7,124 → ~70 lines; 2026-05 and 2026-04 archives under `docs/archive/progress/`); `docs/NEXT_TODO.md` rewritten as a short rolling tracker (full 1,909-line snapshot archived); `docs/INDEX.md` restructured into tiers with doc-governance rules; 2026-06-11 revision sections added to `SERVICE_GRADE_ARCHITECTURE_PLAN.md`, `RUNTIME_ASSET_RETENTION_GOVERNANCE.md`, and the Claude Code handbook.
- Phase 0 test pre-repairs and the test_env TTL prune mode were dispatched as parallel implementation work (AST-based `tests/source_inspection.py` helper replacing orchestrator source-text slicing asserts, patch seams for `_utc_now_iso` and the projection page-size constant, regression-matrix unmapped-file guard, rglob boundary scan, retention prune `local-rebuildable` policy with dry-run); results and validation status are recorded in the follow-up entry/commits for this date.

### Afternoon follow-up: PR #15, TTL apply, CommandKernel extraction

- Opened PR #15 (`governance-phase0-ttl-20260611` → `agent-native-service-checkpoint-2026-06-08`) with the doc-governance, Phase 0, and TTL-policy commits. GitHub SSH (port 22) is blocked in this network environment; pushes work via explicit HTTPS URL with the authenticated `gh` credential helper — no proxy/git config was mutated.
- The multi-user agent serving topology was confirmed after web-verified industry research and recorded in the plan doc: stateless agent-loop worker pool + PG-checkpointed `agent_session`/`agent_turn` with per-session single-writer leases; per-ROLE docker-compose containerization; explicitly no per-user resident containers; sandboxes only later, leased per tool invocation.
- TTL prune applied for real: first attempt was blocked by pid-reuse false positives (month-old `status.json` files inside test_env whose recorded pids now belong to homebrew postgres / a macOS XPC service). Root-cause fixed in `_active_runtime_pid_file_processes` (identity match against ACTIVE_PROCESS_TOKENS/workspace path, with a freshness-window fallback for opaque commands) instead of using `--skip-process-check`; 48 retention tests green. Second apply: `648` directories removed, `130,498,437,051` bytes (~121.5 GiB) reclaimed, `0` failures; disk free went 68Gi → 183Gi; `runtime/test_env` 117G → 1.4G. Record: `runtime/asset_governance/ttl_apply_20260611/ttl_local_rebuildable_apply_v2.json`.
- Phase 1 (Track A) CommandKernel extraction landed: 17 store-only protocol methods (core 8 + policy/exposure band 5 + 4 individually re-verified optional methods) moved verbatim into `src/sourcing_agent/command_kernel.py`; SourcingOrchestrator keeps 17 signature-identical delegating wrappers (zero call-site changes). An independent verifier proved all 17 bodies AST-identical modulo `self.store`→`self._store`, wrappers exact, no import cycle, and reproduced every observed test failure (3 `ci-pre-agent-contract` operation_runtime company-logo node ids + 1 workflow-explain assert) in a control environment WITHOUT the extraction — they stem from unrelated uncommitted worktree changes (`operation_runtime.py`/`media_asset_owner.py`/`asset_reuse_planning.py` in-flight state), not from the kernel.

### Evening follow-up: Phase 2 registry, Docker PG, test-env contract v2, connection pool

- Phase 2 / redefined M1 landed: `CommandTypeSpec` registry (`DEFAULT_COMMAND_TYPE_SPECS`, 41 types) in `durable_runtime` is now the single source of per-command semantics; the 4 literal tables + policy-set families, the metrics owner-contract table, and orchestrator's 3 per-type maps are all derivations; `command_type_manifest()` exports every field JSON-serializably (Agent tool-spec seed). Safety: a golden characterization test written against the pre-refactor code (green before and after) plus an independent verifier that compared pre-refactor `__pycache__` bytecode against the new registry across every dimension — zero drift, including iteration order and exception-message parity. `make ci-pre-agent-contract` now exits 0 (the 3 previously failing company-logo node ids pass; known-failure list shrank to zero). Raw command-type literals in src/ are down to 3 intentional persisted idempotency-key namespaces.
- Track B landed: macOS Docker local PG (`local_postgres_docker.py`, container `sourcing-local-postgres` on 55432, zero-consumer-change DSN integration, `make local-pg-up/down/status`); test-environment contract v2 (per-run schema+dir pairing with `.ephemeral-test-env.json` markers, real `DROP SCHEMA` teardown gated on `pre_existing=false`, orphan-schema janitor with snapshot-before-scan ordering and a `pg_stat_activity` guard). An adversarial verifier found 5 real issues (janitor race, external-schema drop, pool disposal, scope, PoolTimeout) — all fixed and re-verified the same day.
- Track C first lever landed: psycopg connection pool for `LiveControlPlanePostgresAdapter` — per-instance lazy pool, transaction semantics of all 25 call sites preserved (diff-checked), schema isolation per adapter, fork pid-guard; measured 2.0x on 200 sequential ops with server connection churn 200 → 1. Advisory-lock schema-namespacing deliberately deferred with a design note (rolling-deploy lock-identity hazard) for owner review.
- All work shipped as four themed file-scoped checkpoint commits on PR #15 (Docker PG / contract v2 / pool / registry), with the usual disclosure that long-unsynced files (notably `storage.py`, `workflow_smoke.py`, first-ever-committed `durable_runtime.py` and siblings) carry pre-handoff uncommitted state.

## 2026-06-10 (Asia/Shanghai)

### Documentation handoff and asset-governance status checkpoint

- Re-audited the current documentation entrypoints before handing the project to Claude Code. `README.md`, `PROGRESS.md`, and `docs/*.md` all carry `> Status:` headers (`81` checked, `0` missing), so the remaining drift risk is not missing banners but stale current checkpoints. Updated `README.md`, `docs/INDEX.md`, `docs/NEXT_TODO.md`, and `docs/RUNTIME_ASSET_RETENTION_GOVERNANCE.md` to make 2026-06-10 the current state for service-grade closure, Independent Review Gate defaults, and asset governance.
- Added Claude Code handoff docs: `docs/CLAUDE_CODE_PROJECT_HANDBOOK_2026-06-10.md` gives project context, current invariants, asset governance state, safe next commands, and the M1-M6 Agent-native refactor roadmap; `docs/archive/CLAUDE_CODE_CONTINUATION_PROMPT_2026-06-10.md` is a copy-ready prompt for Claude Code to continue asset governance, refactor, and development without bypassing PG-only/Review Gate/dirty-worktree rules.
- Independent Review Gate default is now recorded as GPT-5.5 with `model_reasoning_effort=xhigh` and `service_tier=fast`. The latest fast-mode rule-sync review artifacts are `runtime/reviews/20260610T061608Z_independent-review-gate-fast-mode-defaults.md` and `runtime/reviews/20260610T062155Z_workspace-independent-review-fast-mode-rule.md`, both `GO` after the validator started rejecting old live-gate artifacts that lacked reviewer reasoning/service-tier metadata.
- Asset governance status is no longer just M0.5 planning. M0.6 reviewed apply at `runtime/asset_governance/m0_6_20260609_runtime_test_env_closeout/prune_apply.json` removed `35` low-risk historical runtime/output directories and reclaimed `14745489340` bytes with zero failed operations, without touching company assets, PG state, projections, provider cache, registry pointers, signoff/pressure artifacts, phase/milestone artifacts, or current/latest aliases.
- M0.9 supersession cold bundle is prepared but not applied. The bundle manifest under `runtime/asset_governance/m0_8_20260610_runtime_supersession_cold_manifest/` created and verified `10/10` archives, source bytes `10730637032`, archive bytes `1372774127`, and estimated next reclaim `9357862905` bytes. Current destructive apply is intentionally blocked until a fresh `GO` Independent Review artifact is produced for the current code/docs/prune scope, the prune plan is regenerated with that artifact, the scope digest remains `2fe946d01dec5e534861e5c7ca2cf2e635da15c1d8524cb0d35a901c01add512`, dry-run passes, and active dev runtime is stopped with user acceptance. Historical v8/v9/v10 artifacts are not valid apply prerequisites after the metadata-gate hardening and stale-runbook review.
- Current disk pressure snapshot during this checkpoint: `runtime` about `148G`, `runtime/test_env` about `117G`, `runtime/cold_bundles` about `1.3G`, `output` about `5.3G`, and filesystem free space about `63GiB`. The next safe reclaim batch is the reviewed M0.9 bundle-derived prune, not manual deletion.
- Remaining risks: the worktree is still dirty and long-unsynced, so GitHub synchronization must be scoped by branch/PR rather than a broad dirty-tree commit. The service-grade refactor itself remains pending: workflow/command manifest, Provider Task Runtime, Candidate Acquisition Service, Profile Fetch Service, CRM Public Web quality closure, and the later OpenClaw/Codex adapter still need milestone PRs, tests, docs, and Independent Review Gate artifacts.

## 2026-06-08 (Asia/Shanghai)

### Service-grade workflow closure planning checkpoint

- Re-reviewed the current project direction after the OpenClaw/Codex adapter discussion. The conclusion is that adapter work must wait for bottom-layer service-grade closure: candidate acquisition, profile fetch, CRM Public Web, provider task runtime, serving projection/result views, CRM/export, and Operation Workbench need stable typed service boundaries before a general Agent can safely call them.
- Updated `README.md` to remove stale MVP-era framing and the old 2026-04-25 validation snapshot as current stability evidence. The README now describes the project as a PG-only durable workflow/product workbench moving toward OpenClaw/Codex-callable typed actions, with GitHub sync warnings for the current dirty worktree.
- Promoted `docs/SERVICE_GRADE_ARCHITECTURE_PLAN.md` from historical design/reference status to the current architecture planning entry for Phase 13 pre-adapter work. It now records the 2026-06-08 refactor inventory: workflow/command spec manifest, Provider Task Runtime, Candidate Acquisition Service, Profile Fetch Service, CRM Public Web, model-native Search, Serving Projection, CRM/export, frontend/Operation Workbench, documentation, and GitHub process.
- Updated `docs/INDEX.md` and `docs/NEXT_TODO.md` so new sessions start from service-grade workflow closure rather than older MVP/session handoff framing. The new milestone order is M0 GitHub checkpoint discipline, M1 workflow/command manifest, M2 Provider Task Runtime, M3 Candidate Acquisition Service, M4 Profile Fetch Service, M5 CRM Public Web quality closure, and M6 OpenClaw/Codex adapter after service closure.
- Current repository risk: the worktree is very dirty and includes many modified/untracked files across docs, frontend, runtime, scripts, tests, configs, and logs. A broad `git add .` is not safe. The next GitHub sync should be a scoped checkpoint branch/PR after reviewing staged file scope, excluding runtime/log/secret artifacts, and preserving Independent Review Gate artifacts.
- Follow-up architecture decision: service closure should be Agent-native from M1 onward, not a traditional backend followed by a late adapter. The target service shape is tool manifest + JSON schema + event-visible progress + Activity/Attempt/EntityDelta evidence + budget/approval/control contracts. Candidate Acquisition and Public Web may use both deterministic/API providers such as DataForSEO and reviewed Agent Search/fetch/browser sources, but all sources must enter one ProviderTask/Evidence adjudication path before promotion/export/CRM mutation. Creating a new checkpoint/development branch from the current local state is acceptable because the prior branch has been stale, but only scoped commits/PRs preserve state safely; a broad dirty-tree commit remains unsafe.
- Local asset governance timing decision: place Google/Reflection AI/early mixed production-test asset cleanup after M0 docs checkpoint and before M1/M2 as `M0.5 Local Asset Governance`. It should reuse the existing W5 asset-consolidation pattern: read-only audit, authoritative pointer/reference identity proof, scoped shard retention decision, cold archive manifest, reviewed apply, and rebuild/projection verification. It must not be a manual filesystem deletion because historical snapshots may still be needed for rebuild, audit, projection recovery, or scoped reuse.
- M0.5 first local evidence: ran read-only company snapshot consolidation for Google/Reflection AI and added a separate read-only runtime/output retention inventory because the main disk pressure is not the first company snapshot manifest-ready set. Evidence lives under `runtime/asset_governance/m0_5_20260608_google_reflection_v2/`. Company snapshot audit found 74 snapshots across Google/Reflection AI, 60 archive candidates, but only 6 manifest-ready Reflection AI snapshots (~108MB) after overlap/dependency gates; Google snapshots remain blocked by unique identities/dependencies. Runtime/output retention audit found 341 matching historical directories totaling ~130.9GB, mostly under `runtime/test_env` and mostly W6/nightly/pre-manual/signoff artifacts. No files were moved or deleted; follow-up requires supersession proof, cold-copy/hash proof, rebuild/projection rehearsal where relevant, and Independent Review Gate before any apply/archive operation.

## 2026-06-07 (Asia/Shanghai)

### Target Candidates, CRM Public Web, and Operation Workbench checkpoint

- Closed the current local-asset / target-candidate frontend product slice. The target-candidates page now keeps the local asset tab shell, candidate cards use fixed-height three-line headline and Public Web progress regions, Public Web link chips sit in a fixed-height row, and long review guidance is shown through adjacent `?` help instead of standalone card text.
- Tightened CRM Public Web workspace contracts end to end. Frontend body-style poll/start/cancel/retry/export requests now require explicit backend-owned `workspace_id`, and the backend CRM Public Web body-style entrypoints fail closed on missing workspace instead of silently falling back to `default`. Missing or mixed workspace state clears stale Public Web run/detail caches in the UI. Shared frontend adapter/schema now preserves Public Web run `workspace_id`, backend-owned `phase_command_display_line`, `run_control_state`, `run_display_contract`, `phase_commands`, and `created_at`.
- Productized the operation queue surface as a task approval/execution workbench. The page now presents待确认操作、执行队列、执行详情、执行证据 as product concepts, keeps raw ids/types/statuses in expandable technical details, and routes run/command controls only through Operation / workflow command APIs.
- Independent Review Gate was run after targeted tests and before browser validation. Final artifact `runtime/reviews/20260606T200102Z_final-public-web-workspace-stale-state-run-contract-gate.md` returned `GO`; earlier `NO-GO` findings around workspace fallback, shared adapter/schema drift, stale Public Web state, and frontend-derived run control were fixed before signoff.
- Browser validation was completed against the local frontend/backend. Screenshots: `output/playwright/target-candidates-card-layout.png`, `output/playwright/jackie-public-web-detail.png`, `output/playwright/yuwei-public-web-detail.png`, and `output/playwright/operations-product-surface.png`. Reports: `output/playwright/frontend-validation-report.json`, `output/playwright/frontend-tooltip-visibility-report.json`, and `output/playwright/frontend-detail-tooltip-visibility-report.json`. Metrics confirmed equal first-card heights, fixed headline/progress/link regions, hidden long help text, and removal of old internal Operation Queue copy.
- Validation passed: `uv run pytest tests/test_frontend_candidate_filters.py -k target_candidate_public_web_detail_uses_reviewable_signal_sections -q`; `uv run pytest tests/test_pre_agent_contract_review.py -k 'frontend_public_web_uses_crm_canonical_endpoints_only or operations_page_is_operation_api_only_control_surface' -q`; `uv run pytest tests/test_frontend_local_asset_pages.py -q`; targeted Public Web/results/runtime preflights; `npm run build` in `frontend-demo`; `git diff --check` for the touched files.
- Current CRM Public Web data observation: Yuwei Qin's current PG-owned Public Web state contains a manually promoted Google Scholar link and a rejected X link; no Yuwei GitHub promotion is present in current PG state. Jackie Bow has durable X and GitHub promotions, including `https://github.com/jbow`.
- Remaining risks: real provider/model live validation is still pending for workspace mismatch responses, export watermark reuse, model-provider circuit behavior, and Public Web source quality. Historical Markdown deletion was intentionally not performed in this slice; cleanup/deletion needs a separate Independent Review Gate because older docs still carry audit evidence and contract strings.
