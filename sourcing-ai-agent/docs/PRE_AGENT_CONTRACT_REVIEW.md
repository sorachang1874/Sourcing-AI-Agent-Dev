# Pre-Agent Contract Review

> Status: Active W10 review tracker. Use with `DURABLE_EXECUTION_RUNTIME_CONTRACT.md`, `AGENT_OPERATION_CONTRACT.md`, `CANONICAL_SERVING_PROJECTION_CONTRACT.md`, `CRM_STATE_CONTRACT.md`, and `PERSON_ASSET_EVIDENCE_ASSERTION_CONTRACT.md` before Phase 13 Agent UI/graph work.

## Purpose

Phase 13 Agent interaction can only call durable owners safely if each module has a single normal owner, fail-closed readers, explicit command/action surfaces, and fast preflight coverage. This review is the W10 evidence ledger: it records what is already safe for Agent/Operation use, what remains migration-only, and what still needs a direction decision.

## Review Rules

- A module is Agent-callable only through `AgentAction`, `OperationRun`, `workflow_commands`, or an explicit read-only API.
- New durable runtime, OperationRun, workflow-current-state, CRM task/current-state, and Agent-callable execution state must be PG-only. Do not add SQLite DDL/fallback for these tables.
- Public readers must not repair state, synthesize fallback rows, or write module state during reads.
- Fast preflight must include public-reader source checks for projection/person/company/media read methods. Normal read methods must not call `backfill`, `rebuild`, `upsert`, `update`, `write`, `repair`, or command-planning helpers; repair must stay in explicit operator/Operation command owners.
- Migration bridges must be report-visible, signoff-gated, and paired with a deletion condition.
- Retired Public Web aliases must not receive normal priority-lane handling. `/api/target-candidates/public-web...` may remain only as unconditional `410` compatibility branches; interactive/light-lane treatment is reserved for canonical `/api/crm/records/public-web...` routes. Normal frontend source and frontend contract/adapter files must not contain `/api/target-candidates/public-web` at all; `TargetCandidatePublicWeb*` type names are temporary naming debt only and cannot own endpoint routing.
- Historical candidate-document fallback is migration/reuse compatibility only. `SOURCING_ENABLE_LEGACY_CANDIDATE_DOCUMENTS_FALLBACK` must default off; normal projection/public-reader paths must pass `allow_candidate_documents_fallback=False` and fail closed when canonical materialized artifacts are not ready.
- Expensive W6/nightly runs are not contract discovery tools. Field owner drift, hidden fallback, or missing owner evidence must fail in fast preflight first.
- Normal owner-registry workflow commands must have non-legacy `activity_spine_policy` coverage from `durable_runtime.workflow_command_activity_spine_policy`. `legacy_internal_pending_activity_spine` is now a fail-closed policy state only; adding one back to the normal owner registry or exposing it through the action registry is a W10 preflight failure.
- Significant implementation or contract slices require the independent review gate in `docs/INDEPENDENT_REVIEW_GATE.md` after targeted/fast preflight and before W6/nightly, live provider validation, founder/manual signoff, or Phase 13 work. The reviewer must not be the author and must store a GO/NO-GO review artifact.

## Module Matrix

| Module | Normal owner/source of truth | Agent-callable surface | Fast preflight | W10 status |
| --- | --- | --- | --- | --- |
| Durable runtime | PG-only `workflow_events`, `workflow_commands`, `workflow_current_state`, `runtime_outbox` | Command registry, queued-state command query/cancel/retry/resume APIs, and owner-specific running cancel where the owner explicitly implements it | `tests/test_durable_runtime.py`, `tests/test_workflow_service_metrics.py`, `tests/test_scripted_smoke_signoff.py -k durable_command_owner_contract` | Active; no SQLite normal path allowed |
| Operation runtime | PG-only `agent_actions`, `operation_runs`, `operation_events`, `acquisition_runs`, `workflow_activity_runs`, `workflow_activity_attempts`, `workflow_entity_deltas`, `acquisition_discovery_lanes`; `operation_runtime.ActionRegistry.to_record()` owns default/allowed typed command contracts for acquisition, profile fetch, CRM writer, CRM Public Web, company Public Web refresh, export, and Excel actions | `submit/approve/reject/dispatch/cancel/retry/resume` operation APIs plus read-only `/api/workflow/activities`, `/api/workflow/activity-attempts`, `/api/workflow/entity-deltas`, and `/api/workflow/discovery-lanes`; `/operations` is the minimal read/control UI and only calls Operation APIs | `tests/test_operation_runtime.py` | Active; broader approval queue UX and status polish remain W9 product polish |
| Agent tool result aggregate | PG-only `agent_tool_result_slots`, `agent_tool_result_attempts`, and `agent_tool_result_journal`; the server-owned historical `AgentToolRegistry` owns immutable occurrence pins, and PostgreSQL row/deferred-aggregate constraints own terminal acceptance integrity | Registered Agent tool result reserve/prepare/accept paths only; no direct HTTP, UI, domain-writer, or SQLite mutation surface | `tests/test_pre_agent_contract_review.py::test_agent_tool_result_aggregate_owner_contract_is_canonical`, `tests/test_d1n_agent_tool_result_slot.py`, `tests/test_d1n_agent_tool_result_slot_uow.py`, `tests/test_migration_runner.py` | Non-live substrate; PG-only migrations `0011` through `0014` are active, public/default served population remains zero, and no provider/model/live authority is implied |
| Acquisition root/plan | `acquisition.run.create -> intent.resolve -> plan.build -> plan_review.request -> plan.commit` | `start_acquisition_run` action | `tests/test_d1i_acquisition_root_action_activation.py`, `tests/test_operation_runtime.py::OperationRuntimeTest::test_start_acquisition_operation_plans_root_acquisition_run_command_only` | Fixed-forward candidate, fresh pinned review pending; root/intent/plan/review/commit are typed command owners with no Operation dispatch inline `queue_workflow`; D1i exact current-claim/PG-clock lease, canonical source/causality, strict JSON/type and root-plan-event+intent-child+root-terminal PG-UoW contracts are implemented, with exact succeeded replay/repair. Original pinned advisory `NO-GO 0/2/1/0` is locally reconciled but fresh pinned review remains pending; Operation/action preflight and post-commit sync keep R-019 open |
| Acquisition probe/scale | PG-only `acquisition_runs` plus `acquisition.probe.submit`, `acquisition.probe.collect`, `acquisition.scale.plan` | Downstream commands from plan commit | Operation runtime targeted test and durable owner metrics | Active; probe/scale are typed orchestration commands and discovery handoff uses Operation-native Activity spine |
| Provider-backed discovery | PG-only `workflow_activity_runs` / `workflow_activity_attempts` as the generic side-effect spine plus `workflow_entity_deltas` and `acquisition_discovery_lanes` as read models | `continue_acquisition_run` allowlisted command only; future retry/cancel/resume must target Command/Activity APIs, not lane rows | Operation runtime W11d preflight and durable command owner metrics | Active; operation-native discovery writes ActivityRun/Attempt/EntityDelta/lane evidence and is claimed by `operation_native_discovery_activity_owner`, not the legacy search-seed queue owner |
| Profile fetch/registry | Operation-native `linkedin.profile_fetch.activity.run` writes PG-only Activity/Attempt/EntityDelta cache-hit/fetch-required facts; `linkedin.profile_fetch.provider.fetch` consumes required deltas and writes provider fetch attempts/registry fetched evidence; `linkedin.profile_terminal.admit` consumes cache-hit/provider-fetched deltas and emits terminal-recorded deltas without mutating projection; `projection.profile_admission.apply` consumes terminal deltas and writes canonical `serving_projection_members` through `serving_projection_owner`; legacy job-scoped `linkedin.profile_refill.submit_batch` remains internal workflow/provider owner only but now writes profile-refill Activity/Attempt/EntityDelta evidence | `fetch_profile_sample` and allowlisted continue command through Command/Activity APIs | `tests/test_operation_runtime.py`, workflow service metrics, profile scheduler contract smoke gates | W11f/W11g profile fetch, terminal, projection-admission, and W11j profile-refill evidence slices active |
| Post-profile materialization | Activity-spine-backed `linkedin.profile_url_terminal.record`, `linkedin.local_profile_delta.apply`, `projection.board_visible_patch.publish`, `projection.run_scope.finalize`, projection index/facet commands, and background `snapshot.compaction.run` | Allowlisted continue command for Agent-safe commands only; profile URL terminal record/local apply/board visible/run-scope finalize/snapshot compaction remain owner/internal unless explicitly allowlisted | workflow service metrics and scripted signoff preflights plus profile-refill/profile-url-terminal/local-apply/board-visible/run-scope/facet/snapshot-compaction Activity/EntityDelta regressions | Active for owner boundary; profile refill submit, profile URL terminal record, local apply, board visible, run-scope finalize, facet/layering, and snapshot compaction now write ActivityRun/Attempt/EntityDelta evidence; long-running cancel/resume semantics remain W11 |
| Serving projection/public reader | PG projection rows/membership/index plus canonical reader APIs | Read-only projection search/filter operation actions | `tests/test_projection_crm_api_contracts.py`, `tests/test_serving_projection_storage.py`, frontend contract tests | Active; public readers must stay fail-closed |
| CRM | `CRMWriter`, `crm_records`, `crm_engagements`, PG-only `crm_tasks`, `crm_events`; CRM writer commands record ActivityRun/Attempt/EntityDelta evidence for CRM record, event/note, and task effects | CRM writer commands from operation actions | `tests/test_operation_runtime.py`, `tests/test_person_asset_crm_projection_contracts.py` | Active; running resume is owner-specific requeue only and must not mutate CRM tables from the request path; UI/action queue polish remains W9 |
| Person asset/evidence/assertion | PersonAsset/PersonEvidence/PersonAssertion writer contracts; provider avatar URLs are metadata until `media.asset.cache` owned by `media_asset_owner` writes `PersonAsset(asset_type='avatar_media')`; projection person detail, person summary, and projection candidate page rows expose `media_summary` from stable `avatar_media` assets and otherwise return `avatar_unavailable`; `/api/media/assets/{asset_id}` serves cached object-backed media only; historical avatar repair is explicit through `POST /api/media/backfill-person-avatars`, defaults dry-run, and only plans `media.asset.cache` commands; CRM Public Web signal materialization writes `PersonAsset(asset_type='public_web_signal')` and `PersonEvidence` with entity-delta provenance; historical signal repair is explicit through `POST /api/persons/backfill-public-web-signals`, defaults dry-run, and is not reader repair | Promotion and selected assertion links through owner APIs; media cache is a typed command owner, not a provider-hotlink fallback; stale running media cache may be cancelled only before fetch/upload attempt or resumed through the command owner and does not fetch/upload/materialize in the control API; frontend candidate display may prefer `media_summary.avatar_url` only when `avatar_status=available` and the media contract source is `PersonAsset.avatar_media` with no fallback; projection/person readers must not backfill missing media or Public Web signal assets | `tests/test_person_asset_crm_projection_contracts.py`, `tests/test_pre_agent_contract_review.py`, `tests/test_operation_runtime.py`, `tests/test_projection_crm_api_contracts.py::test_asset_backfill_http_routes_are_explicit_migration_paths`, durable command owner metrics | Active; avatar media cache owner, owner-specific media cache running cancel before fetch/upload attempt, owner-specific media cache running resume, fail-closed media read API, projection candidate page media summary, frontend display preference, explicit historical avatar command-planning backfill, CRM Public Web signal-to-PersonEvidence sync, explicit Public Web signal historical backfill API, and HTTP route-level migration-boundary preflight are implemented; running historical avatar/Public-Web signal migrations remains Phase 7c follow-up |
| Company asset/evidence/assertion | PG-only `company_assets`, `company_evidence`, and `company_assertions` plus `CompanyAssetWriter`; `company.public_web.refresh` is the Agent/Operation normal path for company Public Web refresh, owned by `company_public_web_owner`, but it is now a root orchestration command that plans `company.public_web.source.collect` and `company.public_web.assets.materialize`; `company.logo.profile_experience.discover` is a non-blocking first-acquisition logo evidence command owned by `company_asset_owner` and reads at most one fresh profile payload before planning `media.asset.cache`; `media.asset.cache` owned by `media_asset_owner` writes stable `CompanyAsset(asset_type='logo_media')`; historical logo repair is explicit through `POST /api/media/backfill-company-logos`, defaults dry-run, and only plans media cache commands; historical source-specific company Public Web fact repair is explicit through `POST /api/company-assets/backfill-public-web-assets`, defaults dry-run, and syncs `company_public_web_assets` into `CompanyAsset` / `CompanyEvidence` only on explicit apply; `company_public_web_asset_runs` / `company_public_web_assets` remain source-specific company Public Web read models and sync model-safe rows into CompanyAsset/CompanyEvidence when PG-only storage is active | Company overview logo reads `CompanyAsset.logo_media` when available and uses `/api/media/assets/{asset_id}` for cached object-backed media; canonical read APIs are `/api/company-assets`, `/api/company-assets/evidence`, and `/api/company-assets/assertions`; local profile apply may only queue the profile-experience logo discovery command and must not scan profile JSON or write logo evidence inline; Agent-triggered company refresh actions must plan `company.public_web.refresh` and must not call the synchronous refresh service or mutate source/canonical rows directly; stale running `company.public_web.source.collect` may be cancelled only before provider attempt evidence or resumed through the command owner with canonical asset sync deferred; stale running `company.public_web.assets.materialize` may be cancelled only before sync attempt or resumed through the command owner without request-path CompanyAsset/CompanyEvidence writes; local asset readers must not derive favicon/logo fallbacks | `tests/test_company_public_web_assets.py`, `tests/test_pre_agent_contract_review.py`, `tests/test_operation_runtime.py`, `tests/test_projection_crm_api_contracts.py::test_asset_backfill_http_routes_are_explicit_migration_paths`, durable command owner metrics | Phase 7d/W11 slice active; Company Public Web source collection and asset materialization are split into typed commands, profile-experience company logo discovery is split from local apply and media cache, source collect has owner-specific pre-provider-attempt cancel+resume, assets materialize has owner-specific pre-sync cancel+resume, media cache owner is active, explicit historical company fact/logo backfills are migration-only, HTTP route-level migration-boundary preflight, fail-closed media read API, ActivityRun/Attempt/EntityDelta evidence, command-owner preflight, and fail-closed read APIs active; richer company tabs and running historical company fact/logo migrations remain open; frontend initials are placeholders only when `logo_unavailable` |
| CRM Public Web | `crm.public_web.queue_batch` and per-run phase commands (`crm.public_web.search.submit`, `crm.public_web.search.poll_fetch`, `crm.public_web.documents.fetch`, `crm.public_web.evidence.adjudicate`, `crm.public_web.model_safe.finalize`, `crm.public_web.signals.materialize`) owned by `crm_public_web_owner`; phase commands write `workflow_activity_runs`, `workflow_activity_attempts`, and run-level `crm_public_web_run` `workflow_entity_deltas`; `crm.public_web.documents.fetch` writes document-level `public_web_document` deltas from `document_fetch_payload.json`; `crm.public_web.evidence.adjudicate` owns AI identity/reviewability judgement and must preserve `link_assessments.user_visible_signal` / `review_queue_reason`; `crm.public_web.signals.materialize` writes signal-level `public_web_signal` deltas from `person_public_web_signals` and may expose reviewable links only when the AI output explicitly marks them user-visible; owner-specific running cancel and resume delegates through CRM Public Web owner and records command/activity/delta control state; route-local and worker-daemon Public Web execution are not normal paths; historical Public Web `agent_worker` rows are quarantine/migration evidence only | `enrich_person_public_web` action, CRM Public Web APIs | `tests/test_crm_public_web_runtime_boundary.py`, `tests/test_workflow_service_metrics.py`, targeted fake-provider signoff, guarded W7g runner dry-run | Active through W7f activity-spine slice; live quality validation uses `scripts/run_crm_public_web_live_product_validation.py`, which requires Makefile-provided `CRM_PUBLIC_WEB_LIVE_PRE_AGENT_CONTRACT_PASSED=1` for live mode, and remains before product signoff |
| Model-native Search | Reserved provider id `model_native_search`; no normal owner is registered yet; `build_search_provider(...)` must fail closed instead of silently falling back to DataForSEO, browser search, or DuckDuckGo | None in normal runtime; future experiments must add a typed command/activity owner and follow `docs/MODEL_NATIVE_SEARCH_PROVIDER_CONTRACT.md` | `tests/test_search_provider.py::SearchProviderTest::test_model_native_search_provider_order_fails_closed_without_contract`, `tests/test_settings.py::SettingsRuntimeOverrideTest::test_model_native_search_settings_are_explicit_and_disabled_by_default`, `tests/test_pre_agent_contract_review.py::test_model_native_search_contract_is_fail_closed_before_implementation` | Reserved/fail-closed; may only become `experimental_evidence_only` after explicit owner, cost, provenance, retry/circuit, export/audit, A/B validation, and independent-review evidence |
| Legacy target-candidate Public Web | Migration-only `legacy_public_web_storage.py`; normal HTTP aliases return `410`; new SQLite shadows and normal PG registries do not bootstrap retired tables; `scripts/archive_drop_legacy_public_web_tables.py` is the only archive/drop side-effect entrypoint | None | `tests/test_crm_public_web_runtime_boundary.py`, `tests/test_legacy_public_web_retirement_audit.py` | W7e closed for active runtime: local audit/archive/drop evidence exists; future historical reads remain migration-only |
| Excel intake | `excel.intake.run` command owner; async thread start/terminal/cancel effects are recorded through ActivityRun/Attempt/EntityDelta evidence | Export/import operation surfaces and HTTP route command drain; owner-specific command cancel for claimed/running intake commands | Durable owner metrics, Excel tests, operation runtime cancel regression | Activity-spine and cooperative running-cancel slices active; richer status/resume semantics remain W7/W9 |
| Export | `export.projection.generate`, `export.crm_public_web.generate` owners; generated artifacts are recorded through ActivityRun/Attempt/EntityDelta evidence | `export_candidates` action and export APIs | Durable owner metrics and export tests | Activity-spine slice active for projection and CRM Public Web export; bulk async status/approval polish remains W7/W9 |
| Frontend API | Contract adapter/schema and canonical backend endpoints | Operation/command/activity/projection read APIs | frontend contract/browser tests | Operation/Command/Activity contract active; Phase 13 product UI/action queue remains deferred |

## Agent Tool Result Field-Owner Matrix

These fields are separate contracts. Terminal link shape cannot be inferred by an inspect/readiness consumer, and
readiness cannot authorize or reinterpret a terminal result aggregate.

| Field | Owner | Source of truth | Allowed values | Derivation | Normal consumers | Forbidden consumers | Fallback | Migration status | Deletion condition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `agent_tool_terminal_aggregate.result_link_policy` | Exact historical `AgentToolSpec.behavior.result_link_policy` resolved from the server-owned `AgentToolRegistry`; PostgreSQL owns persisted terminal-aggregate enforcement | `AgentToolResultLinkPolicy` and the exact historical `(tool_name, tool_spec_version, tool_spec_digest)`; the slot exact-copies the resolved value to every attempt and journal row | `no_command_v1`, `workflow_command_acceptance_v1`, `activity_attempt_terminal_v1` | Schema-v1 specs use the frozen effect-class mapping (`read_only`/`commandless_action` to `no_command_v1`, `command_backed_action` to `activity_attempt_terminal_v1`); schema-v2 specs carry an explicit policy; reserve/prepare/accept re-resolve the historical spec and exact-compare all spec-derived pins before reads or writes | Result-slot reserve, result preparation/acceptance, accepted or quarantined attempt validation, immutable journal validation, exact replay/collision checks, and the deferred PostgreSQL terminal-aggregate constraint | Caller/tool-result payloads, HTTP/UI adapters, domain writers, result serializers, and `inspect_operation.result_readiness` must not supply, override, or infer this field | `fail_closed`: unknown/absent policy, missing historical spec, pin drift, or link-shape mismatch fails before schema bootstrap, owner reads, result writes, or aggregate commit | PG-only `0011_agent_tool_result_slots.sql` base plus quiesced/no-default `0013_agent_tool_result_link_policy.sql`; old writers fail closed; `0014` strengthens all-attempt effect/link shape without changing the owner | Keep every policy version lookup-capable while any retained historical tool spec, occurrence slot, attempt, or journal references it; delete only after those identities and rows have expired or migrated under a reviewed replacement |
| `start_acquisition_run.result_hold_release_owner` | `acquisition_start_v2_result_postgres` owns start-v2 command-acceptance terminal preparation, accepted-result UoW, dormant root-command hold release, accepted replay, and late-result quarantine | Exact start-v2 occurrence pins plus locked `agent_tool_result_slots`, approved preview owner row, receipt/planned Operation events, workflow seq1/seq2 root events, and `acquisition.run.create` root command owner-result target/ref/digest; accepted replay and late quarantine after downstream command progress use the immutable owner-result proof rather than mutable succeeded command/current-state projections | `pending_hold`, `released`, `accepted_replay`, `late_quarantined` | S1e2b create writes the dormant root command with `not_before_at=9999-12-31 23:59:59` and records the planned event as the terminal winner. S1e2c prepare reconstructs canonical `acquisition_start_result_v2` bytes from the locked owner. S1e2c accept writes attempt/slot/journal and clears the hold in one transaction. Later replay/quarantine validates the same owner target/ref/digest and canonical terminal bytes before any replay assertion or quarantine-attempt write | `prepare_start_acquisition_tool_result`, `accept_start_acquisition_tool_result_uow`, start-v2 root command consumer, recovery wakeup for released queued commands, and diagnostic result-slot readers | Generic Operation dispatch/control, generic workflow command cancel/retry/resume/claim/ready-list, provider/model callers, domain writers, UI/HTTP payloads, and downstream `acquisition.intent.resolve` must not infer or override the hold/release owner | `fail_closed`: malformed/stale/foreign occurrence pins, owner-result target/ref/digest drift, terminal serializer drift, missing slot/preview/event/command rows, or mixed v2/legacy command state rejects before primary-domain writes; foreign/missing callers get no compatibility fallback | Fixed-forward S1e2b/S1e2c PG-only candidate; public/default served population remains zero and live/provider gates remain blocked until independent review and release policy complete | Delete only after all retained start-v2 result slots, attempts, journals, and root commands have expired or migrated under a reviewed replacement owner-result contract |
| `inspect_operation.result_readiness` | `operation_query_service.result_readiness_projection` | Exact-owned `operation_runs.status` plus presence of the durable nonempty `operation_runs.result_ref_json`, read by the Operation query service after Action/Operation/workspace preflight | `pending`, `ready`, `failed`, `cancelled` | `ready` only for `completed` with a durable result ref; `failed` for failed; `cancelled` for cancelled; otherwise `pending`, including queued/planned/running and completed-without-ref | `inspect_operation` owner snapshot, model-safe inspect serializer, and read-only Agent/UI status display | Terminal-result acceptance, action/command control decisions, retry/dispatch authorization, domain mutation, repair/backfill, and `result_link_policy` derivation | `fail_closed`: owner/schema/semantic mismatch rejects the inspect projection, and completed-without-ref remains `pending` rather than becoming ready or not-applicable | Active read-only `operation_result_readiness_v1` projection; no fallback column, SQLite mirror, read repair, or migration bridge exists | Delete only after all inspect clients and historical tool specs migrate to a reviewed revisioned readiness owner/serializer and the old contract is no longer lookup-capable |
| `filter_projection.publication_foundation` | physical writer remains `serving_projection_owner`; `projection_search_service.filter_projection_publication_candidate` / `filter_projection_publication_candidate_v1` is only a field-validator candidate | Existing projection/member/route publication UoW plus dedicated carrier reservation/invalidation behavior; there is currently no durable exact-start-v2 -> committed Cohort result/snapshot/result-view -> projection join | `foundation_only_unbound` only; product owner, terminal owner, freshness/readiness owner, member-set commitment, and Agent reader are unratified | S1f0b may reserve/strip/invalidate shadow carriers in the existing UoW; S1f0c0-S1f0c2 must ratify and persist exact operation-native start/Cohort terminal lineage before S1f0d can publish a product owner | generic projection writer hardening and PG atomicity tests only | Agent/product reader, result-slot acceptance, exact-start E2E, legacy-job shell bridge, migration/backfill, provider/model/live, served derivation, or adoption of independent caller inputs as owner evidence | `fail_closed`: every Agent/product reader rejects foundation-only carrier; generic semantic membership change atomically removes parent/all-member carriers; collection always strips; malformed lineage writes no product owner/result | `64a7dfc` pinned Ultra review=`NO-GO 0/10/2/2`; fixed-forward status is `foundation_only_unbound`, zero DDL/backfill, served=0. Decision: [Track D D1n S1f0a](modules/serving-product/decisions/TRACK_D_D1N_S1F0_FILTER_PROJECTION_OWNER_DECISION.md); machine contract: [filter_projection_foundation_boundary_v1.json](modules/serving-product/contracts/filter_projection_foundation_boundary_v1.json) | replace only through reviewed S1f0c0-S1f0d contracts; foundation candidate identity must remain distinct and never be reinterpreted as the product owner |
| `operation_run.control_state` | Base owner: `operation_runtime.operation_run_control_state`. Sole start-v2 override classifier: `acquisition_start_v2_control.classify_acquisition_start_v2_generic_control_provenance`. `OPERATION_RUN_CONTROL_FAIL_CLOSED_OVERRIDE_REASONS` only validates the two classifier-selected reasons; it is not another classifier. The locked `inspect_operation` physical-acceptance validator is an integrity fence and cannot mint or reinterpret the classification | Base inputs are exact-owned `operation_runs.status`, `operation_runs.progress.phase`, linked `agent_actions.status`, approval status, and retry Operation identity. Classifier Action sources are `input`/`input_json` (closed `preview_id`, integer `preview_revision`, `preview_digest`), `target_ref`/`target_ref_json` (bound `workspace_id`, `requester_id`, and closed `start_snapshot` including snapshot schema/digest, preview, request/tool/result pins), `metadata`/`metadata_json.result_occurrence_ref` (closed `result_slot_id`, exact-integer `slot_generation`, `logical_occurrence_digest`), plus `action_type`, `owner_module`, `operation_type`, `workspace_id`, request schema version/digest, `tool_name`, tool spec version/digest, result schema version/digest, serializer owner/revision/contract digest, and `idempotency_key=agent-start-v2:<logical_occurrence_digest>`. Matching Operation sources are `action_id` and `workspace_id` equality to the Action, `owner_module`, `operation_type`, the same request/tool/result/serializer pins (including canonical `tool_name`), and the same idempotency key. Physical result-slot id/generation/digest and acceptance causality are separately locked/revalidated by the inspect owner after classification | Classifier values are exactly `non_v2`, `exact_v2`, and `partial_or_mixed_v2`. Public control actions remain an ordered subset of `dispatch`, `resume`, `retry`, `cancel`. `exact_v2` selects only `acquisition_start_v2_generic_operation_control_not_enabled`; `partial_or_mixed_v2` selects only `acquisition_start_v2_generic_operation_control_identity_mismatch`; both disable all four actions. Base disabled reasons remain the checked-in linked-action/status/retry reason set | `exact_v2` requires the closed bound Action contract and, when an Operation is supplied, the closed matching Operation contract. Any current Action/Operation request, tool, result, or serializer pin; a start snapshot marker; or a preview-key marker on `start_acquisition_run` establishes start-v2 provenance. Any such provenance that is incomplete, conflicting, type-aliased, drifted, or split is `partial_or_mixed_v2`; it must never downgrade to legacy controls. `non_v2` requires no start-v2 marker. Generic occurrence-shaped metadata alone on a non-start schema-less Action is intentionally not a marker. Exact linked-owner/workspace lookup precedes classification and masks foreign/missing rows; it is not a second provenance heuristic. The selected override is applied to the canonical base projector, preserving `control_source_of_truth=operation_runtime.operation_run_control_state`, `fallback_status=fail_closed`, and `module_state_mutated_on_control=false` | Operation detail/list, cancel/retry/resume/dispatch preflights and responses, locked Agent `inspect_operation`, frontend API adapter/Operation Queue, and `tests/test_pre_agent_contract_review.py::test_operation_run_control_state_is_contract_owned` callable cross-surface matrix | Authorization, provider/model routing, command claim/wake, ActionRegistry inference, domain mutation, result acceptance, UI-local status heuristics, reader repair, caller-minted provenance/reasons, and any parallel classifier. Physical-acceptance validation may reject corrupt evidence but may not turn `partial_or_mixed_v2` into `non_v2` or `exact_v2` | `fail_closed`: missing linked owner uses the canonical linked-owner disabled state; exact start-v2 is unsupported; partial/mixed/hostile start-v2 is invalid; unknown override reasons, non-rederivable projections, or corrupt locked acceptance evidence reject before mutation/result writes. A genuine no-marker `non_v2` row retains the existing open/schema-less compatibility path without read repair | Fixed-forward PG-only S1e2b candidate with no DDL/backfill. R-019 aggregate atomicity and R-029 remaining schema-less `5/15` bridge stay open; default/public served Agent tool population remains zero, and provider/model/live authority remains zero until a fresh scope-matched independent review and release gates | Delete only after every retained Operation detail/list/control client, frontend adapter, and historical `inspect_operation` result contract has migrated to a reviewed replacement classifier/projector, no retained Action/Operation/result occurrence depends on these pins or reasons, and the old classifier, reason registry, and projection are no longer lookup-capable |
| `operation_runs.progress.reason` | Exact registry owners only: `operation_runtime.OperationRuntimeWriter.control` owns cancel/retry/resume, `orchestrator.projection_reselection` owns projection reselection, and `orchestrator.crm_writer_approval` owns CRM approval-required transitions | PG-only `operation_runs.progress_json`; `operation_runtime.OPERATION_PROGRESS_REASON_REGISTRY` is the checked-in owner/phase/code namespace with globally unique codes; repository reason-bearing writes require the matching explicit owner; operator-authored text belongs only to the matching append-only `operation_events.payload_json.reason` | Absent, or one globally unique code minted through its exact owner/mint-time-phase triple: `cancelled/operation_cancelled`, `queued_retry/operation_retry_requested`, `resume_requested/operation_resume_requested`, `reselection_required/projection_membership_revision_stale`, `reselection_required/projection_read_membership_revision_missing`, `approval_required/bulk_crm_stage_update_requires_approval`, `approval_required/sensitive_crm_stage_requires_approval`, or `approval_required/bulk_add_to_crm_requires_approval` | Each producer resolves an exact plain-string owner/mint-time-phase/code registry entry before its first state write and passes the owner with the same progress patch; the repository independently rejects any explicit new/replaced reason without that registered triple before its own read/native-write path. A later phase-only patch preserves the last registered reason as transition provenance; omission never remints it under the new phase. A patch that omits `reason` also does not validate, normalize, or rewrite an existing brownfield value. Trimmed operator cancel/retry/resume text is copied only to the matching event payload | Authenticated Operation API/provenance diagnostics; retained schema-valid `inspect_operation` v1/v2 preparation and immutable accepted-result replay; and the non-model v3 raw-progress fingerprint | Model-visible `inspect_operation` v3, user-facing status copy, authorization, control-policy/readiness/result-link derivation, provider routing, domain mutation, caller-minted owner/code namespaces, and any fallback inference; consumers must not derive the current phase from a retained reason; Operations UI uses `control_state`, `status_summary`, phase, and event provenance instead | `fail_closed`: an unregistered owner/mint-time-phase/code or non-plain carrier fails before any state write, and the repository boundary rejects it before its own read/native-write path; absent or brownfield text cannot authorize or derive state; canonical status/control comes from Operation status plus phase/control owners, v3 omits the field, and no reader repairs the row | Fixed-forward writes use the eight registered triples. Phase-only updates preserve an already minted unique code as provenance rather than treating it as a new reason write. Existing free-form rows remain byte-untouched when a patch omits `reason`; authenticated diagnostics may read them, v3 omits and fingerprints them, already accepted historical v1/v2 result bytes replay unchanged, and fresh v1/v2 preparation fails its retained schema rather than normalizing free-form text. New operator event text is trimmed and bounded to 500 characters before any runtime-writer repository read or write | Remove brownfield compatibility only after no retained v1/v2 occurrence/result or Operation row needs it and all authenticated diagnostic consumers use typed machine state plus event provenance; any registry deletion requires its own retained-row/result inventory and reviewed migration, never silent reinterpretation |

Owner-preflight ordering is part of the `operation_run.control_state` contract: an `Operation.action_id` mismatch is
`partial_or_mixed_v2` only when the pure classifier is called directly on both supplied records. Public detail/control
and locked Agent inspect must first require the exact linked Action; they mask that mismatch as `not_found`, while list
omits the unlinked Operation. They must not expose the identity-mismatch override as an ownership oracle. Conversely, a
real schema-less non-start Action/Operation with no start-v2 marker remains `non_v2/ready`; detail/list, locked Agent
serialization, and frontend mapping retain the base control projection without either start-v2 override reason.

## Confirmed Architecture Decisions

### W11d Operation-Native Discovery

Direction confirmed: `acquisition.scale.plan` must not turn `acquisition_discovery_lanes` into a mini workflow engine. W11d introduces a generic activity spine and a domain read model:

- PG-only `workflow_activity_runs` is the bounded side-effect boundary for provider submit/poll/fetch, parsing, export chunking, and other long-running owner work.
- PG-only `workflow_activity_attempts` records individual execution attempts, provider refs, retry/rate-limit state, errors, input/output envelopes, and artifact refs.
- PG-only `workflow_entity_deltas` records explainable entity-level effects from activities, including no-op / not-applied reasons before a candidate, evidence, asset, assertion, or projection membership is produced.
- PG-only `acquisition_discovery_lanes` is acquisition-specific current-state/read-model materialization. It references `source_command_id`, `activity_run_id`, `provider_ref`, `artifact_refs`, `entity_counts`, and `downstream_command_ids`; it does not own provider execution state.
- `acquisition.scale.plan` may materialize planned lanes/activity rows, but executable `linkedin.discovery_query.run` commands must be consumed by the operation-native discovery owner from `acquisition_run_id` / `lane_id` / `activity_run_id`, without legacy job shell, `job_id`, and snapshot shell requirements.
- Provider-backed discovery execution writes candidate/evidence deltas through `workflow_entity_deltas` before projection/profile-refill commands consume them.

The long-term Agent-facing contract is Operation/Command/Activity, not lane/table mutation. Agent may query lanes for explanation through read-only `/api/workflow/discovery-lanes`, but retry/cancel/resume/debug controls target typed command/activity APIs.

Current implementation status: first slice adds PG-only activity/lane/entity-delta storage and makes `acquisition.scale.plan` materialize `workflow_activity_runs` and `acquisition_discovery_lanes` with `planned_pending_owner`. A running `acquisition.scale.plan` can be cancelled only before a discovery command exists and before any activity attempt starts; the owner-specific cancel marks the linked acquisition run, activity run, and discovery lane `cancelled_before_discovery`. `continue_acquisition_run -> linkedin.discovery_query.run` now requires `acquisition_run_id`, `lane_id`, and `activity_run_id`; legacy `job_id`/`snapshot_id` discovery payloads are rejected as normal Agent input. The `operation_native_discovery_activity_owner` recovery phase claims these commands, records `workflow_activity_attempts`, calls the provider-backed discovery primitive with `runtime_mode=operation_native_discovery`, writes model-safe activity artifacts, records positive candidate `workflow_entity_deltas`, updates the acquisition lane/read model, and plans downstream `linkedin.profile_fetch.activity.run` commands for W11e. Retryable provider failures remain retry-wait activity attempts. The legacy search-seed queue owner skips operation-native commands report-visibly and does not claim them; the legacy job shell discovery owner is not invoked by this boundary. Activity, attempt, entity-delta, and discovery-lane query rows now expose `control_target` back to the owning `workflow_command` plus its `display_contract`, `control_policy`, `control_state`, `activity_spine_policy`, and `fallback_status=fail_closed`, so Agent/debug callers never need to infer titles, categories, retry/cancel/resume behavior, or Activity-spine requirements from lane or evidence rows.

### W11e Operation-Native Profile Fetch Activity

Direction confirmed: Agent-facing profile fetch must use the generic Activity spine, not the legacy job-scoped profile refill shell. `fetch_profile_sample` defaults to `linkedin.profile_fetch.activity.run`, and `continue_acquisition_run` may plan the same command from discovery entity deltas. The command owner is `linkedin_profile_activity_owner`.

- `linkedin.profile_fetch.activity.run` consumes profile URLs or candidate `workflow_entity_deltas`.
- The owner records a `workflow_activity_run`, a local cache/planning `workflow_activity_attempt`, and profile `workflow_entity_deltas`.
- Registry cache hits become `profile_cache_hit` deltas; unfetched profiles become `profile_fetch_required` deltas with `provider_owner_required=true`.
- The command does not create a legacy job shell, does not call `queue_workflow`, and does not invoke `linkedin.profile_refill.submit_batch` as an Agent normal path.
- Claimed/running `linkedin.profile_fetch.activity.run` commands support owner-specific cancel only before cache lookup starts, before any profile EntityDelta is recorded, before downstream commands are planned, and after lease expiry or explicit force. The owner marks the linked activity/run state `cancelled_before_cache_lookup`; after cache lookup or downstream planning begins, cancellation fails closed and recovery must resume/retry through Command/Activity APIs.
- W11f adds `linkedin.profile_fetch.provider.fetch`, which consumes `profile_fetch_required` deltas, records provider `workflow_activity_attempts`, writes fetched raw-path evidence into `linkedin_profile_registry`, and emits `profile_provider_fetched` deltas.
- W11f follow-up adds `linkedin.profile_terminal.admit`, which consumes `profile_cache_hit` / `profile_provider_fetched` deltas and emits `profile_terminal_recorded` deltas. It deliberately does not mutate projection membership; the projection-admission owner must consume terminal deltas in a later W11 slice.
- W11g adds `projection.profile_admission.apply`, which consumes `profile_terminal_recorded` deltas, writes canonical run-scope `serving_projection_members` through `serving_projection_owner`, and emits `projection_member_admitted` deltas. It is the only normal Agent-callable path from operation-native profile terminal facts into projection membership.
- W11h keeps current run visibility separate from background asset upkeep: after operation-native projection admission, existing `projection.person_search_index.build` and `collection.authoritative.merge` commands are planned on the same Operation workflow id without a legacy job shell or normal-path `job_materialization_items` write. Their command owners record ActivityRun/Attempt/EntityDelta evidence for index pages and collection pointer effects, so Agent/debug queries do not need to infer those effects from command result JSON.
- W11h follow-up applies the same evidence rule to profile URL terminal recording, local apply, board-visible publication, legacy job result-view projection publication, and facet/layering builds: `linkedin.profile_url_terminal.record` records per-profile-url `profile_url_terminal_record` EntityDeltas before local apply can treat registry terminal state as proven; `linkedin.local_profile_delta.apply` records `local_profile_delta` EntityDeltas for completed, partial, waiting-prerequisite, retry-wait, failed, and already-completed effects; `projection.board_visible_patch.publish` records `board_visible_patch` EntityDeltas for completed, waiting-prerequisite, retry-wait, and failed board publication effects; `projection.run_scope.finalize` records `run_scope_projection` EntityDeltas for published, obsolete, retry-wait, and failed finalization effects; `projection.facet_layering.build` records `facet_layering` EntityDeltas for completed, partial, retry-wait, and failed layering effects. These commands are not automatically Agent-callable; exposure still requires the action registry allowlist.
- W7f follow-up brings CRM Public Web run phases onto the same Activity spine: each per-run phase command records one ActivityRun, one ActivityAttempt, and a run-level `crm_public_web_run` EntityDelta keyed by CRM Public Web run id. `crm.public_web.documents.fetch` additionally records document-level `public_web_document` EntityDeltas from `document_fetch_payload.json`, and `crm.public_web.signals.materialize` records signal-level `public_web_signal` EntityDeltas from the materialized `person_public_web_signals` read model plus canonical `person_asset` / `person_evidence` EntityDeltas for the PersonAsset layer under the same activity id. These deltas do not create a separate Public Web state machine.
- W7d/W11 follow-up brings export owners onto the same Activity spine: projection export and CRM Public Web export commands record ActivityRun/Attempt evidence and an export EntityDelta with artifact refs. Export rows/artifacts remain read models/output artifacts, not execution truth.
- W7c/W11 follow-up brings Excel intake onto the same Activity spine: `excel.intake.run` records an ActivityRun/Attempt when it starts the async local owner and records an `excel_intake_job` EntityDelta when the thread reaches a terminal status. Claimed/running Excel intake commands now support cooperative owner-specific cancel through `excel_intake_owner.cancel_excel_intake_run_command`; cancellation records a durable command marker, cancelled Activity/Attempt/EntityDelta evidence, and thread/materialization checkpoints before terminalization. Claimed/running resume goes through `excel_intake_owner.resume_excel_intake_run_command`, records control evidence, and requeues without starting a local thread or writing intake materializations from the request path.
- W9/W11 follow-up brings CRM writer commands onto the same Activity spine: `crm.record.add_from_projection`, `crm.record.update`, `crm.note.add`, and `crm.task.create` record ActivityRun/Attempt evidence and EntityDeltas for the CRM record plus event/note/task effects. CRM tables remain the CRM read model and audit state; they are not the execution owner. Running CRM writer commands expose owner-specific cancel before mutation attempt through `crm_writer.cancel_before_mutation_attempt` and owner-specific resume through `crm_writer.resume_crm_writer_command`; cancel is allowed only before ActivityAttempt/EntityDelta/downstream command creation and after lease expiry or explicit force, while resume may only record control evidence and requeue the command. CRM mutation remains owned by the next `crm_writer` command claim.
- W11 company Public Web refresh now uses the same Activity spine with a split root/phase boundary: `refresh_company_public_web_assets` actions plan `company.public_web.refresh`; the root command only plans `company.public_web.source.collect`; source collection writes or joins source-specific company Public Web rows/artifacts with canonical asset sync deferred; it then plans `company.public_web.assets.materialize`, which syncs model-safe facts into `CompanyAsset` / `CompanyEvidence` and records `company_public_web_run`, `company_asset`, and `company_evidence` EntityDeltas. The source-collection join branch must not opportunistically sync canonical company facts. The synchronous company Public Web refresh service remains a manual API/CLI service entry, not the Agent normal path.

### Future Activity-Spine Extension Rule

Any new Agent-callable bounded side effect must first add or reuse a typed `workflow_commands` boundary, explicit `workflow_command_control_policy`, explicit `workflow_command_activity_spine_policy`, `workflow_activity_runs`, `workflow_activity_attempts`, and `workflow_entity_deltas` evidence. Domain tables may be added only as efficient read models for product queries, and they must reference the command/activity causality instead of owning retry, cancel, resume, provider attempts, or recovery state.

Agent, UI, or recovery code must never directly mutate lane, registry, projection, CRM, Public Web, Excel, export, evidence, or asset rows to simulate execution progress. They must call Operation/Command APIs or owner-specific command handlers, then read Activity/Attempt/EntityDelta/domain read-model rows for explanation.

## W10 Exit Criteria

## Current Completion Evidence Matrix

This matrix is the current completion audit for the pre-Phase-13 refactor scope. It does not replace the normal test output; it records which fast gates prove each boundary and which gates remain external or direction-dependent.

| Scope | Current evidence | Remaining gate |
| --- | --- | --- |
| W7e legacy Public Web physical deletion / isolation | `tests/test_crm_public_web_runtime_boundary.py`, `tests/test_legacy_public_web_retirement_audit.py`, and `scripts/audit_legacy_public_web_retirement.py --strict` prove the legacy target-candidate facade is physically absent, normal imports/storage writes fail closed, legacy rows are zero in the active runtime audit, and historical access is migration-only through `legacy_public_web_storage.py`. | None for normal runtime. Future historical access remains migration-only cold evidence. |
| W7f CRM Public Web typed-command phases | `tests/test_operation_runtime.py`, `tests/test_crm_public_web_runtime_boundary.py`, durable owner metrics, and `make ci-pre-agent-contract` prove `crm.public_web.queue_batch` plans per-run phase commands, phase commands record ActivityRun/Attempt/EntityDelta evidence, route-local/worker-daemon execution is not normal path, and W7g dry-run guards canonical endpoint/owner/model behavior before live validation. | W7g real live/product validation requires reviewed CRM record ids plus explicit live-provider confirmation; it is product-quality evidence, not a missing typed-command boundary. |
| W11 Agent-callable workflow atomization | `tests/test_operation_runtime.py`, durable command-owner signoff, and W10 preflight prove acquisition root/intent/plan/review/commit, probe/scale/discovery, profile fetch/provider/terminal/admission, projection index/collection merge, local apply/board-visible/run-scope/facet/snapshot, CRM Public Web phases, export, Excel, company Public Web, media cache, and CRM writer commands use Operation/Command/Activity surfaces with read-only Activity/Attempt/EntityDelta/Lane status APIs. Provider ActivityAttempt after-start control v1 is confirmed: Harvest/Apify/DataForSEO use local poll-cancel/quarantine where no EntityDelta/downstream effect exists, and Document fetch/Qwen remain terminal fail-closed. | Minimal `/operations` exists; broader product UI polish remains W9/Phase-13-adjacent, not an atomization blocker. |
| W10 full contract review / fail-closed signoff | `make ci-pre-agent-contract` is the unified fast gate: W10 ledger preflight, Operation runtime tests, PG-backed profile retry-wave isolation, DataForSEO item-level batch retry isolation, W7e boundary/audit, storage guardrails, asset backfill HTTP contract, durable command-owner signoff, and W7g guarded dry-run. It runs before W6/nightly, Phase 13, or live-provider validation. | Full completion still needs the external W7g live pass if product signoff requires live provider quality evidence; any stronger provider remote-interrupt semantics must first update this Contract. |
| Independent review gate | `docs/INDEPENDENT_REVIEW_GATE.md`, `docs/INDEPENDENT_REVIEW_BRIEF.md`, `scripts/run_independent_review_gate.py`, and `make independent-review-gate` define a non-author, read-only, adversarial GO/NO-GO review layer for mandatory triggers: Contract docs/schema/owner/source-of-truth/readiness/filter/export/status/projection/permission fields, frontend/backend API semantics, provider/model behavior, durable runtime command/activity/recovery/idempotency/outbox/causality, migration bridge/fallback/legacy deletion, Public Web quality/materialization/export/promotion, CRM/person/company assets, Agent/Operation control surfaces, and milestone closeout. Codex reviews inherit the operator-maintained newest model, highest supported reasoning effort, and service tier from `~/.codex/config.toml`; the runner adds no model/effort/tier CLI pins. `independent_review_effective_config_v2` binds `reviewer_exit_code=0`, Codex CLI/session/thread identity, prompt/events/raw-output/config/rollout digests, actual settings parsed again from the rollout, and a canonical pinned-Git scope digest. The shared verifier is used by destructive-asset and W7g live gates, so hand-written/self-reported metadata, prior-scope replay, reroutes, dirty-tree reference output, nonzero exits, or tampered evidence fail closed. | Required after targeted/fast preflight and before live provider validation, W6/nightly, founder/manual signoff, Phase 13, or claiming a complete feature/milestone. Small local-only changes may skip only when no shared contract, provider/model behavior, storage schema, read/write owner, frontend/backend field, action semantics, gate, or milestone readiness changes. `NO-GO` blocks only the affected scope unless fixed or explicitly accepted by the user/founder and recorded in the review artifact/TODO; unrelated development continues. |
| Model-native Search experiment gate | `docs/MODEL_NATIVE_SEARCH_PROVIDER_CONTRACT.md`, settings parsing, `build_search_provider(...)`, and `make ci-pre-agent-contract` prove `model_native_search` is reserved and fail-closed until a typed provider/command owner exists. | Future A/B validation can compare DataForSEO-only vs DataForSEO+model-native Search, but it cannot be a hidden fallback or normal source before owner/cost/provenance/export/audit contracts and independent review pass. |

## Direction Gates Before Closing This Goal

These gates are not implementation TODOs that can be closed by adding another fallback, hidden bridge, or W6/nightly assertion.

| Gate | Required input before implementation/signoff | Normal-path prohibition | Allowed next step |
| --- | --- | --- | --- |
| W7g CRM Public Web live/product validation | Reviewed CRM record ids and explicit live-provider confirmation plus independent review evidence. The guarded runner must still execute `ci-pre-agent-contract`, require `CRM_PUBLIC_WEB_LIVE_PRE_AGENT_CONTRACT_PASSED=1`, require `--reviewed-crm-record-ids` / `CRM_PUBLIC_WEB_LIVE_RECORD_IDS_REVIEWED=1`, require exact `CRM_PUBLIC_WEB_LIVE_FORCE_REFRESH=1` / `--force-refresh`, require `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_PASSED=1` plus a valid `GO` `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_ARTIFACT` under `runtime/reviews/`, and require `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_SCOPE_DIGEST_SHA256` from the exact pinned request. It uses canonical `/api/crm/records/public-web...` routes and rejects legacy target-candidate endpoint/owner markers. Missing force refresh fails before any backend/provider request, so a prior terminal run cannot masquerade as current product/model evidence. The dry-run report must expose `live_prerequisites`, `record_id_selection_guidance`, `recommended_make_command`, and independent-review artifact readiness so operators can see exactly what remains before live execution. The immutable minimum artifact scope tokens are `W7g`, `CRM Public Web`, and `live`; the immutable required-file set is the complete canonical W7g command in `docs/INDEPENDENT_REVIEW_GATE.md` (`Makefile`, live/product code and tests, PRE_AGENT/DURABLE/review/testing contracts, plus the independent-review runner, shared artifact verifier, and their dedicated test). Environment values may add tokens/files but cannot replace that minimum. The exact title is `W7g CRM Public Web live product validation`; the shared verifier must match that title and `review_scope_digest_sha256`. An unrelated prior `GO`, dirty-tree reference review, or wrong request digest cannot unlock this gate. `NO-GO`, timeout, no-output, invalid, missing-metadata, missing-scope-file, missing/wrong-scope-digest, prompt-file, or unrelated artifacts must fail closed. | Do not mark W7g product validation complete from dry-run, fake-provider, Qwen healthcheck, or W6/nightly evidence alone. Independent review is also not sufficient by itself; it is a pre-live gate, not product-quality evidence. Do not re-enable `/api/target-candidates/public-web...` as a validation shortcut. | Run `make test-crm-public-web-live-product-validation LIVE_CONFIRM=1 CRM_PUBLIC_WEB_LIVE_DRY_RUN=0 CRM_PUBLIC_WEB_LIVE_RECORD_IDS_REVIEWED=1 CRM_PUBLIC_WEB_LIVE_FORCE_REFRESH=1 CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_PASSED=1 CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_ARTIFACT="runtime/reviews/<review>.md" CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_SCOPE_DIGEST_SHA256="<review_scope_digest_sha256>" CRM_PUBLIC_WEB_LIVE_RECORD_IDS="..."` only after the reviewed ids, live-cost confirmation, and valid independent review `GO` artifact are provided. |

Model-provider validation is part of W7g, not an optional log check. For OpenAI-compatible relays, `/models` success is only inventory evidence; the healthcheck must also prove a minimal call through the configured generation API (`/responses` for `api_style=openai_responses`, otherwise `/chat/completions`) succeeds with the configured model. That proof is bounded: healthcheck results are cached and a failed generation opens a provider/model circuit breaker, so frontend/API polling cannot become an unbounded non-streaming LLM call loop. Public Web AI adjudication may fail closed, but it must expose `provider`, requested `model`/`model_version`, `fallback_used`, `fallback_reason`, and `model_error` through artifacts and normal detail/phase metrics. Normal CRM Public Web real-provider adjudication checks configuration before provider transport and permits only the centrally owned `CRM_PUBLIC_WEB_PRODUCT_MODEL` (`gpt-5.6-sol`) over the OpenAI-compatible client; a different or empty configured model produces `model_configuration_mismatch` without a provider request and without opening the shared model circuit, so unrelated shared-client calls remain available. Qwen is a real provider rather than a valid product-model substitute, so its Public Web adjudication returns the same configuration fallback before any Qwen prompt while its non-Public-Web methods remain available. Deterministic, offline/scripted, and fake test clients are outside this real-provider constraint and retain fail-closed test behavior. Every real W7g invocation requires force refresh before even requesting provider health, and its start payload must carry `force_refresh=true`; neither direct-script environment input nor a non-`1` Make value can bypass this. The W7g product model is immutable at `gpt-5.6-sol`; Make/environment input cannot lower it, and the direct CLI compatibility flag fails before backend/provider access on any other value. Guarded live validation must fail when `fallback_used=true`; for every model-verifiable terminal run, the single canonical `latest_run.analysis.phase_metrics` object must co-locate exact `model`, `model_version`, `requested_model`, `response_model`, `effective_model`, and `model_identity_provenance=provider_response`. Empty values, conflicts, fields spread across unrelated payload objects, or provider-health fields used to fill run evidence fail closed. Provider health independently proves the same requested/response/effective identity inside `providers.model`; it cannot silently accept deterministic fallback, a different or aliased model, a model-list-only healthcheck, or repeated calls during circuit-open cooldown. Start must return a nonempty batch id and one run id per requested CRM record; every poll must return that exact batch/run mapping, and each detail's `latest_run` must match it. Record-only fallback polling or stale/concurrent latest runs cannot satisfy this invocation's model proof.

Provider after-start control v1 is confirmed and is no longer a direction gate. Harvest/Apify/DataForSEO use local `poll_cancel_late_result_quarantine`: once a provider ActivityAttempt exists but before any EntityDelta or downstream command, command cancel may stop local polling/waiting, mark the remote run ignored, and terminalize ActivityAttempt evidence as locally cancelled. Document fetch and Qwen adjudication use `fail_closed_until_terminal`: no mid-attempt cancel is exposed, and retries start only after terminal failure. Provider command `control_policy` is machine-readable through `provider_after_start_control_contract='w11_provider_after_start_control_v1'`, `provider_after_start_control_status='active'`, and `provider_after_start_control_mode='poll_cancel_late_result_quarantine'` for Harvest/Apify/DataForSEO families. Do not implement remote interruption from the workflow command API. Do not mutate provider registry, projection, CRM, Public Web, Excel, media, asset, or lane rows from a control endpoint to simulate cancellation. A late provider result must be quarantined unless an explicit owner adoption command accepts it.

Provider after-start control is an extension of durable Activity/Attempt semantics, not a replacement for item-level business retry contracts. LinkedIn profile fetch keeps the old scheduler invariant: normal-wave successful URLs continue through terminal/profile admission immediately, while only failed or unresolved URLs enter a retry bucket. Operation-native `linkedin.profile_fetch.provider.fetch` must therefore emit `profile_provider_fetched` for successful URLs, emit `profile_provider_retry_bucketed` for failed URLs, and plan a retry-wave provider command with `provider_attempt_scope='retry_wave'`, `retry_wave_index`, `retry_unit='linkedin_url_key'`, and `retry_strategy='bucketed_entity_retry_after_normal_wave'`. It must not mark the whole provider command `retry_wait` merely because one URL failed, must not retry successful URLs with failed URLs, and must not mix retry-wave URLs back into unfinished normal-wave work. After the retry budget is exhausted, failed URLs emit `profile_provider_retry_exhausted`; late/old provider results remain quarantined evidence until a future explicit owner command adopts them.

The same rule applies to DataForSEO Standard Queue batch transport. A batch `task_post` can carry many query tasks, but every query has a caller-assigned stable `task_key` analogous to the LinkedIn URL key. That key must be sent as the provider task tag, returned as `query_identity_key`, and used by downstream joins/retries/manifests/outcomes; array order and candidate display ordinal are display compatibility only. Public Web query task keys must be scoped to stable candidate/query identity, not the candidate's position in the current batch. Search seed discovery worker keys must likewise be scoped to stable search query signature, bundle, and employment scope, not the query's ordinal in the current query list. Exploratory enrichment query keys must be scoped to stable candidate/query identity, not `candidate_id::index`. Provider/search-chain code must fail closed when a batch spec lacks `task_key`; it must not generate identity from query text, task id, candidate ordinal, or array position. Provider response mapping is allowed only through provider-echoed `tag` or unambiguous provider-echoed `keyword`. Missing provider identity is an item-level retry/failure, not permission to bind by request order. Failures are query-item outcomes: successful task ids remain submitted, only failed query tasks can be retried, and the retry metadata must expose `retry_unit='search_query'` plus `retry_strategy='dataforseo_batch_failed_query_retry_only'`. A batch `task_get` can fetch many ready task ids concurrently, but one failed `task_get` must not discard sibling successes; failed fetch evidence is scoped to `retry_unit='dataforseo_task_id'` with `retry_strategy='dataforseo_task_get_failed_task_retry_only'`. DataForSEO readiness probes must report provider-pending status codes `40601` and `40602` as waiting metadata, not terminal failure; after a bounded poll budget, an owner may reset only the affected query task with the same `task_key`, preserve `previous_task_ids`, and report `provider_task_reset_reason`, but must not resubmit sibling queries or the enclosing batch. Provider-pending long-tail waiting has its own `max_provider_pending_wait_seconds` hard budget. `max_remote_search_wait_seconds` may terminalize unknown never-ready tasks, but it must not terminalize explicit `40601` / `40602` tasks before the provider-pending budget; those tasks stay `waiting_remote_search` with `provider_pending_wait_deferred` visible in metrics/checkpoint. Public Web, seed-discovery, and exploratory-enrichment readers must consume these as item-level checkpoints rather than falling back to whole-batch retry or whole-batch failure.

- No normal source imports the retired target-candidate Public Web facade.
- No normal source calls legacy target-candidate Public Web storage helpers.
- No new durable/Operation/W11/CRM task/company asset current-state table has SQLite normal-path DDL or fallback. Current PG-only set includes `workflow_events`, `workflow_current_state`, `workflow_commands`, `runtime_outbox`, `agent_actions`, `operation_runs`, `acquisition_runs`, `workflow_activity_runs`, `workflow_activity_attempts`, `workflow_entity_deltas`, `acquisition_discovery_lanes`, `operation_events`, `crm_tasks`, `company_assets`, `company_evidence`, and `company_assertions`.
- All normal public/read APIs identify their source of truth and fail closed when the source is not ready.
- Candidate-document fallback remains an explicit historical asset/reuse compatibility path only: the env gate defaults off, normal public projection readers use canonical materialized artifacts with `allow_candidate_documents_fallback=False`, and any explicit fallback use must stay outside Agent execution truth.
- Projection filter fallback remains migration/debug-only. Normal `/api/projections/{projection_id}/candidates` and search/filter APIs must use `projection_person_search_index` / canonical projection facet rows or return `projection_person_search_index_unavailable`; `SOURCING_ALLOW_LEGACY_PROJECTION_FILTER_SCAN_FALLBACK` must never become a normal Agent/UI path.
- Migration-only env inventory is part of W10 signoff. `SOURCING_ALLOW_LEGACY_TARGET_PUBLIC_WEB_ENDPOINTS` is permanently ignored and must not re-enable legacy Public Web execution; `SOURCING_ALLOW_LEGACY_TARGET_PUBLIC_WEB_TO_CRM_SYNC` is reviewed historical sync only; `SOURCING_ALLOW_LEGACY_TARGET_CANDIDATE_EXPORT` is controlled migration/test export only; `SOURCING_ALLOW_LEGACY_JOB_RESULT_ENDPOINTS` is migration/test compatibility only; `SOURCING_RETIRE_LEGACY_JOB_RESULT_ENDPOINTS` may force cutover behavior for tests/operators but must not restore legacy composition as a normal reader; `SOURCING_ENABLE_LEGACY_CANDIDATE_DOCUMENTS_FALLBACK` and `SOURCING_ALLOW_LEGACY_PROJECTION_FILTER_SCAN_FALLBACK` are the explicit fallback gates above. `SOURCING_ALLOW_MIGRATION_PROJECTION_FACET_LAYERING_OVERLAY` is retired documentation-only evidence; it must not enable overlay execution. `SOURCING_ENABLE_SQLITE_PROFILE_REGISTRY_FALLBACK` is a retired storage-env name and must not appear in production source or scripts. Any new legacy/fallback env that affects public reads, workflow execution, export, CRM, or Agent-callable surfaces must be added to this inventory and a fast preflight before use.
- Durable command owner metrics cover every normal command family used by Operation/Agent surfaces.
- Company Public Web refresh preflight must prove `company.public_web.refresh` is owned by `company_public_web_owner` as an orchestration command, and that `company.public_web.source.collect` / `company.public_web.assets.materialize` are the Activity/Attempt/EntityDelta-backed phase commands. Command result JSON or source-specific `company_public_web_assets` rows alone cannot satisfy Agent execution proof.
- Media cache preflight must prove `media.asset.cache` is owned by `media_asset_owner`, records Activity/Attempt/EntityDelta evidence, and writes stable `PersonAsset.avatar_media` or `CompanyAsset.logo_media` through the canonical asset writers. Provider avatar/logo hotlinks remain metadata until this owner writes a stable asset.
- Explicit historical backfill routes must be covered at HTTP level before W6/nightly. `POST /api/persons/backfill-public-web-signals`, `POST /api/company-assets/backfill-public-web-assets`, `POST /api/media/backfill-person-avatars`, and `POST /api/media/backfill-company-logos` default to dry-run, report `normal_reader_repair=false`, and either mutate canonical asset/evidence state only on explicit apply or plan `media.asset.cache` commands without request-path fetch. They are included in `make ci-pre-agent-contract` through `tests/test_projection_crm_api_contracts.py::test_asset_backfill_http_routes_are_explicit_migration_paths`. Operator execution must use the PG-only dry-run-first script `scripts/backfill_person_company_asset_media.py`; direct ad hoc route calls are acceptable for debugging but not for signoff evidence.
- All HTTP routes whose path contains `/backfill` are operator/migration surfaces unless a future Contract explicitly promotes one into an Operation action. This includes CRM, projection, person raw/evidence index, media, company asset, and Public Web promotion backfills. They must not appear anywhere in frontend normal source (`frontend-demo/src/**/*.ts(x)`), frontend normal contracts/adapters, `/operations`, or `operation_runtime.ActionRegistry`; normal product repair must be represented as a reviewed Operation/Command/Activity owner instead of direct route calls.
- Migration diagnostics are operator-only surfaces, not Agent/UI product APIs. `/api/migrations/legacy-public-web` and `/api/migrations/legacy-result-endpoints` may be called by reviewed operational tooling and signoff checks, but they must not appear anywhere in frontend normal source (`frontend-demo/src/**/*.ts(x)`), frontend API contracts, frontend adapters, or normal `/operations` action surfaces. If a product needs historical migration status later, it must add a new read-only Operation/Command/Activity summary with an explicit owner instead of exposing migration routes directly.
- Historical asset/media backfill apply must be reviewed. The four routes above reject `dry_run=false` with `operator_review_required` unless `reviewed=true` is present. Responses must expose `read_contract.apply_requires_reviewed=true` on rejected apply attempts. The operator script enforces `--apply requires --reviewed` and passes `reviewed=true` to the route payload only after the dry-run report has been inspected.
- Historical asset/media backfill is not an Agent/UI normal action surface. The four route paths above must not appear in frontend normal API contracts or adapters, and `media.asset.cache` must not be allowlisted by `operation_runtime.ActionRegistry` as a product action. Agent-callable media/company/person work should use canonical reviewed actions such as company Public Web refresh or a future reviewed asset operation; historical repair remains operator-only until a new Contract review promotes it.
- Agent-facing profile fetch defaults to `linkedin.profile_fetch.activity.run`; legacy `linkedin.profile_refill.submit_batch` is not an allowed Operation action command type.
- Operation action registry command exposure is blocked at registration time: `operation_runtime.ActionRegistry` rejects missing display labels/categories/descriptions, unregistered commands, duplicate command types, default commands outside the allowed set, and command policies that are not Agent-callable. Contract enrichment is centralized in `operation_runtime.ActionRegistry.to_record()`. HTTP/API handlers must expose that record, not independently reconstruct command owner/control/activity policy.
- Operation action display must be registry-owned. `operation_runtime.ActionRegistry.display_contract_for` is the source of truth for action/run display labels, product category, and action description. `/api/operations/action-registry`, `/api/operations/actions`, `/api/operations/runs`, operation detail/provenance, and operation control responses must expose this `display_contract`; operation control responses must also expose it at the top level beside `control_state`. Frontend and Agent UI may show `action_type` / `operation_type` as debug ids but must not derive product copy or grouping from action type strings, operation type strings, owner module names, or local maps.
- CRM Public Web recovery registers both the queue-batch owner and the per-run phase-command owner; missing phase-owner drain is a W7f preflight failure, not a W6/nightly discovery.
- Legacy target-candidate Public Web is not an importable normal execution surface. Worker daemon quarantines historical `target_candidate_public_web_search` rows with an internal retired recovery-kind string and does not import `legacy_target_candidate_public_web_runtime`; the old CLI experiment command returns a report-visible retired envelope and does not import or call `public_web_search.run_target_candidate_public_web_experiment`. Product validation must use the CRM Public Web guarded runner.
- CRM Public Web per-run phase command owners record ActivityRun/Attempt/EntityDelta evidence; `documents.fetch` records document-level `public_web_document` deltas; `signals.materialize` records signal-level `public_web_signal` deltas and canonical `person_asset` / `person_evidence` sync deltas; command result JSON alone is not accepted as the long-term Agent/debug explanation surface.
- CRM writer command owners record ActivityRun/Attempt/EntityDelta evidence for CRM mutations; command result JSON and CRM table rows alone are not accepted as the long-term Agent/debug explanation surface.
- Owner-specific running command control must be explicit. `durable_runtime.workflow_command_control_policy` is the source of truth, and `/api/workflow/command-registry`, `/api/workflow/commands`, `/api/workflow/commands/{command_id}`, and `workflow_service_metrics.durable_command_owner_contracts` must expose the same `control_policy` so Agent/UI/signoff code does not infer control behavior from command names. Implemented running-cancel cases are CRM Public Web phase-command cancellation, projection/CRM Public Web export cancellation, Excel intake cooperative thread cancellation, early acquisition/company-root orchestration cancellation before downstream planning, acquisition plan-commit cancellation before probe planning, acquisition scale-plan cancellation before discovery command/activity attempt, provider-attempt local cancellation before provider EntityDelta/downstream planning, profile-fetch activity cancellation before cache lookup attempt/entity-delta/downstream planning, CRM Public Web queue-batch cancellation before phase commands, and acquisition plan-review request cancellation: `claimed/running` phase commands may be cancelled only by delegating to `crm_public_web_owner`, running export commands may be cancelled only by delegating to `projection_exporter.cancel_export_command` or `crm_public_web_exporter.cancel_export_command`, provider-attempt commands may be cancelled only by delegating to `workflow_provider_owner.cancel_or_poll_stop_provider_attempt` or the company Public Web source owner; if an ActivityAttempt already exists, the owner may only stop local polling and quarantine late results, not kill remote provider work or mutate domain rows. Excel intake commands may be cancelled only by delegating to `excel_intake_owner.cancel_excel_intake_run_command`, `acquisition.run.create` / `acquisition.intent.resolve` / `acquisition.plan.build` / `company.public_web.refresh` may be cancelled only by delegating to `workflow_orchestrator.cancel_orchestration_before_downstream` when no downstream command has been planned and the lease is expired or force is explicit, `acquisition.plan.commit` may be cancelled only by delegating to `workflow_orchestrator.cancel_acquisition_plan_commit_before_probe` before a probe command exists and must mark the `acquisition_runs` row `cancelled_before_probe`, `acquisition.scale.plan` may be cancelled only by delegating to `workflow_orchestrator.cancel_acquisition_scale_plan_before_discovery` before a discovery command exists and before any `workflow_activity_attempts` row exists, `linkedin.profile_fetch.activity.run` may be cancelled only by delegating to `linkedin_profile_activity_owner.cancel_before_cache_lookup_attempt` before cache lookup attempt/entity-delta/downstream planning, `crm.public_web.queue_batch` may be cancelled only by delegating to `crm_public_web_owner.cancel_queue_batch_before_phase_commands` before per-run phase commands exist and must mark queued batch/run rows cancelled, and `acquisition.plan_review.request` may be cancelled only by delegating to `workflow_orchestrator.cancel_acquisition_plan_review_request`, which cancels/rejects a pending review checkpoint and does not plan `acquisition.plan.commit`. These owners record cancelled command/activity/entity-delta state where their command type owns Activity evidence; export owners also use a temp-file artifact publish checkpoint, and the Excel owner uses a durable cancel marker plus thread terminal checkpoint so cancellation cannot intentionally publish/terminalize a normal result after the command is cancelled. Implemented running-resume cases cover every owner-registry command through an owner-specific delegate; resume records command/activity/entity-delta control evidence and requeues without request-path provider calls, local thread starts, artifact publication, or domain read-model writes. Unsupported running cancel fails closed with `running_command_requires_owner_specific_cancel` and must expose a category-specific `running_cancel_blocked_reason`: `external_provider_attempt_requires_owner_specific_interrupt`, `orchestration_command_must_finish_or_plan_compensating_command`, `domain_mutation_must_finish_or_retry_from_terminal_state`, or `owner_has_no_safe_inflight_interrupt`. Unsupported policies must also expose `running_cancel_upgrade_requirements`. The placeholder `owner_specific_interrupt_not_implemented` is blocked for normal owner-registry commands.
- Running-control maturity must be explicit and machine-readable. `control_policy.running_control_maturity` is one of `owner_specific_cancel_resume`, `owner_specific_cancel_only`, `owner_specific_resume_only`, or `fail_closed_with_upgrade_requirements`; `control_policy.running_control_gap_status` is one of `closed`, `partial_resume_gap_reported`, `partial_cancel_gap_reported`, or `accepted_fail_closed_pending_owner_specific_control`; and `control_policy.running_control_surface` must be `workflow_command_control_api_only`. Agent/UI code must not infer maturity from blocked-reason strings or implement a module-specific bypass when maturity is not closed.
- Media cache running control is owner-specific at the pre-fetch/upload checkpoint. `media.asset.cache` must expose `running_control_category=domain_mutation`, `running_control_maturity=owner_specific_cancel_resume`, `running_control_gap_status=closed`, `running_cancel_delegate=media_asset_owner.cancel_before_fetch_upload_attempt`, and `running_resume_delegate=media_asset_owner.resume_media_asset_cache`. Cancel may only mark command/activity state before media ActivityAttempt, media EntityDelta, or downstream command creation; resume may only requeue stale/forced commands with Activity/Attempt/EntityDelta control evidence. Neither control delegate may fetch media, upload objects, or write PersonAsset/CompanyAsset rows from the API request path.
- CRM writer running control is owner-specific at the pre-mutation checkpoint. `crm.record.add_from_projection`, `crm.record.update`, `crm.note.add`, and `crm.task.create` must expose `running_control_maturity=owner_specific_cancel_resume`, `running_control_gap_status=closed`, `running_cancel_delegate=crm_writer.cancel_before_mutation_attempt`, and `running_resume_delegate=crm_writer.resume_crm_writer_command`. Cancel may only mark command/activity state before CRM writer ActivityAttempt, CRM EntityDelta, or downstream command creation; resume may only requeue stale/forced commands with Activity/Attempt/EntityDelta control evidence. Neither control delegate may write `crm_records`, `crm_engagements`, `crm_tasks`, or `crm_events` from the API request path.
- Company Public Web refresh running control is split by phase. `company.public_web.refresh` is root orchestration and can be cancelled only before downstream `company.public_web.source.collect` is planned and only after lease expiry or explicit force; `company.public_web.source.collect` is `provider_attempt`; `company.public_web.assets.materialize` is `domain_mutation`. Source collection exposes owner-specific local cancel/poll-stop through `company_public_web_owner.cancel_or_poll_stop_source_collect` plus owner-specific running resume: before an ActivityAttempt it cancels local command/activity state, and after an ActivityAttempt it may only stop local polling and quarantine late results until an EntityDelta/downstream command exists. Asset materialization exposes owner-specific pre-sync cancel through `company_public_web_owner.cancel_assets_materialize_before_sync` and owner-specific resume through `company_public_web_owner.resume_assets_materialize`; both may only record control evidence/requeue or mark pre-sync activity state and must not sync CompanyAsset/CompanyEvidence from the control API.
- Orchestration command running cancel remains deliberate fail-closed after downstream planning. `acquisition.run.create`, `acquisition.intent.resolve`, `acquisition.plan.build`, and `company.public_web.refresh` can be safely cancelled only before any downstream command exists and only after lease expiry or explicit force; `acquisition.plan.commit` can be cancelled only before a probe command exists and must terminalize the acquisition run as `cancelled_before_probe`; `acquisition.scale.plan` can be cancelled only before a discovery command exists and before any activity attempt starts, and must terminalize the acquisition run/lane/activity as `cancelled_before_discovery`; `crm.public_web.queue_batch` can be cancelled only before per-run phase commands exist and must terminalize queued batch/run state as `cancelled`; `acquisition.plan_review.request` can be cancelled before approval because its only running side effect is the pending human-review checkpoint. Later acquisition orchestration commands may be requeued through `workflow_orchestrator.resume_orchestration_command` only when the lease is expired or the caller uses explicit force. The resume delegate records Activity/Attempt/EntityDelta control evidence and requeues without running reducers, creating downstream commands, calling providers, or writing domain read models from the resume API.
- Provider-attempt command running cancel is allowed before provider EntityDelta or downstream command evidence exists. Before ActivityAttempt creation it cancels local command/activity state; after ActivityAttempt creation Harvest/Apify/DataForSEO-family commands use local `poll_cancel_late_result_quarantine`, which stops local polling/waiting, marks the attempt `cancelled_remote_ignored`, and requires any late remote result to remain quarantined evidence unless a later explicit owner adoption command accepts it. Acquisition probe, operation-native discovery, operation-native profile provider fetch, job-scoped profile refill submit, and company Public Web source collection may be requeued through `workflow_provider_owner.resume_provider_attempt_command` only when the lease is expired or the caller uses explicit force. The delegates record Activity/Attempt/EntityDelta control evidence and requeue or cancel local command state without killing remote provider work, writing registry rows, or publishing artifacts from the API request path.
- Excel intake command running cancel and resume are both owner-specific. `excel.intake.run` may be cancelled through `excel_intake_owner.cancel_excel_intake_run_command` and requeued through `excel_intake_owner.resume_excel_intake_run_command`; resume requires `payload.job_id` plus expired lease or explicit force, records Activity/Attempt/EntityDelta control evidence, and must not start a local intake thread or write Excel result materializations from the resume API.
- Each owner-registry command must belong to exactly one running-control category in `durable_runtime.workflow_command_running_control_categories`, and `control_policy` must expose both `running_control_category` and `running_control_categories`. A command cannot be both provider-blocked and domain-mutation-blocked for control-policy purposes; if a domain action performs provider work, the command must either be split into smaller typed activities or deliberately pick one control category until the split exists. This prevents Agent/UI callers from receiving different blocked reasons for the same in-flight command depending on branch order.
- Frontend command-control helpers must not hardcode post-action target statuses such as `cancelled` or `queued`. They may treat explicit backend failure statuses (`invalid`, `not_found`, `failed`) as errors, but successful state must be read from the returned `workflow_command` and `control_state`. This keeps queued-state generic controls and owner-specific running controls behind the same backend-owned contract.
- Owner-specific running resume must also be explicit. Generic command resume is limited to `retry_wait`, where it clears `not_before_at` and lets the owner re-claim the command. `claimed/running` resume is fail-closed unless the command owner registers explicit owner-specific resume support. Implemented cases: CRM Public Web phase commands may be requeued through `crm_public_web_owner.resume_crm_public_web_phase_command` only when `payload.run_id` exists and the command lease is expired or the caller uses explicit force; projection and CRM Public Web export commands may be requeued through their export owners only when the artifact has not been published and the command lease is expired or forced; orchestration, provider-attempt, and domain-mutation commands may be requeued only through their registered owner-specific delegates. Each owner records Activity/Attempt/EntityDelta resume evidence and moves the command back to queued without mutating domain read models directly. Domain-mutation cancel uses `workflow_domain_owner.cancel_before_domain_mutation_attempt` only before ActivityAttempt/EntityDelta/downstream evidence; domain-mutation resume uses `workflow_domain_owner.resume_domain_mutation_command`. Neither delegate may write registry, projection, collection, snapshot, CRM, Public Web, media, or asset tables from the control API. Unsupported running commands expose `running_command_requires_owner_specific_resume` in `control_state.disabled_reasons.resume`, a category-specific `running_resume_blocked_reason`, and `running_resume_upgrade_requirements`; the placeholder `owner_specific_resume_not_implemented` is blocked for normal owner-registry commands. `workflow_service_metrics.durable_command_owner_contracts` owns `control_policy_violation_count` and includes control-policy drift in `violation_detected`; scripted signoff with `require_durable_command_owner_contracts=true` must also block active command contracts whose `control_policy` is missing, not `fail_closed`, or still uses placeholder running cancel/resume policy fields, even when owner and causality counts are clean. Agent/UI code must not use resume as a generic repair button for in-flight provider calls, artifact writes, local threads, or domain mutations.
- Status-specific command controls must also be explicit. `durable_runtime.workflow_command_control_state` combines current command status with the policy and is the only source for allowed `cancel` / `retry` / `resume` actions, cancel mode, disabled reasons, and module-state mutation expectation. Command records, command control responses, and Activity/Attempt/EntityDelta/Lane `control_target` records must expose this `control_state`, so Agent/UI callers do not infer buttons from status strings.
- Status-specific OperationRun controls must also be explicit. `operation_runtime.operation_run_control_state` is the source of truth for dispatch/resume/retry/cancel availability on OperationRun rows, control responses, and Agent `inspect_operation` results. Operation Queue UI must render run-level controls from top-level/row `control_state.allowed_actions` and `disabled_reasons`, not from local terminal-status sets or status-string heuristics. If an operation retry creates a child OperationRun, the UI must select the returned child run instead of continuing to render controls for the terminal parent. Owner-specific fail-closed overrides such as unsupported or identity-mismatched `start_acquisition_run` v2 generic controls must preserve this source-of-truth string and use registered disabled reasons rather than introducing a second control-state owner.
- Workflow command display must be registry-owned. `durable_runtime.workflow_command_display_contract` is the source of truth for command display labels, product category, and command description. `/api/workflow/command-registry`, `/api/workflow/commands`, `/api/workflow/commands/{command_id}`, workflow command control responses, and Activity/Attempt/EntityDelta/Lane `control_target` records must expose this `display_contract`; frontend and Agent UI may show `command_type` as a debug id but must not derive product copy or grouping from command type strings.
- Operation UI display copy must fail closed. If `operation_action.display_contract`, `operation_run.display_contract`, or `workflow_command.display_contract` is absent, `/operations` must render an explicit missing-contract label/category and surface that as a frontend-contract regression; it must not fallback to `owner_module`, `owner`, `operation_type`, `action_type`, or `command_type` for product titles/categories.
- Activity-spine evidence must be policy-owned. `durable_runtime.workflow_command_activity_spine_policy` is the source of truth for whether a command must write ActivityRun/Attempt/EntityDelta evidence, only plans a downstream activity boundary, or is orchestration-only. The normal owner registry currently has no `legacy_internal_pending_activity_spine` command types; the operation action registry still fail-closes any such command if a future migration accidentally reintroduces one.
- Durable owner signoff treats display and Activity-spine policy as first-class contracts. `workflow_service_metrics.durable_command_owner_contracts` owns `display_contract_violation_count` and `activity_spine_policy_violation_count`; both are included in `violation_detected`, and scripted signoff blocks active command contracts that omit display copy, lose fail-closed ownership, or regress Activity-spine policy to `legacy_internal_pending_activity_spine`.
- Operation queue summaries must be operation/runtime-owned. `operation_run.status_summary` may only summarize `operation_runs`, `operation_events`, and `workflow_commands`, with command drill-down delegated to `workflow_command.execution_summary`; it must not read CRM/Public Web/projection/Excel/export/registry/lane read models to make an operation look complete, failed, or cancellable.
- Command status/provenance summaries must be Activity-spine-owned. `workflow_command.execution_summary` on command detail, and optional list summaries through `include_execution_summary=true`, may only read `workflow_activity_runs`, `workflow_activity_attempts`, and `workflow_entity_deltas`; it must expose `fallback_status=fail_closed`, `fallback_used=false`, and `module_state_mutated=false`. It must not stitch progress from CRM/Public Web/projection/Excel/export/registry/lane read models, because those are domain materializations rather than execution truth.
- Agent command exposure must be ActionRegistry-owned. `activity_spine_policy.agent_callable` means a command has the technical Activity/Attempt/EntityDelta spine required for future Agent orchestration; it is necessary but not sufficient for product exposure. The ActionRegistry allowlist (`operation_runtime.ActionRegistry.allowed_workflow_command_types`) is the only normal Agent exposure gate. Public operation registries must expose `workflow_command_exposure_gate=operation_runtime.ActionRegistry.allowed_workflow_command_types`, `workflow_command_exposure_status`, and per-command `agent_exposure_gate` / `agent_exposure_status` so UI/Agent code never infers callable commands from the durable owner registry alone.
- Action-level command control summaries must also be ActionRegistry-owned. `workflow_command_control_summary` is derived from `allowed_workflow_command_contracts` and exposes maturity/gap/category counts plus UI guidance for closed or partially closed command-control surfaces. Agent/UI code may use it to explain that an action can plan a command but may not be safely interruptible while running; it must not recompute those summaries from command type strings or owner names.
- Workflow command exposure must be explicit on both registry and concrete command rows. `/api/workflow/command-registry` is a durable owner/control registry, not an Agent action allowlist. Every command-registry entry and every `/api/workflow/commands` list/detail row must expose the same `agent_exposure_gate` and `agent_exposure_status`; commands that are technically runnable but not ActionRegistry-allowlisted must say `not_action_registry_allowlisted`, so Agent/UI callers cannot treat owner-registry membership or a visible command row as product permission.
- Operation UI command status copy must be Activity-spine-owned. The `/operations` page may show command counts, latest effect, reason, entity, and sample truncation only from `workflow_command.execution_summary`; it must not infer command effects from command type names, timestamps, domain read models, or owner-private result JSON.
- Operation UI Activity drill-down is read-only runtime inspection. `/operations` may query `/api/workflow/activities`, `/api/workflow/activity-attempts`, and `/api/workflow/entity-deltas` by command id, but all dispatch/resume/retry/cancel controls must still go through OperationRun or WorkflowCommand control APIs.
- Activity/Attempt/EntityDelta/Lane rows are read-only evidence, not mutable workflow state. Each row returned from `/api/workflow/activities`, `/api/workflow/activity-attempts`, `/api/workflow/entity-deltas`, and `/api/workflow/discovery-lanes` must expose `module_state_mutated=false`, a `mutation_contract` beginning with `read_only_`, and a `control_target` pointing to the owning workflow command when command causality exists. That `control_target` must include command-owned `display_contract`, `control_policy`, `control_state`, `activity_spine_policy`, and `fallback_status=fail_closed`. Agent/UI callers must never mutate or control Activity/Attempt/Delta/Lane rows directly, and must not derive display copy or control behavior from their domain fields.
- Operation UI command control is WorkflowCommand-owned. `/operations` may render command-level cancel/resume/retry buttons only from `workflow_command.control_state.allowed_actions` / `disabled_reasons`, and those buttons must call `/api/workflow/commands/{command_id}/cancel|resume|retry`; no command status string, owner name, Activity row, or domain read model may infer control capability.
- Operation UI command control must surface rejected controls. If the WorkflowCommand control API returns `invalid`, `not_found`, or another non-success status, the frontend must show the backend `reason` / `command_status` rather than treating a refresh as success.
- Command control responses must expose `display_contract`, `control_policy`, and `activity_spine_policy` at the top level as well as on `workflow_command`, so Agent/UI callers do not need to inspect nested command detail or infer labels, control behavior, or Activity-spine requirements from command type strings.
- Remaining migration bridges are visible in `NEXT_TODO.md`, metrics, and signoff gates.
- W11d discovery materialization boundary is resolved and covered by fast preflight before broad W6/nightly; operation-native discovery commands must be owned by `operation_native_discovery_activity_owner`, not by the legacy search-seed queue phase.

## Contract Decision Records (migrated 2026-06-11 from NEXT_TODO)

> The records below were carried verbatim out of the pre-restructure `docs/NEXT_TODO.md` (full snapshot: `docs/archive/NEXT_TODO_2026-06-10_full.md`). They are normative W-series decision/contract records pinned by `tests/test_pre_agent_contract_review.py`; they are not TODO items.

- [ ] **M0 GitHub checkpoint discipline.** 当前 worktree 非常脏，且之前长期未同步到 GitHub。可以从当前本地状态创建新的 checkpoint/development branch，但不要把全量 dirty tree 作为一个“当前版本”提交。优先创建 scoped planning/checkpoint branch 和 PR，先提交当前架构文档，再拆后续 milestone PR。M0 是 docs-only checkpoint，不包含 Operation UI、frontend API adapter、runtime registry、pre-agent 大测试、provider payload、runtime/log/output 或截图产物；2026-06-07 UI/contract slice 如需同步，应单独开 implementation PR 并列全依赖范围。
  Required staged files for M0 checkpoint (git-root-relative):
  - `sourcing-ai-agent/README.md`
  - `sourcing-ai-agent/PROGRESS.md`
  - `sourcing-ai-agent/docs/INDEX.md`
  - `sourcing-ai-agent/docs/NEXT_TODO.md`
  - `sourcing-ai-agent/docs/SERVICE_GRADE_ARCHITECTURE_PLAN.md`
  - `sourcing-ai-agent/docs/AGENT_OPERATION_CONTRACT.md`
  - `sourcing-ai-agent/docs/PRE_AGENT_CONTRACT_REVIEW.md`
  - `sourcing-ai-agent/docs/DURABLE_EXECUTION_RUNTIME_CONTRACT.md`
  - `sourcing-ai-agent/docs/MODEL_NATIVE_SEARCH_PROVIDER_CONTRACT.md`
  - `sourcing-ai-agent/docs/INDEPENDENT_REVIEW_BRIEF.md`
  - `sourcing-ai-agent/docs/INDEPENDENT_REVIEW_GATE.md`
  PR 前用 `git diff --cached --name-only` 对照该 Git-root 相对清单，缺 current-entry contract doc 或多出 runtime/log/secret/generated artifact 都阻塞。

- [x] Add fast preflight: `tests/test_search_provider.py::SearchProviderTest::test_model_native_search_provider_order_fails_closed_without_contract` is included in `make ci-pre-agent-contract`.

- [ ] **Phase W7: module reuse after LinkedIn stability.** Migrate CRM/Public Web, Excel, export, and other non-LinkedIn modules onto the same durable runtime primitives before Phase 13 Agent graph implementation.
  - [x] **Phase W7a: CRM Public Web typed owner convergence.** CRM Public Web normal path now uses CRM owner storage (`crm_public_web_*`), `crm_public_web_search` worker recovery, execution backend `crm_public_web_v1`, and typed command `workflow_commands(command_type='crm.public_web.queue_batch', owner='crm_public_web_owner')`. Target-candidate Public Web aliases/helpers are retired by default and migration/test-only; even with the alias override enabled, CRM record ids do not auto-create target-candidate compatibility rows, and target-candidate batch-summary sync does not mirror `target_candidate_public_web_v1` runs into `crm_public_web_*` by default. Explicit historical target->CRM Public Web sync requires `SOURCING_ALLOW_LEGACY_TARGET_PUBLIC_WEB_TO_CRM_SYNC=1` and is report-visible migration evidence.
  - [x] **Phase W7a signoff evidence.** Fresh targeted CRM Public Web fake-provider/action smoke passed at `output/w7_crm_public_web_fresh_targeted_20260524/`: case status `completed`, `expectation_failures=[]`, Pre-Manual Signoff `passed`, blocking/manual/warning counts `0`, `storage_owner_counts={'crm_public_web_v1': 1}`, `execution_backend_counts={'crm_public_web_v1': 1}`, and `queue_batch_command_succeeded_count=1` owned by `crm_public_web_owner`.
  - [x] **Phase W7b: CRM Public Web live-provider product validation.** 2026-05-24 local live validation used the Anthropic collection target candidate Long Nguyen via the CRM-owned path. The run completed with `public_web_storage_owner=crm_public_web_v1`, `public_web_execution_backend=crm_public_web_v1`, `legacy_target_candidates_used=false`, 40 public signals, 5 documents, and detail drawer rendering. Product finding: common-name results can be noisy, so Public Web signals remain review/export candidates and must not become automatic assertions without promotion.
  - [x] **Phase W7c: Excel durable command reuse first owner slice.** Excel intake job creation now plans `workflow_commands(command_type='excel.intake.run', owner='excel_intake_owner')` with typed causality and produced counts. The route may return after the owner starts the async workflow, but stale Excel recovery no longer calls `_run_excel_intake_workflow(...)` directly; it requeues/plans `excel.intake.run` and drains the Excel command owner. 2026-05-26 activity-spine follow-up: the command owner now creates `workflow_activity_runs` / `workflow_activity_attempts` before starting the async thread, and the thread records terminal `excel_intake_job` `workflow_entity_deltas`. W11 control follow-up: claimed/running Excel intake commands now support owner-specific cooperative cancel via `excel_intake_owner.cancel_excel_intake_run_command`; the owner writes a durable command-cancel marker, marks the Excel job read model cancelled, records cancelled Activity/Attempt/EntityDelta evidence, and the async thread checks the command before terminalization and at major materialization checkpoints. Claimed/running resume is also owner-specific via `excel_intake_owner.resume_excel_intake_run_command`; it records control evidence and requeues without starting a local thread or writing intake materializations from the request path. Remaining W7/W9 work: richer user-facing command status and stronger terminal-command semantics for very large uploads.
  - [x] **Phase W7d: export durable command reuse first owner slice.** `/api/projections/export` now plans and drains `workflow_commands(command_type='export.projection.generate', owner='projection_exporter')`; `/api/crm/records/public-web-export` now plans and drains `workflow_commands(command_type='export.crm_public_web.generate', owner='crm_public_web_exporter')`. Both write ZIP artifacts under command-owned export directories and return from artifact on replay. 2026-05-26 activity-spine follow-up: projection export and CRM Public Web export owners now write `workflow_activity_runs`, `workflow_activity_attempts`, and export `workflow_entity_deltas` with artifact refs, so Agent/debug surfaces do not infer export effects from ZIP paths alone. W11 control follow-up: claimed/running projection and CRM Public Web export commands support owner-specific cancel/resume and publish through command-status/temp-file checkpoints so cancelled commands do not intentionally publish normal ZIP artifacts; resume records control evidence and requeues without publishing artifacts from the request path. Remaining W7/W9 work: async long-running export status APIs and approval-event integration for Agent-initiated bulk exports.
  - [x] **Phase W7e: legacy target-candidate Public Web physical deletion.** Normal helper/import surfaces are retired or isolated: CRM runtime imports go through `crm_public_web_runtime.py`, explicit legacy references go through `legacy_target_candidate_public_web_runtime.py`, and the old `target_candidate_public_web.py` facade has been physically deleted. Orchestrator target-candidate Public Web execution/read/export helpers are retired envelopes. API aliases are now unconditional `410` canonical endpoint branches, with no dead override path back into legacy orchestrator methods. Lower-level target-candidate helper bodies no longer keep unreachable write paths into `target_candidate_public_web_*`; `public_web_runtime_core.py` no longer directly calls legacy storage helpers. Added `legacy_public_web_storage.py` as the only migration-only reader/writer; it returns empty lists when legacy tables are physically absent, and `ControlPlaneStore.upsert/update_target_candidate_public_web_*` now fail closed unless called inside `legacy_target_public_web_migration_write_context(...)`. Historical tests that need old rows must use `legacy_public_web_storage.seed_legacy_target_public_web_*`; the only direct store-write exception is the PG storage-authoritative test under an explicit migration context. Added `/api/migrations/legacy-public-web` plus `workflow_service_metrics.legacy_public_web_retirement`; Pre-Manual Signoff can now require `require_legacy_public_web_retirement_ready` so legacy rows or row-limit-truncated audits block W7e deletion before a long W6/nightly run. `tests/test_crm_public_web_runtime_boundary.py` blocks normal source calls, direct non-migration test writes, and bare runtime writes to `target_candidate_public_web_*` storage helpers. 2026-05-26 physical-deletion closeout: new SQLite compatibility shadows no longer bootstrap `target_candidate_public_web_*` tables; migration seed helpers recreate retired tables only inside `legacy_target_public_web_migration_write_context(...)`; PG live/bootstrap defaults no longer list retired tables; PG writer-schema repair skips legacy indexes unless historical tables already exist. Local active-runtime audit `runtime/audits/legacy_public_web_retirement_w7e_strict_latest.json` returned `ready_for_physical_deletion` with legacy target-candidate rows `0`; `runtime/audits/legacy_public_web_archive_w7e_latest.json` archived an empty legacy set; `runtime/audits/legacy_public_web_drop_w7e_latest.json` dropped the three retired PG tables under `legacy_public_web_drop_v1`. Future historical access is migration-only cold evidence through `legacy_public_web_storage.py`, not a normal runtime surface. Same-day W10 hardening: retired `/api/target-candidates/public-web...` aliases are also excluded from API priority-lane/light-lane normal handling; only canonical `/api/crm/records/public-web...` routes receive interactive Public Web scheduling.
  - [x] **Phase W7f: CRM Public Web internal typed-command phases.** `crm.public_web.queue_batch` no longer creates CRM Public Web agent workers as a hidden side effect. It creates/validates CRM-owned batch/run rows and plans per-run commands owned by `crm_public_web_owner`: `crm.public_web.search.submit`, `crm.public_web.search.poll_fetch`, `crm.public_web.documents.fetch`, `crm.public_web.evidence.adjudicate`, `crm.public_web.model_safe.finalize`, and `crm.public_web.signals.materialize`. The recovery tick drains both `crm_public_web_queue_batch` and `crm_public_web_phase_commands`; phase commands carry parent command causality, and OperationRun completion is deferred until terminal downstream phase command success. Command-level `cancel` / `retry` / `resume` is active for safe queued-state phase commands; All claimed/running CRM Public Web phase commands support owner-specific `resume` through `crm_public_web_owner.resume_crm_public_web_phase_command`; resume records control evidence and requeues without executing phase side effects in the request path. `search.poll_fetch` pending remote work still regresses through `retry_wait` plus command `resume` instead of worker-private waiting state. W7f fast preflight requires the six phase command types in W10 review docs, `durable_command_owner_contracts`, and source-boundary recovery registration, so W6/nightly no longer needs to discover a missing phase owner. Claimed CRM Public Web phase commands write `workflow_activity_runs`, `workflow_activity_attempts`, and run-level `workflow_entity_deltas`; command result JSON is no longer the only Agent/debug evidence for Public Web phase effects. `crm.public_web.documents.fetch` records document-level `public_web_document` entity deltas from `document_fetch_payload.json`, and `crm.public_web.signals.materialize` records signal-level `public_web_signal` entity deltas from `person_public_web_signals` plus canonical `person_asset` / `person_evidence` sync deltas under the same activity id, so Agent/debug can explain individual Public Web documents/signals and their PersonAsset-layer effects without mutating the read models outside the phase owner. W11 control follow-up: `claimed/running` CRM Public Web phase commands support owner-specific cancel through the command API; it cancels the CRM Public Web run via `crm_public_web_owner`, interrupts owned workers where present, marks the command cancelled from `claimed/running`, and records cancelled Activity/Attempt/EntityDelta evidence. Fake lifecycle preflight covers the full Operation -> queue batch -> search.submit -> poll_fetch retry_wait/resume -> documents.fetch -> evidence.adjudicate -> model_safe.finalize -> signals.materialize chain and asserts that OperationRun completes only after the terminal phase command. Worker-daemon closeout: historical CRM Public Web and legacy target-candidate Public Web `agent_worker` recovery rows are quarantined/retired report-visibly by the worker daemon and are not executed through Public Web runtime functions; normal execution is phase-command owned only. Validation: operation phase targeted slice `3 passed`, workflow smoke Public Web service slice `5 passed`, W7e boundary suite `12 passed`, durable command owner slice `2 passed`.
    - 2026-05-29 quality/latency follow-up: `crm.public_web.evidence.adjudicate` now writes the exact compact `adjudication_input_payload.json` before model output is applied, so bad signals can be audited against DataForSEO results, selected input window, model decision, and materialized signal separately. The phase drain can execute independent adjudication commands with bounded concurrency (`CRM_PUBLIC_WEB_ADJUDICATION_COMMAND_CONCURRENCY`, default 2); other Public Web write/materialization phases remain command-owned and idempotent. Adjudication input construction now treats `max_ai_entry_links` and `max_ai_evidence_documents` as owner-controlled model evidence windows instead of applying a second hard cap; guarded live validation passes `CRM_PUBLIC_WEB_LIVE_MAX_FETCHES_PER_CANDIDATE`, `CRM_PUBLIC_WEB_LIVE_MAX_AI_ENTRY_LINKS`, and `CRM_PUBLIC_WEB_LIVE_MAX_AI_EVIDENCE_DOCUMENTS` (default `10` each) so quality runs can prove whether DataForSEO/fetch recall or model judgement is the limiting factor. Signal materialization is now fail-closed against unreviewed search noise: raw DataForSEO entry links remain in artifacts, but `person_public_web_signals` only receives publishable confirmed/likely profile-shaped links, or AI-explicit reviewable links where the adjudication output sets `user_visible_signal=true` with a `review_queue_reason`, plus email candidates that are not suppressed/not-same-person. This prevents third-party mention snippets, single-publication pages, unrelated Scholar profiles, and suppressed emails from becoming user-visible target-candidate signals without relying on fixed per-site heuristics for identity judgement.
    - 2026-06-04 sharedchat/live validation follow-up: `model_provider` can now call OpenAI-compatible Responses relays with `api_style=openai_responses`; the verified sharedchat relay base is `https://new.sharedchat.cc/codex`, model `gpt-5.5`, with `timeout_seconds>=120` for Public Web adjudication. A force-refresh W7g pass proved model fallback was eliminated for runs that reached adjudication, but exposed a phase owner contract gap: overlapping force-refresh batches reused the same CRM Public Web run id, so stale commands could mutate current run state and a phase command could execute a neighboring phase when run status had drifted. CRM Public Web phase owner now enforces expected input statuses per command, quarantines stale superseded batch commands as no-op, plans missing downstream commands when the run has already advanced, and waits in `retry_wait` when prerequisites are not met. Validation: `tests/test_model_provider.py tests/test_settings.py` `37 passed`; `tests/test_operation_runtime.py` `85 passed`.
    - 2026-06-04 sharedchat phaseguard follow-up: the next three-person force-refresh showed two remaining live-gate issues rather than Public Web identity-quality failures. First, the model provider returned transient `/responses` 502/DNS errors and opened the circuit, but `/api/providers/health` could still report cached `ready`; healthcheck cache now never masks an open model circuit and degraded health is not cached. Second, the live runner's operator poll timeout could be shorter than Public Web's DataForSEO remote/pending task budgets; W7g runner now preflights `/api/providers/health` before creating any batch and passes explicit `CRM_PUBLIC_WEB_LIVE_MAX_REMOTE_SEARCH_WAIT_SECONDS`, `CRM_PUBLIC_WEB_LIVE_MAX_PROVIDER_PENDING_WAIT_SECONDS`, and `CRM_PUBLIC_WEB_LIVE_MAX_CONCURRENT_CANDIDATE_ANALYSES` into the backend options. Live validation must fail before provider work when model health is not ready, and provider task terminal budgets must be aligned with poll timeout.
    - 2026-06-04 force-refresh promotion/export follow-up: a two-person sharedchat live pass reached terminal runs with expected model metadata, but default `promoted_only` export returned zero signals because refreshed `person_public_web_signals.signal_id` still included run-scoped identity while historical `crm_public_web_promotions` pointed at old signal ids. A second failure mechanism was command-owned export artifact caching: the same record/mode scope could reuse a succeeded ZIP generated under stale inputs. `person_public_web_signals` now uses stable person/value identity (`person/record + signal_kind + signal_type + normalized email/url`), detail/export overlays historical promotion/rejection by the same stable identity when exact signal id no longer matches, and CRM Public Web export commands include both `CRM_PUBLIC_WEB_EXPORT_CONTRACT_VERSION` and owner-computed `export_input_watermark_hash` in workflow/operation/idempotency scope/payload/result/manifest. The watermark covers latest CRM Public Web run, latest-run signals, CRM Public Web promotions, and relevant PersonAssertions; promotion/rejection/latest-run/assertion changes therefore plan a new command/artifact instead of replaying a stale ZIP. Regression coverage: `test_crm_public_web_promotions_survive_force_refresh_signal_identity`, `test_crm_public_web_export_reads_crm_owner_storage`, and `test_crm_public_web_export_command_scopes_artifact_reuse_by_input_watermark`.
- [ ] **Phase W7g: CRM Public Web live/product validation.** Run a small live-provider validation on the CRM-owned multi-phase path before product signoff. This validates DataForSEO/provider quality, model adjudication quality, promotion UX, and export payload quality; it must not re-enable target-candidate Public Web aliases or worker-daemon execution. Guarded entrypoint added: `make test-crm-public-web-live-product-validation` runs `scripts/run_crm_public_web_live_product_validation.py` in dry-run mode by default, requires `LIVE_CONFIRM=1` plus `CRM_PUBLIC_WEB_LIVE_DRY_RUN=0`, `CRM_PUBLIC_WEB_LIVE_RECORD_IDS_REVIEWED=1`, and exact `CRM_PUBLIC_WEB_LIVE_FORCE_REFRESH=1` for real provider calls, re-runs `ci-pre-agent-contract` before real live execution, passes `CRM_PUBLIC_WEB_LIVE_PRE_AGENT_CONTRACT_PASSED=1`, `--reviewed-crm-record-ids`, and `--force-refresh` to the script, supports `CRM_PUBLIC_WEB_LIVE_POLL_TIMEOUT_SECONDS` for legitimate long DataForSEO force-refresh waits, requires the immutable product model `gpt-5.6-sol` by exact equality in returned model metadata for model-verifiable terminal runs, writes `output/w7g_crm_public_web_live_product_validation.json`, and blocks legacy target-candidate endpoint/owner markers plus model fallback. Missing force refresh fails before backend/provider access; default dry-run still returns its no-request report but lists force refresh as a live prerequisite. Poll timeout is a run-terminality failure with non-terminal run ids/statuses; it must not be reported as missing model metadata for runs that have not reached adjudication. 2026-05-27 dry-run evidence: `make test-crm-public-web-live-product-validation` returned `status=dry_run_ready`, `guard_failures=[]`, canonical CRM endpoints only, expected owner `crm_public_web_v1`, and `warnings=["dry_run_only: no backend or provider call was made"]`. 2026-05-28 guarded W7g runner dry-run evidence now also reports `live_prerequisites`, including `missing_or_required_before_live`, `record_id_selection_guidance`, and `recommended_make_command`, so the live pass cannot proceed from target-candidate ids, missing reviewed-record confirmation, missing force refresh, or an implicit direct script command. Remaining W7g evidence is the actual live pass with reviewed CRM record ids through the same gate.
  - 2026-06-04 independent review hardening (scope binding strengthened 2026-07-10): W7g product/manual signoff now also requires `make independent-review-gate` evidence after targeted/fast preflight and before further live-provider validation or user-facing signoff. The review must cover Public Web detail UI contract, signal grouping/materialization, promotion/rejection/export state, retry confirmation semantics, model/provider fallback/circuit behavior, and stable signal identity. Store the artifact under `runtime/reviews/` and reference it here or in the relevant Contract before marking W7g product validation complete. Launch the canonical command in `docs/INDEPENDENT_REVIEW_GATE.md` with an explicit pinned `REVIEW_BASE`; dirty-tree output is reference-only. Live execution is fail-closed unless `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_PASSED=1`, `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_ARTIFACT=<valid GO runtime/reviews/*.md>`, and `CRM_PUBLIC_WEB_LIVE_INDEPENDENT_REVIEW_SCOPE_DIGEST_SHA256=<digest from that exact request>` are provided. The live runner validates durable metadata, exact title and scope digest, final verdict, the immutable W7g/CRM Public Web/live tokens, and immutable live runner/Contract file coverage; environment values can only add requirements. It rejects `NO-GO`/timeout/invalid/missing-metadata/missing-scope-file/missing-or-wrong-digest/prompt/unrelated artifacts and records the bound artifact/digest in its report.
  - 2026-06-05 Public Web detail UI contract follow-up: target-candidate Public Web detail now separates reviewable email candidates, reviewable profile/homepage candidates, and read-only evidence/audit sources. Reviewable rows use a single status control (`待复核` / `已确认并导出` / `已排除`), show only the top candidate by default, and expose lower-confidence candidates behind an explicit expansion. Drawer copy no longer repeats card-level provider progress, retry controls, or unsupported synthetic profile-completeness scores. Starting a selected-candidate Public Web Search and retrying a run both require explicit browser confirmation because they can trigger real provider/model work and switch the latest run view. Independent review NO-GO artifact `runtime/reviews/20260604T172707Z_public-web-detail-ui-contract-final.md` found one blocker: CRM Public Web export could replay stale ZIPs after promotion/latest-run changes. Follow-up landed in the same slice: drawer detail reads only canonical `GET /api/crm/records/{crm_record_id}/public-web-search`, cache entries bind to `latest_run.run_id`, stale run signals are fail-closed for confirmation/export in both UI and CRM Public Web promotion owner (`public_web_signal_not_latest_run`), v1 disables review reset after manual confirm/exclude until a CRM Public Web owner-backed reset/void action is designed, and CRM Public Web export artifact reuse is scoped by `export_input_watermark_hash`. Independent review rerun artifact `runtime/reviews/20260604T174326Z_public-web-detail-ui-contract-final-rerun.md` found two additional export fail-open blockers; follow-up now revalidates the watermark after queued command claim and before ZIP side effects, makes the archive owner reject stale payload watermarks, and makes `/api/crm/records/public-web-export` return JSON failure for every non-`ok` owner result instead of a successful empty binary download. Second rerun artifact `runtime/reviews/20260604T175547Z_public-web-detail-ui-contract-final-rerun-2.md` found the remaining export-policy blockers: watermark did not cover `person_asset` / phase-command fields written into ZIP summaries, and the frontend default export path hardcoded `promoted_and_publishable`. Follow-up makes the watermark digest the same model-safe per-record export input snapshot used by ZIP generation, including `person_asset` and phase-command summary; default Web Search export is now `promoted_only`, and the AI-publishable-unconfirmed mode requires an explicit UI selection plus browser confirmation.
  - 2026-07-10 product-model contract update (owner-approved): the shared `model_provider` target and W7g product model moved from `gpt-5.5` to immutable `gpt-5.6-sol`. This affects every service path using the startup-scoped shared `ModelClient`, not only CRM Public Web, and requires a service restart after configuration changes. The non-live contract proves exact outbound `model=gpt-5.6-sol`; generation results and phase metrics now separate configured `requested_model` from provider-authored `response_model`/`effective_model`, emit `model_identity_provenance=provider_response` only with response evidence, and retain only bounded token counts in `model_usage`. Health/W7g validation requires exact co-located `requested_model == response_model == effective_model == gpt-5.6-sol`, provider-response provenance, and no fallback; missing/mismatched identity opens the circuit and rejects the model judgement rather than letting a relay reroute affect business decisions. The W7g runner has no Make/environment model override; its legacy direct CLI option accepts only the immutable value and otherwise fails before any backend/provider call. Its live evidence chain also pins the start response's batch/run ids through poll and detail, so a record-level fallback poll or stale/concurrent `latest_run` cannot prove this invocation. Non-live validation: model/provider/runtime-boundary suites **188 passed + 11 subtests**; the settled worktree contract lane **255 + 2 + 11 + 1 + 2 passed**, with W7g `dry_run_ready failures=[]`, Ruff green, and mypy unchanged at **87 errors / 4 files**. Pinned review scope `508799e..47ec9d0` is deferred as R-016 after the canonical network preflight found all reviewer/GitHub HTTPS endpoints unreachable; this is scope-local and does not block simulated development. It does not claim the relay accepted or returned that model, nor independently verify the owner-provided price/performance rationale. Before product signoff, a valid review GO and guarded force-refresh live pass must prove `/models` availability, a real Responses generation, exact effective-model provenance, no fallback, and product quality. Review Gate settings remain a separate operator-owned control plane and do not flow into product requests.
  - 2026-05-29 Anthropic live evidence: reviewed CRM records for Yuwei Qin, Connie Sun, Alexander Wu, Aaron Vontell, Margaret V., Jackie Bow, and Brian E. were validated through canonical CRM Public Web endpoints with `public_web_storage_owner=crm_public_web_v1`, expected model metadata, `fallback_used=false`, export `X-Sourcing-Non-Terminal-Run-Count=0`, and report `output/w7g_anthropic_public_web_live_recheck.json` status `live_validation_passed`. Force-refresh evidence exposed a legitimate DataForSEO wait beyond the default 600-second operator timeout; the runner now reports non-terminal run ids/statuses as poll timeout evidence and skips model-metadata checks for runs that have not reached adjudication. Remaining product signoff item: promotion/override UX still needs a reviewed publishable or manually overridden signal because this Anthropic sample produced 0 promoted signals.
  - 2026-05-29 Jackie Bow quality root-cause closeout: DataForSEO did return `https://x.com/jbowocky`; the wrong `staceywueste` result was a third-party snippet from the company-context X query. Public Web now persists exact model input snapshots, filters same-name-missing profile pages out of the adjudication input window, marks speaker/event/commerce/article pages evidence-only, and uses bounded parallel drain for independent `crm.public_web.evidence.adjudicate` commands. Cached GPT 5.5 back-half rerun `output/w7g_jackie_adjudication_input_recheck_v2/` selected `jbowocky` and did not select `staceywueste` or the wrong Scholar profile; GPT left `jbowocky` as `needs_review` due weak corroboration, which is the intended fail-closed behavior.
  - 2026-05-29 expanded live evidence-window pass: guarded pre-Agent contract passed before live execution, then a force-refresh three-person Anthropic sample ran with `max_fetches_per_candidate=10`, `max_ai_entry_links=10`, and `max_ai_evidence_documents=10` through canonical CRM endpoints using expected model `gpt-5.5` and no model fallback. Source report: `output/w7g_anthropic_public_web_live_validation_budget_force_refresh_3p.json`; post-materialization quality report: `output/w7g_anthropic_public_web_signal_materialization_quality_after_gate.json`. Result after the signal gate: Yuwei Qin exposes confirmed Scholar plus reviewable GitHub/X only, Jackie Bow exposes GitHub plus reviewable `jbowocky` only, and Brian E. exposes no user-visible signals. Previously noisy Yuwei Nottingham email, Brian Goldman Scholar, ResearchGate publication pages, and Stacey Wueste X links remain in artifacts/evidence audit only and are not normal user-visible signals.

- [ ] **Phase W9: recoverable operation API/UI and approval gates.** Backend control/provenance foundation is partially complete: `/api/operations/action-registry`, `POST/GET /api/operations/actions`, `GET /api/operations/runs`, action/run query, action approve/reject, operation cancel/retry/resume, command registry/query, safe queued-state command cancel/retry/resume, and run provenance now persist through PG-only durable tables. Retry creates an idempotent queued child run and does not rewrite a terminal parent. Owner-adapter slices are active: `export_candidates` dispatch plans `workflow_commands(command_type='export.projection.generate', owner='projection_exporter')` with `operation_id=<operation_run_id>` and no synchronous owner execution; `filter_projection` / `search_projection` dispatch through the canonical projection reader and persist bounded result without module-state writes; `enrich_person_public_web` dispatch plans `workflow_commands(command_type='crm.public_web.queue_batch', owner='crm_public_web_owner')`; the queue-batch command owner creates CRM Public Web batch/run rows, then plans per-run phase commands instead of directly writing module rows or spawning workers from Operation dispatch. W9b.3 completion propagation is active for projection export, CRM Public Web queue-batch/downstream phase commands, and CRM writer commands. W9b.4 CRM mutation adapters are active for `add_to_crm`, `set_crm_stage`, `add_crm_note`, and `create_crm_task`: Operation dispatch plans `crm_writer` commands only, the command owner writes CRM records/engagements/events plus PG-only `crm_tasks`, sensitive/bulk stage changes require approval before planning, and CRM writer commands now record ActivityRun/Attempt/EntityDelta evidence for CRM record/event/note/task effects. 2026-05-27 W11 control follow-up: stale `claimed/running` CRM writer commands expose owner-specific cancel/resume through `crm_writer.cancel_before_mutation_attempt` and `crm_writer.resume_crm_writer_command`; these delegates record Activity/Attempt/EntityDelta control evidence and either cancel before CRM mutation evidence or requeue without direct CRM table mutation. Once a CRM writer ActivityAttempt, EntityDelta, or downstream command exists, cancellation fails closed and retry/resume must go through the command owner. 2026-05-26 status-surface follow-up: operation-run detail now returns `operation_run.status_summary`, list can opt in with `include_status_summary=true`, command detail returns a bounded `workflow_command.execution_summary`, command list can include the same summary with `include_execution_summary=true`, and operation-run provenance returns commands through the same API record shape; both summaries are read-only, fail-closed, and source status from operation/runtime tables rather than domain read models. Same-day frontend contract follow-up: action queue / operation queue methods and typed response mappers are in `contracts/frontend_api_adapter.ts`, covering action list/submit/query/approve/reject, run list/query/provenance/cancel/retry/resume/dispatch. Same-day product-surface follow-up: `/operations` is now wired into the frontend shell as a minimal Operation Queue page that lists approval-required `AgentAction` rows, calls approve/reject action APIs, lists `OperationRun` rows, loads provenance, shows command Activity/Delta summaries, and calls only operation control APIs for dispatch/resume/retry/cancel. Same-day Activity-spine copy/drill-down/control follow-up: `/operations` now renders command status/kind counts, latest effect, latest reason/entity, and sample truncation from `workflow_command.execution_summary`; can load read-only ActivityRun/ActivityAttempt/EntityDelta rows by command id through workflow runtime APIs; and exposes command-level cancel/resume/retry buttons that call `/api/workflow/commands/{command_id}/cancel|resume|retry` only when `workflow_command.control_state.allowed_actions` permits it. It does not infer command state/control from command names, timestamps, owner-private result JSON, Activity rows, or domain read models, and it does not control Activity/Attempt/Delta rows directly. Same-day display-contract follow-up: workflow command rows, registry records, command-control responses, and Activity/Lane control targets now expose `display_contract` from `durable_runtime.workflow_command_display_contract`; `/operations` renders command title/category from that contract and keeps raw `command_type` as a debug id only. Remaining W9b work: broader product polish; this page is not Phase 13 natural-language Agent UI.
  - 2026-05-26 company Public Web operation slice: `refresh_company_public_web_assets` is now an approval/budget-gated Operation action. Dispatch plans `workflow_commands(command_type='company.public_web.refresh', owner='company_public_web_owner')` only; the root command owner now orchestrates phase commands instead of executing the synchronous refresh service inline. `company.public_web.source.collect` writes source-specific company Public Web rows/artifacts with canonical asset sync deferred; `company.public_web.assets.materialize` syncs canonical `CompanyAsset` / `CompanyEvidence`, records ActivityRun/Attempt/EntityDelta evidence, and propagates command completion back to the linked OperationRun/AgentAction. Operation dispatch does not call the synchronous refresh service inline.
  - 2026-05-26 Operation display-contract follow-up: Operation action registry/action/run records and operation control/provenance responses now expose `display_contract` from `operation_runtime.ActionRegistry.display_contract_for`; operation control responses also expose it at the top level beside `control_state`. `/operations` renders action/run titles and categories from that backend contract; raw `action_type` and `operation_type` remain technical/debug identifiers, not product-copy sources.
- [ ] **Phase W10: pre-Agent full contract review and fail-closed signoff.** Before Phase 13, review projection, workflow readers, CRM, person assets/evidence/assertions, Public Web, Excel, export, and operation APIs for owner/source-of-truth matrices, field visibility, fallback status, migration bridges, deletion conditions, and fast contract preflights. 2026-05-26 slice: durable command owner preflight now covers LinkedIn acquisition/profile, projection/collection/snapshot, CRM Public Web queue/phase, CRM writer, Excel, and export command families; PersonAsset/CRM/projection API contract preflight passed after updating the CRM Public Web test expectation from pre-W7 worker queueing to W7f phase-command queueing. 2026-05-26 follow-up: `docs/PRE_AGENT_CONTRACT_REVIEW.md` is now the W10 evidence ledger for module owner/source-of-truth, Agent-callable surface, fast preflight, migration-only bridge, and unresolved direction decisions; `tests/test_pre_agent_contract_review.py` turns that ledger into a fast preflight and blocks durable/Operation/W11/CRM task current-state table DDL from reappearing in SQLite. Same-day evidence: `crm_tasks` moved to PG live DDL only and is treated as PG-only current state alongside Operation/W11 tables. W11d direction is now recorded as the generic activity spine (`workflow_activity_runs`, `workflow_activity_attempts`), entity-effect layer (`workflow_entity_deltas`), plus domain read model (`acquisition_discovery_lanes`), and preflight blocks those tables from SQLite normal-path DDL. Candidate-document fallback is now explicitly migration/reuse compatibility only: `SOURCING_ENABLE_LEGACY_CANDIDATE_DOCUMENTS_FALLBACK` defaults off; normal projection/public-reader paths must use `allow_candidate_documents_fallback=False` and fail closed when canonical materialized artifacts are not ready. Projection filter scan fallback is likewise migration/debug-only: normal Agent/UI projection reads must use `projection_person_search_index` or fail closed with `projection_person_search_index_unavailable`, not scan projection membership under `SOURCING_ALLOW_LEGACY_PROJECTION_FILTER_SCAN_FALLBACK`. Same-day W10 env inventory preflight now lists every remaining legacy/fallback env that affects public reads, workflow execution, export, CRM, or Agent-callable surfaces, and blocks any new unregistered legacy/fallback env from bypassing the review ledger. Same-day status-summary preflight blocks `operation_run.status_summary` and `workflow_command.execution_summary` from reading domain read models; they may only summarize operation/runtime tables and Activity spine evidence. `make ci-pre-agent-contract` is now the unified fast W7/W9/W10/W11 gate before Phase 13, long W6/nightly, or live-provider product validation; it runs W10 preflight, operation runtime, W7e Public Web boundary/audit, durable command-owner signoff proof, and the guarded W7g CRM Public Web dry-run runner so live validation cannot be the first place basic endpoint/owner/model guards are checked. Phase 13 may start only after this review has no normal-path hidden fallback or dual-write owner.
  - 2026-05-26 company asset command preflight follow-up: durable command owner contracts now include `company.public_web.refresh`, `company.public_web.source.collect`, and `company.public_web.assets.materialize`; W10 documentation requires ActivityRun/Attempt/EntityDelta evidence for company Public Web phase commands. Source-specific `company_public_web_assets` rows or command result JSON alone are not sufficient Agent execution proof.
  - 2026-05-26 Operation/Command display-contract gate evidence: after adding backend-owned `operation_action.display_contract`, `operation_run.display_contract`, top-level operation-control `display_contract`, and registry-required action display descriptions, `make ci-pre-agent-contract` passed with `88 passed`, `1 passed, 14 deselected`, and `2 passed, 46 deselected`. This keeps product copy/category ownership in backend registries instead of frontend type-string heuristics.
  - 2026-05-27 ActionRegistry exposure-gate preflight follow-up: `activity_spine_policy.agent_callable` is now documented as necessary but not sufficient for Agent exposure. `operation_runtime.ActionRegistry.allowed_workflow_command_types` is the only normal Agent command allowlist, and registry records expose `workflow_command_exposure_gate`, `workflow_command_exposure_status`, plus per-command `agent_exposure_gate` / `agent_exposure_status`. W10 preflight now blocks UI/Agent code from treating the durable owner registry or Activity-spine eligibility as the product exposure list.
  - 2026-05-27 workflow command registry exposure follow-up: `/api/workflow/command-registry` now marks every durable command with `agent_exposure_gate=operation_runtime.ActionRegistry.allowed_workflow_command_types` and `agent_exposure_status`. Internal phase commands such as `crm.public_web.search.submit` are visible for owner/control/debug but explicitly `not_action_registry_allowlisted`; action commands such as `crm.public_web.queue_batch` are `action_registry_allowlisted`. Fast preflight now checks the ActionRegistry allowlist as a total function over the durable owner registry, and Operation runtime regression verifies registry/list/detail command rows all expose the same status, so adding a new owner command cannot accidentally promote it to an Agent product action.
  - 2026-05-27 Operation UI display fail-closed follow-up: `/operations` no longer falls back to owner/action/command type strings for product labels or categories when display contracts are missing. It renders explicit `Display contract missing` / `display_contract_missing`, and W10 preflight blocks regressions that would mask backend registry/display-contract drift in the frontend.
  - 2026-05-27 Activity evidence read-only UI follow-up: `/operations` Activity spine drilldown now renders each ActivityRun/ActivityAttempt/EntityDelta row with its `mutation_contract` and `module_state_mutated=false` evidence. Control buttons remain command-owned only, and W10 preflight blocks regressions where frontend code treats Activity/Attempt/Delta rows as mutable workflow state.
  - 2026-05-27 CRM Public Web frontend endpoint preflight follow-up: frontend Public Web actions must call canonical CRM routes (`/api/crm/records/public-web-search`, `/cancel`, `/retry`) and must not reintroduce `/api/target-candidates/public-web...` aliases. The remaining `TargetCandidatePublicWeb*` type/function names are product naming debt, not endpoint ownership; normal execution is CRM Public Web only.
  - 2026-05-27 W10 frontend endpoint hardening follow-up: the canonical endpoint preflight now scans normal frontend source plus frontend contract/adapter files and blocks any `/api/target-candidates/public-web` substring, including future template-string aliases. The same check requires the CRM Public Web start, poll, cancel, retry, export, detail, and promotion routes to remain present, so product naming debt cannot silently become endpoint ownership drift.
  - 2026-05-27 running-control category follow-up: `durable_runtime.workflow_command_running_control_categories` now makes owner-registry command control categories explicit and W10 preflight requires exactly one category per command. `workflow_command.control_policy` now exposes `running_control_category` and `running_control_categories`, so Agent/UI callers do not infer provider/domain/orchestration semantics from command type strings or blocked reasons. CRM Public Web phase commands have their own category, and company Public Web now splits root orchestration (`company.public_web.refresh`) from provider collection (`company.public_web.source.collect`) and domain materialization (`company.public_web.assets.materialize`). This blocks future bugs where running cancel/resume semantics depend on branch order.
  - 2026-05-27 provider after-start/profile retry compatibility follow-up: provider after-start control is now documented as an Activity/Attempt semantic extension, not a replacement for item-level profile retry. Operation-native `linkedin.profile_fetch.provider.fetch` preserves the legacy scheduler invariant: normal-wave fetched URLs immediately plan `linkedin.profile_terminal.admit`, failed URLs emit `profile_provider_retry_bucketed`, and only those failed URLs plan a `provider_attempt_scope=retry_wave` command with `retry_strategy=bucketed_entity_retry_after_normal_wave`. A partial provider result must not mark the whole command `retry_wait`, retry successful URLs, or mix retry URLs into unfinished normal-wave work. Same-day gate closeout: `make ci-pre-agent-contract` now includes PG-backed `tests/test_enrichment.py -k 'isolates_retry_wait or retry_wait_as_isolated_batch'` with `SOURCING_REQUIRE_PG_DURABLE_RUNTIME_TESTS=1`, so this contract is checked in the fast W10 gate rather than only through W6/Nightly or SQLite compatibility tests.
  - 2026-05-27 provider after-start control policy follow-up: provider-attempt command `control_policy` now exposes explicit after-start fields through `provider_after_start_control_contract=w11_provider_after_start_control_v1`. The confirmed v1 strategy is Harvest/Apify/DataForSEO `provider_after_start_control_status=active` + `provider_after_start_control_mode=poll_cancel_late_result_quarantine`, while Document fetch and Qwen adjudication use `provider_after_start_control_mode=fail_closed_until_terminal`. Provider cancel after ActivityAttempt start may stop only local polling/waiting and quarantine late results; it must not kill remote work, write domain rows, or accept late provider results without an explicit owner adoption command.
  - 2026-05-27 DataForSEO item-level retry follow-up: DataForSEO Standard Queue `task_post` batch envelopes are now documented and tested as transport batches, not retry/progress units. Each query item uses stable `task_key` / `query_identity_key`, analogous to LinkedIn URL key, and downstream joins/retries/manifests/outcomes must use that key rather than provider response order. Provider response mapping must use provider-echoed `tag` or unambiguous provider-echoed `keyword`; missing provider identity fails closed to query-level retry/failure instead of request-order fallback. Successful query task ids stay submitted; only failed query items retry once through `retry_unit=search_query` / `retry_strategy=dataforseo_batch_failed_query_retry_only`. Concurrent `task_get` fetch also preserves sibling successes when one task id fails, recording failed fetch evidence as `retry_unit=dataforseo_task_id` / `retry_strategy=dataforseo_task_get_failed_task_retry_only`. Public Web batch submission now treats submit-failed checkpoints as item-level failed queries instead of keeping them as fake submitted work. `make ci-pre-agent-contract` includes this provider/Public Web regression slice so W6/Nightly is not the first detector for DataForSEO batch-envelope drift.
  - 2026-05-27 running-control maturity follow-up: `workflow_command.control_policy` now also exposes `running_control_maturity`, `running_control_gap_status`, and `running_control_surface=workflow_command_control_api_only`. Supported commands report `owner_specific_cancel_resume` / `closed`, `owner_specific_cancel_only` / `partial_resume_gap_reported`, or `owner_specific_resume_only` / `partial_cancel_gap_reported`; unsupported running commands report `fail_closed_with_upgrade_requirements` / `accepted_fail_closed_pending_owner_specific_control`. Agent/UI code can display the explicit gap without inferring capability from blocked-reason text or trying module-specific bypasses. Current durable owner-registry metrics report no `fail_closed_with_upgrade_requirements` commands and `40` owner-specific cancel+resume commands. Same-day W11 closeout: `acquisition.run.create`, `acquisition.intent.resolve`, `acquisition.plan.build`, and `company.public_web.refresh` now support owner-specific running cancel only before any downstream command is planned and only after lease expiry or explicit force; `acquisition.plan.commit` supports owner-specific running cancel only before probe command planning and marks the acquisition run `cancelled_before_probe`; `acquisition.scale.plan` supports owner-specific running cancel only before discovery command planning and before any activity attempt starts, marking the acquisition run/lane/activity `cancelled_before_discovery`; `crm.public_web.queue_batch` supports owner-specific running cancel before per-run phase commands exist, cancelling queued batch/run rows instead of leaving orphaned CRM Public Web state; `acquisition.plan_review.request` supports owner-specific running cancel through `workflow_orchestrator.cancel_acquisition_plan_review_request`, cancels/rejects the pending review checkpoint, does not plan `acquisition.plan.commit`, and is blocked after approval; provider-attempt commands support owner-specific local cancel/poll-stop until provider EntityDelta/downstream evidence exists; domain-mutation commands support owner-specific pre-attempt cancel and fail closed after ActivityAttempt/EntityDelta/downstream evidence. True remote provider interruption remains out of scope until a provider owner documents and tests a remote cancel endpoint.
  - 2026-05-27 media running-control audit: `media.asset.cache` originally remained fail-closed for running cancel because the owner performs fetch/upload/canonical asset write inside one bounded activity. W11l.12 now adds the first safe checkpoint: cancel is allowed only before media ActivityAttempt creation, media EntityDelta creation, downstream commands, and after lease expiry or explicit force. Cancel remains fail-closed after fetch/upload/materialization may have started; broader interruption still requires splitting media cache into fetch/upload/materialize typed activities or adding compensating-command semantics.
  - 2026-05-27 company Public Web phase split: `company.public_web.refresh` is now root orchestration; `company.public_web.source.collect` owns provider/collector source collection; `company.public_web.assets.materialize` owns CompanyAsset/CompanyEvidence sync. All three expose owner-specific running resume; root refresh, source collect, and asset materialize also expose bounded owner-specific running cancel before downstream planning, provider ActivityAttempt, or sync attempt. Source collect still fails closed after provider ActivityAttempt/EntityDelta/downstream evidence until provider interruption exists. This retires the previous single-owner activity shape for Agent normal path while keeping interruption semantics conservative.
  - 2026-05-27 company Public Web joined-run guardrail: `company.public_web.source.collect` keeps canonical asset sync deferred even when the source refresh service joins an existing source run. Only `company.public_web.assets.materialize` may sync `CompanyAsset` / `CompanyEvidence`; the joined source branch cannot act as a compatibility shortcut around the phase boundary.
  - 2026-05-27 company Public Web source resume closeout: stale running `company.public_web.source.collect` commands now support owner-specific resume through `company_public_web_owner.resume_source_collect`. Resume records ActivityRun/Attempt plus a queued `company_public_web_source_collect` EntityDelta, requeues the command, and keeps canonical asset sync deferred to `company.public_web.assets.materialize`.
  - 2026-05-27 company Public Web asset materialize running-control closeout: stale running `company.public_web.assets.materialize` commands now support owner-specific cancel before sync through `company_public_web_owner.cancel_assets_materialize_before_sync` and owner-specific resume through `company_public_web_owner.resume_assets_materialize`. Cancel is allowed only before ActivityAttempt/EntityDelta/downstream evidence; resume records ActivityRun/Attempt plus a queued `company_public_web_assets_materialize` EntityDelta and requeues without syncing CompanyAsset/CompanyEvidence from the request path.
  - 2026-05-27 media cache resume closeout: stale running `media.asset.cache` commands now support owner-specific resume through `media_asset_owner.resume_media_asset_cache`. Resume records ActivityRun/Attempt plus a queued `media_asset_cache` EntityDelta and requeues the command without request-path media fetch, object upload, or PersonAsset/CompanyAsset writes.
  - 2026-05-27 ActionRegistry control-summary follow-up: action registry records now expose `workflow_command_control_summary` derived from `allowed_workflow_command_contracts`. This gives Agent/UI callers a backend-owned action-level view of running-control maturity/gaps/categories, so they can show fail-closed commands explicitly without re-deriving control semantics from command type strings, owner names, or private result JSON.
  - 2026-05-27 operator backfill surface follow-up: all HTTP paths containing `/backfill` are treated as operator/migration surfaces unless a future Contract explicitly promotes one into an Operation action. W10 preflight now checks CRM, projection, person raw/evidence index, media, company asset, and Public Web promotion backfill routes are absent from the full frontend normal source tree, frontend normal contracts/adapters, `/operations`, and `operation_runtime.ActionRegistry`.
  - 2026-05-27 public-reader no-repair follow-up: W10 preflight now source-checks normal projection/person/company/media read methods and `ServingProjectionReader` methods for forbidden repair/mutation tokens (`backfill`, `rebuild`, `upsert`, `update`, `write`, `repair`, command planning). Public readers must fail closed or return canonical read contracts; repair remains explicit operator or Operation/Command owner work.
  - 2026-05-27 legacy/fallback env inventory hardening: W10 inventory now also records retired `SOURCING_ALLOW_MIGRATION_PROJECTION_FACET_LAYERING_OVERLAY` and `SOURCING_ENABLE_SQLITE_PROFILE_REGISTRY_FALLBACK`. `make ci-pre-agent-contract` now runs `tests/test_storage_surface_guardrails.py`, so retired SQLite/profile-registry fallback entrypoints cannot reappear in production source or scripts before Phase 13 work.
  - 2026-05-27 legacy Public Web priority-lane closeout: retired `/api/target-candidates/public-web...` aliases are now excluded from `_request_priority_lane`; they may only return unconditional `410` compatibility envelopes. W10 preflight checks canonical `/api/crm/records/public-web...` routes remain the only interactive/light-lane Public Web HTTP surface, so retired aliases cannot regain normal scheduling treatment. Validation: `tests/test_pre_agent_contract_review.py` `31 passed`, W7e runtime boundary `12 passed`, and `make ci-pre-agent-contract` passed with `98 passed`, `1 passed, 14 deselected`, and `2 passed, 47 deselected`.
  - 2026-05-27 historical asset/media backfill review gate: `POST /api/persons/backfill-public-web-signals`, `POST /api/company-assets/backfill-public-web-assets`, `POST /api/media/backfill-person-avatars`, and `POST /api/media/backfill-company-logos` still default to dry-run, but now reject `dry_run=false` unless `reviewed=true` is supplied. Rejected apply attempts return `operator_review_required` and `read_contract.apply_requires_reviewed=true`; the PG-only operator script already enforces `--apply requires --reviewed` and now passes that reviewed marker into route payloads.
- [ ] **Phase W11: Agent-callable workflow atomization gap closure.** Before natural-language Agent graph work, expose the remaining workflow phases as typed operation/command surfaces with query/retry/cancel/resume status instead of owner-private repair calls. Required review items: acquisition run root, intent/plan/plan-review, probe/scale and live-roster discovery commands, profile-registry refill/terminal-record commands, local-apply/board-visible/projection/index/collection commands, CRM Public Web internal run phases, and export/Excel long-running command state. Agent/OperationRun must target typed actions or `workflow_commands`; it must not write `job_materialization_items`, provider registry rows, projection rows, CRM rows, or Public Web run rows directly. Command query/control surface is active: `/api/workflow/command-registry`, `/api/workflow/commands`, `/api/workflow/commands/{command_id}`, safe queued-state controls `/cancel`, `/retry`, `/resume`, owner-specific running cancel/resume for CRM Public Web phase commands and projection/CRM Public Web export commands, owner-specific cooperative running cancel/resume for Excel intake commands, owner-specific running cancel/resume for early acquisition/company-root orchestration commands before downstream planning, `acquisition.plan.commit` before probe planning, `acquisition.scale.plan` before discovery command/activity attempt, `crm.public_web.queue_batch` before phase commands, and `acquisition.plan_review.request` before approval, plus owner-specific cancel/resume for provider-attempt and domain-mutation commands. These command query surfaces now expose `control_policy` from `durable_runtime.workflow_command_control_policy`, making generic statuses, owner-specific running control, delegate owner, fallback status, and module-state mutation expectation machine-readable for Agent/UI callers. Command detail also exposes `execution_summary` from ActivityRun/Attempt/EntityDelta evidence, and list can opt into that summary with `include_execution_summary=true`; this is the owner-neutral Agent/UI status card and it must not read domain tables to infer progress. Provider-attempt commands can be cancelled before provider EntityDelta/downstream evidence exists; after ActivityAttempt start Harvest/Apify/DataForSEO use local poll-cancel with late-result quarantine, while Document fetch/Qwen remain fail-closed until terminal. Domain-mutation commands similarly fail closed after ActivityAttempt/EntityDelta/downstream evidence exists. Activity query surface is active through read-only `/api/workflow/activities`, `/api/workflow/activities/{activity_run_id}`, `/api/workflow/activity-attempts`, `/api/workflow/activity-attempts/{attempt_id}`, `/api/workflow/entity-deltas`, and `/api/workflow/entity-deltas/{delta_id}`; these rows expose `control_target` back to the owning command with command-owned `display_contract`, `control_policy`, `control_state`, `activity_spine_policy`, and `fallback_status=fail_closed`, so Agent/debug controls never mutate activity, attempt, entity-delta, lane, registry, projection, CRM, Public Web, Excel, or export rows directly and never infer display/control state from domain read-model fields. Current acquisition/profile adapters: `start_acquisition_run` plans `acquisition.run.create` as the causal root command and no longer plans `linkedin.discovery_query.run` directly; `acquisition_run_writer` claims that root command, plans `acquisition.intent.resolve`, and keeps the linked operation running/deferred without creating a legacy job or calling `queue_workflow`; `acquisition.intent.resolve` deterministically normalizes bounded intent and plans `acquisition.plan.build`; `acquisition.plan.build` creates a typed plan result and plans `acquisition.plan_review.request` without model/provider calls; these three early orchestration commands and `company.public_web.refresh` can be cancelled while running only if no downstream command exists and the lease is expired or force is explicit; `acquisition.plan.commit` can be cancelled while running only before probe command planning and marks its acquisition run `cancelled_before_probe`; `acquisition.scale.plan` can be cancelled while running only before discovery command planning and before any activity attempt starts, marking its acquisition run, discovery lane, and activity run `cancelled_before_discovery`; `crm.public_web.queue_batch` can be cancelled while running only before per-run phase commands exist, marking queued batch/run rows cancelled; `acquisition.plan_review.request` creates or reuses a pending `plan_review_session`, moves the linked operation to `acquisition_plan_review_requested`, and can be cancelled while running before approval by cancelling the review checkpoint without planning `acquisition.plan.commit`; approved plan review calls plan `acquisition.plan.commit`, whose owner verifies the approved session, materializes canonical PG-only `acquisition_runs` with `status=committed_pending_probe`, and moves the linked operation to `acquisition_plan_committed_pending_probe`; `acquisition_runs` and future W11 current-state tables must be PG live DDL only, with no SQLite schema/fallback normal path; `fetch_profile_sample` plans `linkedin.profile_fetch.activity.run`; `continue_acquisition_run` can plan only command types exposed by the action registry's `allowed_workflow_command_types` when `command_type` and `command_payload` are explicit, so a durable command type is not Agent-callable unless the action contract permits it. 2026-05-26 W11d foundation: `acquisition.scale.plan` now materializes PG-only `workflow_activity_runs` and `acquisition_discovery_lanes`, with `workflow_activity_attempts` as the provider-attempt envelope and `workflow_entity_deltas` as the explanation/projection-causality layer; these rows are read-model/current-state evidence and do not execute the legacy job-shell discovery owner. Same-day follow-up: `continue_acquisition_run -> linkedin.discovery_query.run` now requires existing `acquisition_run_id` + `lane_id` + `activity_run_id`, rejects legacy job/snapshot discovery payloads, and `operation_native_discovery_activity_owner` records activity attempts, runs provider-backed discovery in operation-native mode, writes model-safe artifacts, records positive candidate entity deltas, plans downstream `linkedin.profile_fetch.activity.run`, and leaves the linked Operation running for profile fetch / projection admission. W11e first slice: `linkedin_profile_activity_owner` claims operation-native profile-fetch commands, records cache-hit or fetch-required profile entity deltas, updates the acquisition run to `profile_fetch_activity_planned`, and does not call `queue_workflow`, create a legacy job shell, or invoke legacy profile refill as an Agent normal path. W11f/W11g slices: `linkedin.profile_fetch.provider.fetch` consumes `profile_fetch_required` deltas, records provider attempts, marks fetched raw paths in `linkedin_profile_registry`, emits `profile_provider_fetched` deltas, and remains retryable as a typed command/activity boundary; `linkedin.profile_terminal.admit` consumes cache-hit/provider-fetched deltas and emits `profile_terminal_recorded` deltas without mutating projection membership; `projection.profile_admission.apply` consumes terminal deltas, writes canonical run-scope `serving_projection_members` through `serving_projection_owner`, and emits `projection_member_admitted` deltas. Retryable provider errors remain retry-wait attempts; zero-result discovery is represented by no-op entity deltas. The legacy search-seed queue owner report-visibly skips operation-native commands instead of claiming them. Long-term target: every bounded side effect should be represented by Command -> ActivityRun -> ActivityAttempt -> EntityDelta/domain read-model causality, so Agent can retry/cancel/resume/debug provider poll, dataset fetch, parse, profile terminalization, Public Web signal materialization, export chunking, and projection admission without mutating domain tables directly. Domain tables such as `acquisition_discovery_lanes` remain efficient read models, not universal workflow state. Future architecture rule now recorded: if a new Agent-callable module needs retry/cancel/resume/debug, add or reuse a typed command/activity/entity-delta boundary first, then add a domain read model only for efficient product queries. Current remaining W11 work is product UI polish beyond the minimal `/operations` queue and manual/live validation of CRM Public Web quality; stronger remote provider interruption remains a future provider-specific contract, not a blocker for v1 local poll-cancel/quarantine. This phase is mostly API/status/owner-boundary hardening on top of W2/W6/W7/W9, not Phase 13 natural-language Agent UI.
  - 2026-05-27 current goal pause line: W7e and W7f are closed for normal runtime; W10/W11 local pre-Agent contract/atomization evidence is covered by `make ci-pre-agent-contract`. Provider after-start control v1 is confirmed and implemented as local poll-cancel/quarantine for Harvest/Apify/DataForSEO plus fail-closed-until-terminal for Document fetch/Qwen. The remaining top-level external gate is W7g, which requires reviewed CRM record ids plus explicit live-provider confirmation and is not another hidden fallback/dual-owner implementation slice. Do not use W6/nightly or extra guardrail patches to substitute for that product/live decision.
  - 2026-05-26 W11h follow-up: operation-native projection admission now plans existing `projection.person_search_index.build` and `collection.authoritative.merge` typed commands on the same Operation workflow id after run-scope projection publication. These downstream commands are background asset/index updates, not blocking proof for current run result visibility, and they are planned without a legacy job shell or `job_materialization_items` normal-path write. The projection-index and collection-merge owners now also record `workflow_activity_runs`, `workflow_activity_attempts`, and `workflow_entity_deltas`, so Agent/debug queries can see whether a projection index page was built, whether a collection pointer was updated, or why either effect was not applied. Same-day W11i slice starts applying the same activity spine to CRM Public Web phase commands, export command owners, Excel intake terminal thread results, and CRM writer commands. Latest W11j slice moves `linkedin.profile_refill.submit_batch`, `linkedin.profile_url_terminal.record`, `linkedin.local_profile_delta.apply`, `projection.board_visible_patch.publish`, `projection.run_scope.finalize`, `projection.facet_layering.build`, and `snapshot.compaction.run` from legacy-internal or command-result-only evidence to ActivityRun/Attempt/EntityDelta-backed evidence: the profile-refill submit owner records per-profile-url `profile_refill_submit` deltas for provider submit queued/deferred/failed effects; the profile URL terminal owner records per-profile-url `profile_url_terminal_record` deltas before registry terminal state can unblock local apply; the local-apply owner records `local_profile_delta` deltas for completed, partial, waiting-prerequisite, retry-wait, failed, and already-completed effects; the board-visible owner records `board_visible_patch` deltas for completed, waiting-prerequisite, retry-wait, and failed board publication effects; the `serving_projection_owner` records `run_scope_projection` deltas for published, obsolete, retry-wait, and failed finalization effects; the facet/layering owner records `facet_layering` deltas for completed, partial, retry-wait, and failed layering effects; and the snapshot owner records `snapshot_compaction` deltas for background full-materialization maintenance. Long-term architecture target: every bounded side effect uses Command -> ActivityRun -> ActivityAttempt -> EntityDelta/domain read-model causality; domain read models such as `acquisition_discovery_lanes`, Public Web run rows, export rows, Excel rows, CRM rows, registry rows, and projection rows are efficient/queryable materializations, not execution owners. W11j target: extend entity deltas from run-level facts to important entity-level facts wherever the domain read model already exists; current slices cover CRM Public Web document-level/signal-level deltas, CRM writer record/event/note/task deltas, profile-refill submit deltas, profile URL terminal deltas, local-apply deltas, board-visible publication deltas, run-scope projection finalization deltas, facet/layering deltas, and snapshot compaction deltas without introducing another module-specific workflow state table. Remaining W11 work narrows to provider in-flight interrupt/checkpoint design after ActivityAttempt start, product UI polish beyond the minimal `/operations` queue, and manual/live validation of CRM Public Web quality.
  - 2026-05-26 W11j policy closeout: `durable_runtime.workflow_command_activity_spine_policy` is now the central policy for whether a command must write ActivityRun/Attempt/EntityDelta evidence, only creates a downstream activity boundary, or is orchestration-only. Action registry / command registry / activity control-target records expose this policy. Normal owner registry now has no `legacy_internal_pending_activity_spine` command types; Agent exposure is still gated separately by the action registry allowlist.
  - 2026-05-26 W11k read-model query closeout: acquisition discovery lanes now have a read-only `/api/workflow/discovery-lanes` query surface plus per-lane detail endpoint. Lane rows expose `control_target` back to the owning command and keep `module_state_mutated=false`, so Agent/UI can explain lane state without direct table mutation; retry/cancel/resume remains command-owned.
  - 2026-05-26 W11l control-state closeout: command rows, command control responses, and Activity/Attempt/EntityDelta/Lane `control_target` records now expose `control_state` computed by `durable_runtime.workflow_command_control_state`. This combines current command status with `workflow_command_control_policy` and is the only source for allowed `cancel` / `retry` / `resume` actions, cancel mode, disabled reasons, and module-state mutation expectation. Agent/UI surfaces must render controls from `control_state`, not from local status-string heuristics.
  - 2026-05-26 W11l.2 running-resume policy closeout: `workflow_command_control_policy` now explicitly exposes `running_resume_supported`, `running_resume_blocked_reason`, `running_resume_upgrade_requirements`, and `unsupported_running_resume_reason`; `workflow_service_metrics.durable_command_owner_contracts` reports the same fields for signoff and includes control-policy drift in `violation_detected` through `control_policy_violation_count`. Generic resume remains limited to `retry_wait`; `claimed/running` resume is allowed only through owner-specific delegates that record Activity/Attempt/EntityDelta evidence and requeue without request-path domain side effects. Scripted signoff blocks active durable command contracts whose `control_policy` is missing, not `fail_closed`, or still uses placeholder running cancel/resume fields even when owner and causality counts are clean, so W6/nightly no longer acts as the first detector for basic Agent command-control drift. Same-day W10 hardening also puts `display_contract_violation_count` and `activity_spine_policy_violation_count` under `durable_command_owner_contracts.violation_detected`, and signoff blocks missing command display copy or `legacy_internal_pending_activity_spine` regressions on active command contracts. This prevents Agent/UI from treating resume as a generic repair button for in-flight provider calls, artifact writes, local threads, or domain mutations. Validation: targeted preflight/service-metrics slices passed; follow-up `tests/test_scripted_smoke_signoff.py -k durable_command` passed with `3 passed, 46 deselected`; refreshed `make ci-pre-agent-contract` passed with `88 passed`, `1 passed, 14 deselected`, and `2 passed, 47 deselected` after the metrics-owned control/display/activity policy gates were added.
  - 2026-05-27 W11l.3/W11l.5 owner-specific CRM Public Web phase resume closeout: all per-run phase commands now register `crm_public_web_owner.resume_crm_public_web_phase_command` as their claimed/running resume delegate. The API blocks active-lease resume unless `force=true`, records ActivityRun/Attempt and `crm_public_web_run` EntityDelta resume evidence, and requeues the command for normal CRM Public Web owner drain.
  - 2026-05-27 W11l.6 orchestration running-resume closeout: orchestration commands now register `workflow_orchestrator.resume_orchestration_command` as their claimed/running resume delegate. Resume requires an expired lease or explicit force, records ActivityRun/Attempt plus a queued `workflow_orchestration_command` EntityDelta, and requeues without running reducers, creating downstream commands, calling providers, or writing domain read models from the request path.
  - 2026-05-27 W11l.7 provider-attempt running-resume closeout: provider-attempt commands now register `workflow_provider_owner.resume_provider_attempt_command` as their claimed/running resume delegate. Resume requires an expired lease or explicit force, records ActivityRun/Attempt plus a queued `workflow_provider_attempt_command` EntityDelta, and requeues without calling providers, polling remote work, writing registry rows, or publishing artifacts from the request path.
  - 2026-05-27 W11l.8/W11l.13 domain-mutation running-control closeout: remaining domain-mutation commands now register `workflow_domain_owner.resume_domain_mutation_command` as their claimed/running resume delegate and `workflow_domain_owner.cancel_before_domain_mutation_attempt` as their pre-attempt cancel delegate. Resume requires an expired lease or explicit force, records ActivityRun/Attempt plus a queued `workflow_domain_mutation_command` EntityDelta, and requeues without writing registry, projection, collection, snapshot, CRM, Public Web, media, or asset read models from the request path. Cancel is allowed only before domain-mutation ActivityAttempt creation, before domain-mutation EntityDelta creation, before downstream commands, and after lease expiry or explicit force; after mutation evidence exists it fails closed.
  - 2026-05-27 W11l.9 Excel running-resume closeout: `excel.intake.run` now registers `excel_intake_owner.resume_excel_intake_run_command` as its claimed/running resume delegate. Resume requires `payload.job_id` plus expired lease or explicit force, records ActivityRun/Attempt plus a queued `excel_intake_command` EntityDelta, and requeues without starting a local thread or writing Excel result materializations from the request path.
  - 2026-05-27 W11l.10 profile-fetch running-cancel closeout: `linkedin.profile_fetch.activity.run` now exposes owner-specific cancel+resume instead of generic domain-mutation cancel blocking. Running cancel delegates to `linkedin_profile_activity_owner.cancel_before_cache_lookup_attempt` and is allowed only before cache/planning `workflow_activity_attempt` creation, before profile `workflow_entity_deltas`, before downstream commands, and after lease expiry or explicit force. The owner marks linked activity/acquisition state `cancelled_before_cache_lookup`; once cache lookup or downstream planning starts, cancellation fails closed and callers must use Command/Activity resume or retry. Durable command metrics now report `owner_specific_cancel_resume=18` and `owner_specific_resume_only=22`.
  - 2026-05-27 W11l.11 CRM writer running-cancel closeout: CRM writer mutation commands (`crm.record.add_from_projection`, `crm.record.update`, `crm.note.add`, `crm.task.create`) now expose owner-specific cancel+resume. Running cancel delegates to `crm_writer.cancel_before_mutation_attempt` and is allowed only before CRM writer ActivityAttempt creation, before CRM writer EntityDelta creation, before downstream commands, and after lease expiry or explicit force. The owner can mark preplanned CRM writer activities `cancelled_before_crm_mutation`, but it must fail closed once CRM table mutation may have started. Durable command metrics now report `owner_specific_cancel_resume=22` and `owner_specific_resume_only=18`.
  - 2026-05-27 W11l.12 media cache running-cancel closeout: `media.asset.cache` now exposes owner-specific cancel+resume. Running cancel delegates to `media_asset_owner.cancel_before_fetch_upload_attempt` and is allowed only before media ActivityAttempt creation, before media EntityDelta creation, before downstream commands, and after lease expiry or explicit force. The owner can mark preplanned media activities `cancelled_before_fetch_upload`, but it must fail closed once media fetch/upload or asset writes may have started. Durable command metrics now report `owner_specific_cancel_resume=23` and `owner_specific_resume_only=17`.
  - 2026-05-27 W11l.13 generic domain-mutation running-cancel closeout: projection/index/collection/snapshot/profile terminal/local-apply/domain materialization commands now expose owner-specific pre-attempt cancel+resume through the generic domain owner. The control API can cancel only when no ActivityAttempt, EntityDelta, or downstream command exists; otherwise it returns `domain_mutation_cancel_blocked_after_attempt_started` or downstream-planned blocked reasons. Durable command metrics now report `owner_specific_cancel_resume=33` and `owner_specific_resume_only=7`.
  - 2026-05-27 W11l.14 company asset materialize running-cancel closeout: `company.public_web.assets.materialize` now exposes owner-specific pre-sync cancel+resume through the company Public Web owner. The control API can cancel only when no materialize ActivityAttempt, EntityDelta, or downstream command exists; otherwise it returns `company_public_web_assets_materialize_cancel_blocked_after_sync_started` or downstream-planned blocked reasons. Durable command metrics now report `owner_specific_cancel_resume=34` and `owner_specific_resume_only=6`.
  - 2026-05-27 W11l.15 provider-attempt running-cancel closeout: provider-attempt commands (`acquisition.probe.submit`, `acquisition.probe.collect`, `linkedin.discovery_query.run`, `linkedin.profile_refill.submit_batch`, `linkedin.profile_fetch.provider.fetch`, and `company.public_web.source.collect`) now expose owner-specific cancel+resume before provider EntityDelta/downstream evidence exists. The control API cancels only local durable command/activity state before ActivityAttempt start. After Harvest/Apify/DataForSEO ActivityAttempt start, it may stop local polling and mark the remote result ignored/quarantined; after EntityDelta or downstream command evidence it returns `provider_attempt_cancel_blocked_after_provider_entity_delta_recorded` or the downstream-planned blocker. Durable command metrics now report `owner_specific_cancel_resume=40` and `owner_specific_resume_only=0`.
  - 2026-05-27 W11l.16 DataForSEO query-item identity closeout: DataForSEO Standard Queue batch requests now follow the same item-level identity rule as profile URL fetch. Every query spec must carry caller-assigned `task_key` / `query_identity_key`; provider/search-chain code fails closed when it is missing and may not generate identity from query text, task id, response/request order, or array position. `task_post` retry remains query-item scoped (`retry_unit=search_query`, `retry_strategy=dataforseo_batch_failed_query_retry_only`), and `task_get` failure remains task-id scoped (`retry_unit=dataforseo_task_id`, `retry_strategy=dataforseo_task_get_failed_task_retry_only`) without discarding sibling successes.
  - 2026-05-28 W11l.17 Public Web query identity closeout: Public Web batch query keys now use stable candidate/query identity (`public_web_query_identity_key`) instead of candidate display ordinal. This keeps DataForSEO/Public Web retry, poll, fetch, manifest, and materialization joins aligned with the LinkedIn URL-level work item model even when candidate order changes inside a transport batch.
  - 2026-05-28 W11l.18 Search seed query identity closeout: search seed discovery worker keys now use stable search query signature + bundle + employment scope instead of query-list ordinal when dispatching/prefetching DataForSEO-style batch work. Legacy ordinal-shaped keys are retained only when no query text is available; normal call sites pass query text and are covered by `ci-pre-agent-contract`.
  - 2026-05-28 W11l.19 Exploratory enrichment query identity closeout: exploratory enrichment DataForSEO-style batch keys now use stable candidate/query identity (`candidate_id::q_<query_hash>`) instead of `candidate_id::index`; `query_index` remains only display/file naming metadata. This prevents query-order changes from creating new provider work identities or mispairing ready/fetch responses.
  - 2026-05-27 W7e import-boundary closeout: worker daemon no longer imports `legacy_target_candidate_public_web_runtime`; historical `target_candidate_public_web_search` workers are quarantined through a local retired recovery-kind constant only. The old `run-target-candidate-public-web-experiment` CLI command now returns a report-visible retired envelope and no longer imports or calls the target-candidate Public Web experiment implementation. Regression matrix coverage points to CRM Public Web runtime-boundary tests plus migration-only legacy storage tests instead of treating the legacy experiment as a runnable product path.
  - 2026-05-27 W11l.4 export running-resume closeout: `export.projection.generate` and `export.crm_public_web.generate` now register owner-specific running resume delegates (`projection_exporter.resume_export_command`, `crm_public_web_exporter.resume_export_command`). The API blocks active-lease resume unless `force=true`, records export ActivityRun/Attempt and export EntityDelta resume evidence, and requeues the command without publishing an artifact. Export cancel/resume therefore both stay command-owner controlled and never mutate export read models directly from Agent/UI.
  - 2026-05-26 W11m operation-control closeout: OperationRun rows and Operation control responses now expose `control_state` computed by `operation_runtime.operation_run_control_state`. `/operations` renders dispatch/resume/retry/cancel availability from this backend state instead of a local terminal-status set, and retry selection follows the returned child OperationRun instead of staying on the terminal parent, keeping run-level controls aligned with OperationRuntimeWriter semantics.
  - 2026-05-26 W11n company Public Web refresh closeout: company-level refresh now follows the same Command -> ActivityRun -> ActivityAttempt -> EntityDelta/domain read-model boundary as other Agent-callable side effects. The root `company.public_web.refresh` command plans source collection and materialization phase commands; `company.public_web.source.collect` writes source-specific company Public Web rows/artifacts; `company.public_web.assets.materialize` writes canonical `CompanyAsset` / `CompanyEvidence` and records `company_public_web_run`, `company_asset`, and `company_evidence` deltas. Source collect has owner-specific running resume; assets materialize has owner-specific pre-sync cancel+resume. Remaining company-asset work is historical company fact backfill, richer company tabs, source-provider interrupt design, and live product validation.
  - 2026-05-26 W11o media asset cache closeout: stable person/company media now has a generic bounded command owner. `media.asset.cache` is owned by `media_asset_owner`, accepts inline media payloads or bounded URL/file fetch inputs, writes object storage refs plus canonical `PersonAsset.avatar_media` / `CompanyAsset.logo_media`, and records `person_asset` / `company_asset` EntityDeltas. `/api/media/assets/{asset_id}` serves object-backed cached media through a fail-closed read API. Projection candidate rows now expose `media_summary`, and frontend candidate display prefers that canonical media contract over provider hotlink metadata. Provider media URLs remain metadata until this owner writes a stable asset; remaining media product work is historical backfill.
  - Future W11 extension rule: any new Agent-callable bounded side effect must first add or reuse a typed `workflow_commands` boundary, explicit control policy, explicit Activity-spine policy, ActivityRun/Attempt evidence, and EntityDelta/domain read-model causality. Domain tables are allowed only as efficient read models for product queries; they must not own retry, cancel, resume, provider attempts, or recovery state. Agent/UI/recovery code must call Operation/Command APIs or owner-specific command handlers, never directly mutate lane, registry, projection, CRM, Public Web, Excel, export, evidence, or asset rows to simulate execution progress. Projection export, CRM Public Web export, and Excel intake now expose owner-specific running cancel; export owners publish artifacts through a command-status/temp-file checkpoint, and Excel writes a durable cancel marker plus thread terminal/materialization checkpoints before terminalizing. These requirements are visible through command query/control APIs and `workflow_service_metrics.durable_command_owner_contracts`, so signoff can catch unsafe control-surface drift before W6/nightly.
  - Future Activity Spine target architecture: keep `workflow_activity_runs` as the generic bounded-side-effect spine and `workflow_activity_attempts` as the provider/local attempt envelope; keep `workflow_entity_deltas` as the explainability layer for why a candidate, profile, evidence item, asset, assertion, export artifact, or projection member was created, suppressed, not applied, or skipped. Domain tables such as `acquisition_discovery_lanes`, profile registry rows, CRM rows, Public Web rows, Excel rows, export rows, and projection rows remain owner-specific read models that reference command/activity causality; they must not become a second workflow engine. The intended Agent capability is to retry, cancel, resume, or inspect any typed activity such as provider poll, dataset fetch, parse, profile terminalization, Public Web signal materialization, export chunking, projection admission, index page build, or collection merge through Operation/Command/Activity APIs. W11/W10 preflight must block any new Agent-callable side effect that lacks command owner registry entry, control policy, Activity-spine policy, PG-only storage boundary, EntityDelta evidence, and a fail-closed domain read model contract.
  - 2026-05-26 W10 frontend contract follow-up: `contracts/frontend_api_contract.ts`, `contracts/frontend_api_contract.schema.json`, `contracts/frontend_api_adapter.ts`, and `docs/FRONTEND_API_CONTRACT.md` now include the Operation action registry, workflow command registry, command cancel/retry/resume responses, command/activity/attempt/entity-delta query responses, `display_contract`, `control_policy`, and `activity_spine_policy`. Command records and command control responses expose `display_contract`, `control_policy`, and `activity_spine_policy` at the top level as well as on `workflow_command`, keeping future Agent UI from inferring labels, command control, or Activity-spine requirements from command names. `operation_runtime.ActionRegistry` now blocks unregistered, duplicate, default-mismatched, and non-Agent-callable workflow command exposure at registration time; `ActionRegistry.to_record()` owns action-to-command contract enrichment directly, so non-HTTP Agent/runtime callers get the same owner/display/control/activity policy records as `/api/operations/action-registry`.

- [ ] **Phase 5c: company asset overview with stable company media.** Company overview now exposes an explicit `company_media` contract. If a PG-only `CompanyAsset(asset_type='logo_media')` exists, overview/detail payloads return `logo_status=available`, `logo_url`, and `logo_asset_id`; otherwise they return `logo_status=logo_unavailable` and the frontend renders initials only as a placeholder. Company Public Web refresh now syncs model-safe homepage/research/blog/publication rows into canonical `CompanyAsset` / `CompanyEvidence` when PG-only storage is active, so future company tabs can consume the canonical layer instead of source-specific Public Web rows. 2026-05-26 media owner slice: `media.asset.cache` owned by `media_asset_owner` now imports/fetches bounded logo media, uploads it to object storage, writes `CompanyAsset(asset_type='logo_media')`, records ActivityRun/Attempt/EntityDelta evidence, and exposes cached object-backed media through `/api/media/assets/{asset_id}`. `POST /api/media/backfill-company-logos` is now the explicit historical logo repair entrypoint; it defaults dry-run, accepts explicit logo URLs or existing logo evidence/hotlink assets, and plans `media.asset.cache` commands instead of fetching in the request path. `POST /api/company-assets/backfill-public-web-assets` is the explicit historical source-specific company fact repair entrypoint; it defaults dry-run and syncs `company_public_web_assets` into `CompanyAsset` / `CompanyEvidence` only on operator apply. `scripts/backfill_person_company_asset_media.py` is the PG-only dry-run/apply entrypoint for both. Homepage favicon derivation is opt-in and report-visible. 2026-05-28 local evidence: the default automatic logo dry-run found no existing `logo_url` / homepage evidence, so a reviewed explicit favicon-source mapping was applied through `media.asset.cache`; `/api/collections?limit=50` now reports all 23 collection entries with `company_media.logo_status=available`, `/api/media/assets/{asset_id}` serving object-backed PNG/JPEG bytes, and `media_contract.fallback_used=false`. Same-day profile-experience logo audit found Harvest profile `companyLogo` fields in work-history entries and proved they can identify target-company logo candidates, but historical LinkedIn CDN URLs are time-bound and already expired; a live sample returned `403 text/html`, so historical `companyLogo` hotlinks must not be used as stable logo backfill. Fresh profile captures now have a dedicated normal-path command boundary: local profile apply only queues non-blocking `company.logo.profile_experience.discover` work; `company_asset_owner` reads at most one profile payload, no-ops when stable `CompanyAsset.logo_media` exists, turns one unexpired work-experience `companyLogo` URL into `CompanyEvidence.logo_url`, and then plans `media.asset.cache`. Expired or near-expiry URLs return `source_discovery_required` and fall back to logo source discovery without writing evidence or commands. Evidence: `output/company_logo_from_profile_experience_scan_20260528.json`, `tests/test_operation_runtime.py::test_profile_experience_company_logo_plans_evidence_and_media_cache`, and `tests/test_operation_runtime.py::test_profile_experience_company_logo_skips_existing_or_expired_sources`. Remaining Phase 5c work is not logo placeholders; it is company fact/tabs depth: official homepage, Research, Engineering Blog, publications, RSS/arXiv/OpenReview/crawl bundles, and selected company facts.

- [ ] **Phase 7c: person media and Public Web asset cutover.** Complete the PersonAsset layer beyond storage/index foundation: historical `person_public_web_assets` / `person_public_web_signals` need backfill/reader preference cleanup into canonical `PersonAsset` / `PersonEvidence`, and avatar serving must prefer stable object/media refs over provider `avatar_url` metadata. 2026-05-26 read-contract slice: projection person detail, person summary, and projection candidate page APIs now expose `media_summary` from `PersonAsset.avatar_media` when a stable public-summary asset exists, and return `avatar_unavailable` instead of treating provider hotlinks as stable media when it does not. Same-day media owner slice: `media.asset.cache` owned by `media_asset_owner` imports/fetches bounded avatar media, uploads it to object storage, writes `PersonAsset(asset_type='avatar_media')`, records ActivityRun/Attempt/EntityDelta evidence, and exposes cached object-backed media through `/api/media/assets/{asset_id}`. Frontend candidate display now prefers `media_summary.avatar_url` only when the media contract is canonical and fail-closed. `POST /api/media/backfill-person-avatars` is now the explicit historical avatar repair entrypoint; it defaults dry-run, reads provider avatar metadata from `serving_projection_members`, and plans `media.asset.cache` commands instead of fetching in the request path. CRM Public Web `signals.materialize` now syncs each materialized signal into canonical `PersonAsset(asset_type='public_web_signal')` + `PersonEvidence` and records `person_asset` / `person_evidence` EntityDeltas. `POST /api/persons/backfill-public-web-signals` is now the explicit historical signal repair entrypoint; it defaults dry-run, reports `normal_reader_repair=false`, and must not be called by projection/person readers. Remaining work is running/applying historical avatar/Public-Web-signal migrations where needed and reader/index preference cleanup.
- [x] **Phase 7d: company asset/evidence/assertion foundation.** Added PG-only `company_assets`, `company_evidence`, and `company_assertions` plus `CompanyAssetWriter`; no SQLite normal-path schema/fallback is allowed. `company_public_web_asset_runs` / `company_public_web_assets` remain the source-specific company Public Web read model, not the owner of official company facts. Model-safe refresh output now materializes canonical `CompanyAsset` / `CompanyEvidence` rows through `CompanyAssetWriter` in PG-only runtime, while SQLite compatibility callers receive a report-visible `postgres_required_for_company_asset_layer` skip. Agent-callable company Public Web refresh now uses `company.public_web.refresh -> company_public_web_owner -> ActivityRun -> ActivityAttempt -> EntityDelta` before writing canonical company facts; retry/cancel/resume controls remain command-owned.

- [x] Add Makefile operator wrappers for the same Phase 5c/7c repair path. `make asset-media-backfill-dry-run` previews through the PG-only script; `make asset-media-backfill-apply` requires `ASSET_MEDIA_BACKFILL_REVIEWED=1` and still passes script-level `--reviewed`. Use these wrappers for historical person/company media/Public Web fact backfill evidence instead of ad hoc script invocations.
