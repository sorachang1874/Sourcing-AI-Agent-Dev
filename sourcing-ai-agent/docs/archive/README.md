# docs/archive

> Status: Archive directory marker. Files here are historical records (session handoffs, closed trackers, incident postmortems, one-shot prompts, superseded plans) moved out of `docs/` on 2026-06-11 during the doc-governance pass, plus rotated `PROGRESS.md` months under `progress/`.

- Do not treat anything in this directory as active guidance; current docs are tiered in `../INDEX.md`.
- Cross-references in archived files may still assume their old `docs/` location; resolve against `docs/` first, then `docs/archive/`.
- Numbers, SHAs, provider strategies, and operational sequences in these files reflect their snapshot date only.
