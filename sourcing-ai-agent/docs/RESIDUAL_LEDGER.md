# Residual Ledger（残差台账）

> Status: Living ledger. 已知失败预算与已接受残差的唯一权威清单（2026-07-09 起取代
> `TRACK_B_REPOSITORY_MIGRATION_HANDBOOK.md` §6 与 `NEXT_TODO.md` 中的两份互相漂移的预算列表，
> 两处现均指向本文件）。评审/审计对残差类 finding 必须引用行 id，而不是每轮重新论证。

## 规则

1. **入账前先归因**：新失败必须先做基线对照实验（**git worktree 基线副本 + `PYTHONPATH=<worktree>/src` 覆盖**，
   勿用 `git stash` —— 会翻共享工作树，见 handbook §6 效率纪律）证明 pre-existing，才能入账。
2. **每行必有 tripwire（到期条件）**：无 tripwire 的接受 = 有纸面记录的遗忘。tripwire 触发时该行必须被修复或重新裁决。
3. **计数类条目只降不增（棘轮）**：计数下降时同批把基线改小；上升 = lane 红，不允许静默增长。
4. **修复后不删行**：状态改 `closed` 留审计痕迹。
5. 评审终止规则见 `INDEPENDENT_REVIEW_BRIEF.md` Output Format：finding 引用本台账行 id 即不阻塞。

## 台账

| id | 范围 | 接受了什么 | 为何接受 | 正确修法 | tripwire（到期条件） | 归因证据 | 日期 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | `test_results_api` ×1 | `lovable_board_visible_patches` 看板分子计数失败 | owner 分子契约当时未裁决（见 handbook §7 决策卡 D-3） | owner 已批准修订 (a)：exact membership 独立拥有 `N/N`，exact card readiness 拥有 `C/N`；Lovable projection extension 与四公共端点改守同一 revision 下 `140/140` 主同步和 `115/140` card detail | 若主同步再次取 card/profile/patch/loaded-row 分子，或同 revision 四端点不一致则重开 | D-3 results 最终全量 **314 passed / 2 failed / 4 skipped**，本行原失败已转绿；新增 Lovable exact membership/readiness 四端点回归。剩余 2 个 entity-delta collision 均在 clean `5f14ed8` 精确同败，与本行无关 | 2026-07-13 | closed |
| R-002 | `test_settings` ×1 | settings runtime-override 命名断言失败 | pre-handoff 命名漂移，无可引用契约 | 契约化 runtime-override 命名后修 | settings/config 契约化工作启动时 | stash 复证 | 2026-06-21 | accepted |
| R-003 | `test_snapshot_materialization_backfill` ×1 | snapshot idempotent 0!=1 | backfill 探针读 legacy 表（对抗校验降级为 CONTRACT_DRIFT/low） | 探针改读 canonical 表 | 触碰 snapshot backfill 域的下一批 | wf_48a95eaf 对抗校验 | 2026-06-21 | accepted |
| R-004 | `test_results_api` ×2 | crm sync-export 契约漂移（`crm_public_web_promotions_survive_force_refresh`、`crm_public_web_service_e2e`） | C1.4 已把导出异步化，两测试仍断言退役的同步契约 | 转 submit→drain→poll（同已迁移的兄弟测试） | **C1.4 submit→drain→poll 转换落地即修，不许再增** | 批次 3 stash 复证 | 2026-06-21 | accepted |
| R-005 | `test_hosted_workflow_smoke` asset_population 组 | default_results_mode None 失败组 | pre-existing，与 Track B 无关 | 补 default_results_mode 解析或更新期望 | 触碰 hosted workflow smoke 的下一批 | ②.0 stash 复证 | 2026-07-02 | accepted |
| R-006 | `test_workflow_explain` ×1 | legacy-standard-bundle full-coverage-proof | pre-handoff uncommitted worktree state 遗留（2026-06-11 归因） | 按 explain 契约重写断言 | workflow explain 契约工作启动时 | ②.0 stash 复证 | 2026-06-11 | accepted |
| R-007 | `test_results_api` ×3 | 看板合并计数桶族（112/297 vs 186/297、115/140 vs 140/140、population floor 80≠297 的余量） | pre-handoff 投影/看板多真源漂移，当时无已提交契约 | D-3 修订 (a) 已把 owner 拆为 exact membership `N`、card readiness `C`、profile readiness `P`、explicit capture evidence；legacy summary/patch/lifecycle 不再 max/floor 合并 | 任一 public reader 恢复 baseline+delta floor、较大计数胜出或 readiness 互推时重开 | 原 3 个预算项在 results 最终全量 **314 passed / 2 failed / 4 skipped** 中均已转绿；新增上调/下调/归零、`C>P`/`P>C`、缺失或不等 membership revision fail-closed 回归。剩余 2 个失败均为 clean `5f14ed8` 同败的 entity-delta collision | 2026-07-13 | closed |
| R-008 | `test_control_plane_live_postgres` ×1 | `test_serving_projection_foundation_uses_live_postgres_tables`（2026-06-11 入账） | pre-handoff worktree state | —— | —— | 2026-07-09 套件 56/56 绿，条目已失效 | 2026-07-09 | closed |
| R-009 | `test_pipeline`（非 lane，全量 457） | postgres_only 下 ~15+ pre-existing 失败（含 ~33 个直写已删影子的 AttributeError） | SQLite 时代套件，永不全量跑（>90 分钟）；②.4d 又确认 refill reader 先全局截断 `limit*3`、再按 `source_job` 过滤，规模增大后会漏掉目标行；②.4e 精确组另命中已退役 `_lock/_connection` 白盒缝隙 | test_pipeline 单独重设计（NEXT_TODO Track B 收尾项）；refill reader 把 owner/source 过滤下推 SQL 后再 limit；白盒测试改走 PG fixture/公开 repository contract，不恢复 SQLite 影子 | test_pipeline 重设计或 refill reader 查询语义启动时；重设计前任何人不得把其失败归因到当前改动 | ②.0/②.1 多次 worktree 子集对照；②.2 精确 5 项在迁移树/`c19c0fd` 均 3 pass/2 fail；②.4d 允许节点在迁移树/`889848e` 均 `0 != 1`，共享 PG 1,656 行、目标 rank 1,650、全局窗口 1,500；②.4e 四节点 current/`dbb40f3` 均 3 pass/1 fail，唯一 `test_get_job_progress_triggers_auto_recovery_when_runner_is_not_alive` 在 `tests/test_pipeline.py:9782` 因 `ControlPlaneStore._lock` 不存在而同败 | 2026-07-13 | accepted |
| R-010 | `test_seed_discovery` ×5（非 lane 全套件；基线 7→5，棘轮改小 2026-07-22） | 剩余 5 个失败**重新归因**：非 ordinal 字面量——fixture 的 `_FakeItemStore` 只实现 `upsert_job_materialization_item`，而 `_record_search_seed_discovery_query_item` 现走 `DurableRuntimeWriter`+workflow 机制（fixture 接口漂移）。原 7 个中：`test_harvest_connectors` ×1 已自愈（132/132 绿）；2 个经 2026-07-22 字面量→`_search_seed_worker_key` helper 派生改写转绿（3146/3432/3478 三位点不再有 `::01` 魔数） | 全套件非 lane，注册路径 durable 化时 fake 面未跟进 | 剩余 5 个 fixture 迁移到 PG fixture 或扩展 fake 到 DurableRuntimeWriter 面 | **B1 Step 2（former 统一）批触碰同一注册路径时同批收口** | 2026-07-09 基线 7/7 同败；2026-07-22 复测 5/60（seed_discovery）+0/132（harvest_connectors），re-attribution 见会话记录（原始入账 2026-07-09） | 2026-07-22 | closed (exit evidence 2026-07-22 同日：余 5 个全部迁移到现行 durable 注册合同——真 PG store（pg_backed_control_plane_store per-test schema），断言面从已退役的 job_materialization_items 写面改为 DiscoveryQueryStateRecorded/CompletionProofRecorded workflow 事件（折叠视图镜像旧行合并语义）与 retry-wait 规划的 `linkedin.discovery_query.run` 命令；另两处独立演进同批收口：paid-fallback 测试补 GDM 事故后新增的 former_broad_past_company_only 显式授权、stop_reason 词汇 completed→provider_people_search_primary（lane 来源化）。套件 60/60 绿。) |
| R-011 | lint 门 mypy 段 | **81 条 pre-existing mypy 债务**（candidate_artifacts 1 / orchestrator 58 / public_web_runtime_core 1 / workflow_smoke 21；D-3 重钉基线） | 继承债务，非本期修复范围 | 分模块清偿 | **棘轮：只降不增** —— 每批验收 = 与基线计数逐字相同或更小；下降时同批重钉基线 | ②.1 历史基线 87（1/64/1/21）；D-3 `make typecheck` 降至 **81 errors / 4 files**（1/58/1/21），无新增模块 | 2026-07-13 | accepted |
| R-012 | `test_results_api` flake ×1 | `test_job_result_lifecycle_stage1_event_time_write_before_public_read` 全量跑偶现（PG teardown 竞态） | 隔离跑绿；对照归因 pre-existing flake | teardown join 线程（同 Phase 3b 尾巴） | fixture teardown 修缮批 | 2026-06-12 归因 | 2026-06-12 | accepted（flake，非计数预算） |
| R-013 | Track B ②.2 independent review | 宿主机 Claude Opus/xhigh 与首个 Codex runner 分别只产出 `ConnectionRefused` / timeout，无 findings/verdict；两份无效 artifact 均未算 GO | owner 的 Claude 路径由 `reclaude` 拉起专用容器并连接独立 daemon，宿主机 `claude` 不走该 transport；owner 2026-07-10 改为接受独立只读 Codex reviewer session，同家族不再禁用 | 已由独立非作者 Codex session 对 pinned `c19c0fd..d0828e7` 完成对抗评审；低优先级永久 fault-test 建议留待下次触碰 manual_review 域 | —— | 无效：`20260710T021502Z_async-reference-track-b-2-2-manual-review.md`、`20260710T032015Z_async-reference-track-b-2-2-codex.md`；有效 GO：`runtime/reviews/20260710_async-reference-track-b-2-2-codex-subagent.md`（AST 14/14、fault 8/8、pipeline base/head 同 3/2） | 2026-07-10 | closed |
| R-014 | Track B ②.3b independent review | pinned scope `b3818f0..aaf13fb` 尚无 runner-verifiable GO；首轮只产 chat GO 未落 artifact，后续含一次 capacity / 两次 interrupted，2026-07-10 hardened-runner 重试又在 900 秒无输出超时并产 invalid artifact | telemetry 的 300 分钟窗口为 `used_percent=0.0`、`rate_limit_reached_type=null`，不支持 5h quota 耗尽；实际是 artifact 交付/会话收口失败叠加一次容量事件与一次 runner timeout，不是 repo/provider-live 失败 | Codex CLI/backend 能产完整输出且 rollout 持久化 effective tier 时，由独立非作者 session 重跑，artifact 必须含 `reviewer_exit_code=0` + hash-bound effective-config/rollout evidence + findings + GO/NO-GO | 异步重试；有效 GO 前只阻断 ②.3b 的 live/W6/manual/里程碑签收，其他开发继续 | author evidence：A/B 3+9、pinned hash `74cc0b13...d0f00`、direct 21+2+7、lane 190/0、mypy 87/4；invalid timeout=`runtime/reviews/20260710T090756Z_Track_B_2_3b_projection_manifest_shards_pinned_review.md` | 2026-07-10 | pending async Codex review |
| R-015 | Track B ②.3c fixed-forward independent review | pinned scope `40bdcf8..508799e` 尚无 runner-verifiable GO | 2026-07-10 canonical network preflight 证实 shell/git proxy 均未设置、DNS 为真实地址且 Clash GLOBAL 非 DIRECT，但 GitHub/API/raw/ChatGPT HTTPS 全超时，`gh auth status` 与 `git ls-remote` 同败；按 workspace 网络规则不得注入 proxy env、改 git proxy 或切换 Clash/TUN | reviewer transport 恢复后，用 operator 全局配置中的 newest model + highest effort 运行 canonical pinned runner；artifact 必须满足 v3 scope/effective-config/rollout 校验并给出 GO/NO-GO | 异步重试；有效 GO 前只阻断 ②.3c fixed-forward 的 live/W6/manual/里程碑签收，②.3d 与无关开发继续 | author evidence：projection/writer/surface 105+4 subtests、person/fault/API/legacy 30、operation 93、lane 255+2+11+1+2、mypy 87/4；commit=`508799e` | 2026-07-10 | pending async Codex review |
| R-016 | CRM Public Web `gpt-5.6-sol` product-model contract independent review | pinned scope `508799e..47ec9d0` 尚无 runner-verifiable GO | 与 R-015 同一次只读 preflight 的 HTTPS/backend 不可达；未启动无望完成的 reviewer，也未把作者/测试结果当 GO | transport 恢复后对该独立 scope 运行 canonical pinned runner，重点审查 pre-transport model lock、Qwen fail-closed、W7g exact provider-response identity 与全停重启合同 | 异步重试；有效 GO 前阻断该模型合同的 live W7g、manual/product 与里程碑签收，不阻断 simulated 开发或其他模块 | author evidence：model/provider/runtime-boundary 188+11 subtests、lane 255+2+11+1+2、dry_run_ready、mypy 87/4；commit=`47ec9d0`；没有 relay 接受性/价格/质量 live 证明 | 2026-07-10 | pending async Codex review |
| R-017 | Track B ②.3d projection person search index independent review | pinned `ea03da3..7b49f63` 独立 Codex review 已给 **NO-GO**：mixed missing filter record 假 ready、job count 覆盖、paged build 无三键 fence、public reason 泄漏；fixed-forward 对抗审计又发现 facet/readiness 未绑定 build，均已在 `93d9f9e` 修复，待 pinned re-review | findings 触及公开 readiness/count 与 stale index/facet publication，作者复验不能替代 GO；owner 明确该 scope 不阻断无关开发 | 对 `93d9f9e` 的 projection-index/facet 路径做 scope-local re-review：整页 fail-closed、三键写读围栏、completed 防降级、产品绑定/失效、semantic no-op 保留与 command identity | 有效 re-review GO 前只阻断本 scope 的 live/W6/manual/里程碑签收，不阻断 ②.4 后续批 | prior artifact=`runtime/reviews/20260710_async-reference-track-b-2-3d-codex-subagent.md`；remediation=`93d9f9e`；projection/storage 122+7 subtests、live-PG 61+4、no-op identity 1、lane 279+2+11+1+2、lint 49、mypy 87/4 | 2026-07-13 | pending fixed-forward re-review |
| R-018 | Track B ②.4a operation-control independent review | Repository cutover、CAS、reject/cancel UoW、HTTP conflict 及 retry chain 尚无 pinned 独立 verdict；fixed-forward 对抗审计发现 terminal exact-child replay 的确定性 action 重排与 cross-workspace public control_state 漂移，均已在 `93d9f9e` 修复 | owner 允许 Codex-only 异步评审；作者 A/B、真实 PG 与 lane 结果不替代独立 reviewer | 对 `93d9f9e` 的 operation-control 路径启动 scope-local Codex review，核对 facade/dispatch 清零、UoW rollback/idempotency/identity、missing/cross-workspace fail-closed、stale ancestor/parent-bound/exact-child retry 及 R-019 边界 | 有效 GO 前只阻断 ②.4a 的 live/W6/manual/里程碑签收；②.4b 与其他开发继续 | implementation=`93d9f9e`；A/B 11/11、pinned hash `bbcc134a...971d`、operation 109、UoW/CAS 40、retry/control 10、新增真实 PG 3、lane 279+2+11+1+2、lint 49、mypy 87/4 | 2026-07-13 | pending async Codex review |
| R-019 | workflow_runtime operation state-sync / dispatch atomicity | 24 个生产 `update_action_state` / `update_operation_state` 调用仍未统一为锁内 JSON merge 与 operation+action+event/command UoW；command-plan 可在 cancel 竞态中留下可执行 command，retry reservation 与 child/events 可部分完成。②.4c 后仍无 parent-command generation/lease-token fence，stale owner 可首次插入 phantom child/ActivityAttempt，linked OperationRun/AgentAction sync 仍在多数 completion 路径的 UoW 外。②.4d 的 `DurableRuntimeWriter` 仍按 event → commands → outbox → state 多次提交；相同 event checkpoint 的并发 reducer 会在 state lock 前各自读取 commands，counts 可 last-writer-wins | ②.4a/②.4c 只闭合有界 reject/cancel 与既有 acquisition rows；②.4d 只把单表 state merge/sequence 锁定并 fail-closed。D-3 新增 stale-input fixed UoW，并修复 pool-max=1 下 dispatch 持 session lock、publisher/cancel 持 pooled connection 等 xact lock 的循环等待；pool contender 现用 try-lock，busy 时 rollback/归还连接后退避，但 helper 仍无总 acquisition deadline，永久锁持有者可让 caller 持续重试。cancel 仅在已提交且仍非终态的 status advance 上有一次有界重试；这些修复仍不能把可重试协调器宣称为四表事务，也不取消已计划 command。D3b 仅锁定 future contract，零产品码/零 migration，不改变本残差状态 | 按 `TRACK_D_D3B_WORKFLOW_COMMAND_CLAIM_FENCE_CONTRACT.md`：review session 必须先创建；canonical `coordination_plan_review_id=plan_review_sessions.review_id` 为 positive `BIGINT`，brownfield `NULL`、strict `>0`、禁止 `TEXT`/empty，并由 strict OperationRun pin、command exact-copy、authority/receipt bind，全链 FK/key 用同一 numeric encoding。poll/action strict-D3 的唯一 scope/coordination issuer 是 scoped review-session repository，但 bootstrap 不得调用尚无 positive review/gate 的 normal fence。source session-create command 独立使用 `scoped_session_bootstrap_v1` context；exact-selection private factory 铸 one-use `ScopedReviewSessionBootstrapAuthority`，Stage A claim commit 后返回 pre-session current-claim `ScopedReviewSessionBootstrapReceipt`（不含 ActivityRun/ActivityAttempt/review/event/result），factory+verifier 以 `scoped-session-bootstrap-lock-v1(scope tuple, creation_idempotency_key)` 序列化（key 不含 review id/gate），exact-verify source generation/epoch/post-claim command attempt/lease、authenticated workspace、immutable source event 与 typed plan pins；create variant 以 `(scope_digest, creation_idempotency_key)` unique 在 specialized bootstrap Stage-B one-PG-UoW 创建 claim-bound ActivityRun/ActivityAttempt + session/event/source terminal。Stage A 后未 commit 的 crash 须等 lease expiry/reselection；commit 后 credential-free `ScopedSessionCommittedReplayContext` 只读 exact-compare，禁止重建 authority/receipt/token 或写入；stale/cancelled/cross-tenant/collision 零写且 post-commit replay 返回同一 review id。只有 session commit 返回独立 `ScopedReviewSessionCreateResult` 后 OperationRun 才 lock scoped session、exact-copy positive review scope/coordination，并进入 normal `d3_v1` authority/business/coordination fence；legacy unscoped/JSON-scan creator 不可 seed strict D3。action-backed root 另受 Plan §6#6 action-root durable-scope gate（无 OB-ID、非 OB-10.4）阻断；`workflow_commands` 复用 existing `operation_id` 且全链 exact-copy scope，不新增 alias；strict additive columns 精确 20（prior 16 + 四项 `workflow_commands` typed nullable predecessor columns；all-null initial/complete successor，禁止 JSON/half-null）。canonical factory 只在 exact-command selection 后单次 mint one-use ClaimAuthority；strict registry digest pins `claim_fence_policy=d3_v1` + `allowed_stage_ids`/stage policy；Migration-C guards 使用 registry-generated hash-bound `scoped_session_bootstrap_command_types_v1` 与 `strict_d3_command_types_v1` + immutable command type，分别约束 bootstrap pre-review/terminal 与 normal positive-review D3 shape，digest sentinel 不可充当 discriminator；`expected_stage_id` 只来自 exact selected row；authority 同时绑定 coordination/business pins、selection generation、repository-time expiry、pre-claim command attempt 与 current-selection one-use consumed slot（reselection generation+1 后 clear；该 slot 不承担/不宣称 durable historical audit SOT）；selection UoW 先证明旧 execution lease/selection reservation absent/expired，再持久化 exact unexpired reservation，Stage A exact-match 并以 execution lease 覆盖它（不要求 absent/expired）+ atomic consumed id + command attempt `+1`，ClaimIdentity/ActivityAttempt exact-copy post-claim attempt。完整 29-wrapper inventory 仅作 observation/zero-unauthorized-increase denominator；scope-local deletion 只覆盖双 fenced manifests，任何 `legacy_unfenced` entry/caller 尚存时 generic bridge 明确保留；fenced wrapper 禁止启动期铸造/standing capability。Stage B/async supersession 共享四项 typed physical predecessor CAS；initial tuple all-null 且证明无 current phase，successor tuple complete，half-null fail closed；只有 typed `final_adjudication` 计划唯一 record command；source binding 分 immutable seven-field core（含 `source_command_attempt`）+ append-once terminal tuple，record 有 independent current claim，并在同一 record UoW 内 fixed-order 锁定 source + record command，exact-compare current source operation/scope/generation/epoch/post-claim command attempt/status/event/digest；exact outcome map 为 authorizable→verification shadow_would_verify/intent applied、awaiting_budget→pending/awaiting_budget、needs_human→needs_human/applied、failed→failed/applied、timed_out record outcome→timed_out/applied，每项发 typed `company_identity_verification_recorded` discriminant event；fallback/policy-invalid→needs_human，nonretryable execution→failed，control timeout 单独写 intent timed_out + verification needs_human + control event。awaiting resume 走 post-gate-apply convergence：record 只写 awaiting intent+recorded_event 并 terminalize；gate owner 在推进 exact watermark 前 reserve/lock resume-v2，gate-event/grant owner 共用 `maybe_plan_resume` exact-replay/reawaken；identity 含 recorded_event_id，不含 grant/delivery。`not_applied(reason=stale_claim|business_precondition_conflict)` 是唯一 stale/business 零写返回，且不是 event/state。human/recompile/所有 cancel、D3 OperationRun terminal/cancel/retry/requeue/resume/reset/rebuild/recovery 与 dispatch-invalidating control 共用 `d3-dispatch-v2` monotonic-budget coordination lock，key=immutable scope tuple + exact coordination id，root intent 不参与；Stage B/async supersession/record/terminal/control/dispatch/resume_after_grant 统一按 operation root → optional plan/review/gate → all participating workflow_commands（source/record/resume/supersession/current owner/idempotency target，确定序）→ intent/predecessor → ActivityRun/Attempt → optional grant/cost；进入 intent 后不得回头 insert/lock command，未触及 aggregate 只可跳过、不可逆序，并经 closed typed context union 的 `d3_business_fence_v1` predicate 复查：claimed-command stage_b/terminal/record/dispatch/resume_after_grant 带 authority+receipt，aggregate control 带 registered control authority/expected revisions 且无 claim token；六 phase mandatory、wrong context fail closed；仅 heartbeat/read-only exact replay 窄例外；只有 invalidating control/input/requeue 接受态推进 epoch `+1`，result-terminal 不加 epoch但清 token/lease并写 terminal pair；control-first/stale/business mismatch 的 domain/attempt/intent/event/command/source/result 零写，dispatch-first response 才进 shared quarantine repository；immutable identity/digests insert-once，cost/retention typed CAS entrypoints 写集分离且 monotonic，`cost_state: pending_reconciliation -> reconciled_confirmed | reconciled_uncertain | reconciled_no_call` 与 `retention_state: retained -> purged_tombstone` 正交、domain 零应用且禁止 transaction 跨网络。record apply 是 terminal-UoW specialization：同一 PG transaction 执行 record+terminal predicates并写 verification/intent、recorded domain event、workflow terminal event、attempt+command pair；crash 全回滚/exact replay 全 aggregate。terminal policy registry 与 transport evidence receipts 分属两个 durable owner row。checked-in `TERMINAL_PROVENANCE_SPECS` / closed `TerminalProvenanceSpec` union 是唯一 semantic SOT，精确包含 `TransportResponseSpec|TransportAttemptFailureSpec|NoExposureTerminalSpec`；每个 `(command_type, stage_id, terminal_transport_variant, terminal_status, terminal_event_type, terminal_outcome, terminal_reason if exposure, failure_code if attempt_failure)` 必须唯一 applicable。`terminal_provenance_policy_digest` 在 command creation 固定、在 exposure creation exact-copy；historical spec/manifest digest 在仍有 active/retained command、exposure、response/failure receipt、terminal event、source-intent tuple、quarantine/tombstone 或 cost/audit 引用时不得删除或 reinterpret。transport response terminal exact-bind committed exposure/physical-call/provider-call/`ModelInvocationEnvelope`/stable occurrence + canonical response/result digest + result artifact pair；合法 `length|content_filter` terminal envelope 是 response，只有 incomplete/truncated wire 或 protocol-parse failure 才写 `TransportAttemptFailureReceipt`。failure receipt exact-pin registered code/spec/digest/artifact、claim/business、wire-call、retry revision/disposition；retryable 只可 `retry_wait` 且 terminal/source tuple 零写。response/failure creator 与 exposure-backed terminal 共锁同一 exposure：response-first 拒 failure terminal；terminal-disposition failure-first 后 valid response 仅入 quarantine；retryable-failure→`retry_wait` 后 late valid response 也仅留 response receipt/quarantine，不可恢复 apply。registered non-transport 或 proven pre-call/no-send terminal 仅由 `NoExposureTerminalSpec` 授权 complete-none `no_exposure`，并在 common `d3-dispatch-v2` coordination lock 下证明 dispatch/exposure absence，绝不锁不存在的 exposure row。provider delivery id 或 durable inbound `TransportResponseReceipt` get-or-create stable occurrence，redelivery 复用，`late-response-v1:<scope_digest>:<dispatch_exposure_id>:<canonical_delivery_identity>` collision fail-closed；post-network 固定 exposure→receipt→quarantine/cost-axis order，绝不回到 operation/command/intent/domain，也不授权 send/apply。strict-D3 terminal command+event 同 UoW 以 local both-null/both-non-null CHECK + `MATCH SIMPLE DEFERRABLE` composite FK 存 outcome digest/event id；pair 在 result-terminal 期间 immutable，仅 registered reopen 可清；future native-PG DDL 证明 null/half-null/exact/mismatch/non-orphan，exact replay 零写、mismatch fail-closed；heartbeat 按 occurrence identity。CAS miss 不写 action/event/command/domain，retry reservation/child/events 同事务；把 linked Operation sync 纳入同一因果边界，并为 transaction-lock caller 增加 typed busy 与永久持锁回归 | **下一次触碰 operation retry/dispatch/command completion、workflow commands 分子批、任何新增 `_connect_with_transaction_lock` caller，或任何 live/W6/manual operation/acquisition-control 签收前必须修；调用点不得新增（当前 24 为上限；D-3 的 26 为历史 checkpoint）**。2026-07-14 owner 指示按既定 Track D plan 继续、不由 pending review 阻断；据此对 D1c 的 pre-write request-pin validation/copy/verify 记录一次有界例外。随后 invalid review 提供直接新证据，fixed-forward 仅额外允许 schema-less continuation 在首次 domain mutation/handler 前 append 一条 epoch-scoped、幂等 compatibility evidence event；不得新增 action/run/command state mutation 或 transaction-lock caller；该 D1c 时点的 26-call ratchet 保留为历史 checkpoint。D1m 随后把 final source Command/Activity/Attempt closure 收进 specialized UoW，current mechanical direct state-sync inventory 降为 **24**；该 UoW 保留 fully exact prior terminal Attempt；owner-specific resume control 仅在 succeeded、deterministic id/key、generation<=current、exact workspace/activity/workflow/command/provider/request-ref/lease + target/company/boolean-force/nonblank-output-reason 语义成立时保留；exact current failed partial 只在 `error.reason=metadata.owner_lost_reason=output.reason` nonblank、`output.status=skipped`、`error.owner_lost=true`、`error.deterministic_terminal_failure=false` 时视为可收敛。此有界 reduction 不解除本行对任何其他 retry/dispatch 改动或签收的阻断。D3b decision-only 可继续；D3c1 baseline 已把 public command 收敛为 42-field closed projection、9-field typed operation sync、7 schema refs、7 raw bypass→0 与 16 routes/17 method-route variants；D3c1a current candidate 进一步封闭 `23/27/26/10` Activity/control-target shapes、normalized/compact-prefix private aliases、generic `execution_summary` forging 与 safe-integer diagnostics，primary manifest 为 23 method-route variants且 compact materialization 独立计数；final author evidence 已记录，fresh pinned review 仍 pending。D3c2a 仅安装 `workflow_commands` 20-column/16-`NOT VALID` dormant command subbatch，33-column descriptor、runtime writers 与 claim/CAS 均未激活，完整 Migration A 仍 open；D3c1/D3c1a/D3c2a 都不得宣称 R-019/claim fence remediation。Plan §6#7 的 26×10 owner matrix 已将 terminal policy registry 与 transport terminal receipts 分行，仍仅 decision-shape complete，physical owner/repository/runtime 与其余 migration 仍 NOT IMPLEMENTED；rollout 必须先落完整 migration、bootstrap/strict-D3 双 population manifests、registry/policy pins 与 bootstrap authority factory+verifier，再启 `scoped_session_bootstrap_v1` specialized Stage-B session UoW + credential-free committed replay，session commit 后才允许 exact-copy OperationRun 进入 normal positive-review `d3_v1` fence，并在三 variant/race acceptance 后才启 non-live strict path；后续 migration/mint/CAS 必须按前述 contract 实施且不得把文档当 evidence | D-3 stale-input transition 的 apply/exact replay/event-failure rollback、完整 operation runtime **129 passed**、静态 ratchet **26**；真实 PG pool max=1 的有限竞争 dispatch/publication 与 dispatch/cancel 线程均有界退出且单 command/event，但未证明永久持锁 deadline，且 cancel race 后仍有 1 个已计划 command。②.4d 也未证明 event/commands/outbox/state 原子性或并发 command-count coherence。D1c fixed-forward 证明 observation 失败时 approve/retry state/run/events 全快照不变，且真实 old-row submit/approve/retry/dispatch 路径留证；它仍未闭合任一 UoW、generation/lease 或 lock-budget 缺口。D3b 作者测试只证明合同、13/29/29/39/71/15/3/62/34/26 counter 与 projection debt 被机械冻结；scope issuer、authority mint、CAS、race、dispatch、terminal/quarantine 与 PG migration 运行证据仍为零；第三次 fresh local advisory `0/5/4/0` 的 9 findings（此前 `0/8/2/0`、`0/8/3/0`）、round4 fresh advisory `0/3/2/0` 与 parallel semantic audit `0/5/4/0`、round5 semantic `0/5/2/0` 与 fresh broad `0/3/3/0`、round6 semantic `0/1/2/0` 与 broad `0/4/0/0` 、round7 semantic `0/4/0/0` 与 broad `0/4/2/0`、round8 semantic `0/1/0/0` 与 broad `0/6/1/0` 均为 NO-GO fixed-forward 输入；两次 round9 local advisory 因 Codex operator usage limit 在 final response/artifact 前终止，故没有 verdict；direct partial findings 已 fixed-forward。current D3b author `32/49/58/81` + diff clean。D3c1 首个 pinned attempt substantive=`0/2/3/0`，但 causal-binding artifact invalid；五项 direct findings 已由 D3c1a current candidate fixed-forward。D3c1a final author evidence 已记录，fresh pinned non-author review 仍 pending，当前没有 formal verdict。D3c2a author=migration/PG `9 passed + 20 subtests`、D3a+D3b+D3c1+durable adjacency `51`、lint `58 files`、mypy `81/4`、diff clean；fresh pinned review pending，且仅对 Live/signoff fail closed，served=0、R-019/action-root/OB gates 仍 open | 2026-07-15 | pending remediation |
| R-020 | acquisition owner-specific cancel cross-table atomicity | `acquisition.plan.commit`、`acquisition.scale.plan`、`linkedin.profile_fetch.activity.run` 原先先写 run/lane/activity，再 CAS workflow command；冲突、异常或进程中断可留下 `module_state_mutated=true` 的部分提交 | ②.4b 只具备单行 identity/terminal 保护，跨表 cancel 必须等 activity spine 分子批把相关 facade 与 owner call sites 同批收口 | ②.4c 已落固定 allowlist UoW `cancel_acquisition_owner_command`：command → run → activity/lane → attempt/delta/downstream 同事务锁定、验证 lease/identity/effect/terminal winner，再一次更新 module rows 与 command；返回 `applied/repaired/already_applied/blocked/conflict/not_found` committed outcome，exact replay 不改时间戳，legacy partial 可 repair，异常整笔 rollback | 若新增 acquisition owner cancel 绕过该 UoW、handler 在事务外复制 module write，或把 `blocked/conflict/not_found` 报成成功则立即重开；提交后 phantom child/attempt ownership fence 与 linked Operation sync 不属于本行，继续由 R-019 阻断签收 | A/B 3→mutation 1 fail/2 pass→restore 3，pinned hash `7fc2308e...e44f`；真实 PG R-020+identity battery 12，handler/spec 25，operation runtime 127；old receiver 299→0。故既有行+command partial-commit window closed，但未声称 R-019 barrier | 2026-07-13 | closed |
| R-021 | Track B ②.4b acquisition-control independent review | pinned scope `a1c2a99..d6e1e2a` 尚无 runner-verifiable GO；作者 lane、子代理静态审计与 fixed-forward 不代替独立 artifact | owner 允许 Codex-only 异步评审；先前 canonical read-only network preflight 已证明 reviewer backend/GitHub transport 不可达，本批未绕过运行器或伪造 verdict | transport 恢复后，由独立非作者只读 Codex session 使用 canonical runner 和 operator 配置的 newest model / highest effort 评审 facade/mapper/key 清零、双身份锁、JSON object merge/list replace、terminal 集、profile retry-exhaustion 修复与 R-020 边界 | 有效 GO 前只阻断 ②.4b 的 live/W6/manual/里程碑签收；②.4c 与其他开发继续 | implementation=`d6e1e2a`；A/B 6/6，mutation 1/5，pinned hash `8d795a0d...5865`，精确 PG 6，operation 115，lane 289+2+11+1+2，`dry_run_ready`，lint 53，mypy 87/4；静态扫描旧 receiver 53→0，对抗审计 P1 1→0 | 2026-07-13 | pending async Codex review |
| R-022 | Track B ②.4c activity-spine / R-020 fixed-forward independent review | pinned scope `af50f45..30a703e` 尚无 runner-verifiable GO；作者 lane、并发子代理审计与测试结果不代替独立 artifact | owner 允许 Codex-only 异步评审且明确评审不得阻断无关开发；review request 已固定到 implementation commit，live provider 因 Apify 凭据轮换另行不可用 | reviewer transport 可用时，由独立非作者只读 Codex session 使用 canonical runner 和 operator 配置的 newest model / highest effort，核对 9 facade + 3 mapper 退役、五表 identity/terminal/write-once 原语、三条 cancel UoW 的 rollback/replay/identity/effect fence，以及 R-019 phantom/post-sync 边界 | 有效 GO 前只阻断 ②.4c 的 live/W6/manual/里程碑签收；read-model trio 与其他非 live 开发继续 | implementation=`30a703e`；A/B 3/3，mutation 1/2，pinned hash `7fc2308e...e44f`，R-020+identity 12，operation 127，lane 305+2+11+1+2，`dry_run_ready`，lint 57，mypy 87/4；旧 receiver 299→0、positional bulk-write 0。R-022 登记前 canonical preflight：shell/git proxy unset、真实 DNS、Clash GLOBAL 非 DIRECT，但 GitHub/API/raw/ChatGPT HTTPS 全超时，`gh auth status`/`git ls-remote` 同败；未改网络、未启动 reviewer、无 artifact | 2026-07-13 | pending async Codex review |
| R-023 | runtime_outbox future dispatch lease ABA | ②.4d 对 `claimed`/`running` 只要求匹配 `lease_owner` 字符串；没有 attempt/generation/claim token，owner 名复用时旧 worker 可能误 dispatch 新一轮 claim | 当前没有 production outbox claim/consumer，本批只收紧现有 dispatch 边界；凭 owner 名无法证明 claim 世代 | claim 时生成不可复用 fenced token/generation，dispatch `UPDATE` predicate 同时绑定 row id、owner、token/generation 与未 dispatched 状态，并以真实 consumer E2E 验证 stale claimant 被拒绝 | **Track C 5d、任何 production outbox claim/consumer 落地，或 live/W6/manual outbox 签收前必须修** | ②.4d 已测试 owner mismatch fail-closed、exact replay timestamp 不变；未测试且未声称跨 claim generation 的端到端 delivery fencing | 2026-07-13 | pending remediation |
| R-024 | `test_results_api` ×1 | run-scope projection 已有 1 个 member，但 `counts.public_facet_counts` 缺 `candidate_count` | pre-existing projection finalize/public-facet owner 漂移；pinned 基线证明非 ②.4d 回归 | D-3 已明确 public facets 只由 revision-fenced `projection_person_search_index` 拥有：initial membership publication 写 `pending/unavailable`，index finalization 才发布 exact facet counts；读端缺失/mismatch fail closed | membership publication 再内联伪造 facets、成员扫描成为 public facet fallback，或 index product 未绑定 membership revision 时重开 | 实现/测试修正后该精确节点 targeted 通过，并覆盖 initial pending → index completed exact transition；最终 results **314 passed / 2 failed / 4 skipped**，本节点不在剩余两处 clean-baseline 同败中 | 2026-07-13 | closed |
| R-025 | Track B ②.4d workflow runtime read-model trio independent review | pinned scope `889848e..7048d83` 仍无 runner-verifiable GO；首轮 executable discovery 与第二轮 Codex 0.144 transcript 校验均为 `invalid_transport`，不能当 GO/NO-GO | 第二轮模型的 root/child turns 均 completed 且无 model error，但 Codex 0.144 改为并行 child threads，`turn/completed` 不再内联 items；旧 runner 的单-thread/exact-inline 校验 fail closed。提取出的 substantive 文本只作修复参考，不是 formal verdict | runner carrier `dc5af51` 已适配多 thread / non-inline-items，同时保持 root exclusivity、turn identity、唯一 durable final、hash-bound rollout/verifier parity。operator 顶层 `model_reasoning_effort` 当前仍为 `medium`；改为最高支持档后，由独立非作者 session 正式重发原 pinned scope | 有效 GO 前只阻断 ②.4d 的 live/W6/manual/里程碑签收；session/trace 与其他非 live 开发继续 | implementation=`7048d83`；runner fix=`dc5af51`（runner 55、retention/verifier 50、pre-agent 60，独立 runner audit GO）。invalid artifacts=`runtime/reviews/20260713T104113Z_Track_B_2_4d_workflow_runtime_read-model_trio.md`、`runtime/reviews/20260713_track_b_2_4d_read_models_rerun.md`；后者明确 `reviewer_exit_code=invalid_transport`。②.4d author evidence 仍为 A/B 3、mutation 1/2、lane 314+2+11+1+2、mypy 87/4 | 2026-07-13 | pending async Codex review |
| R-026 | Track B ②.4e workflow recovery intents independent review | implementation `f09ffbd` 的 Codex 0.144 review 同样产出 `invalid_transport`；提取内容仅参考，无正式 verdict | 与 R-025 相同的旧 runner transcript 协议不兼容；作者 A/B、变异、targeted/lane 与模型实际完成 turn 均不能替代有效 artifact | 从 runner carrier `dc5af51` 固定 `REVIEW_BASE=dbb40f3`，`REVIEW_FILES` 仍仅含原 8 个实现/测试文件；operator 顶层 reasoning effort 改为最高支持档后，由独立非作者 session 重发并取得 substantive GO/NO-GO | 有效 GO 前只阻断 ②.4e 的 live/W6/manual/里程碑签收；②.4f 与其他非 live 开发继续 | implementation=`f09ffbd`；base=`dbb40f3`；invalid artifact=`runtime/reviews/20260713_track_b_2_4e_recovery_intents.md`（`reviewer_exit_code=invalid_transport`）；runner fix=`dc5af51`。author evidence：A/B 6,062 bytes/hash `d0359eef...f3f2b3`、mutation `cmp=1`、targeted 21/38/59、live-PG 61+4、lane 321+2+11+1+2、mypy 87/4 | 2026-07-13 | pending async Codex review |
| R-027 | D-3 revised (a) canonical membership/readiness contract independent review | owner-approved implementation is pinned at `82d69a1` but has no valid formal review artifact; author tests and local non-author adversarial audits do not count as the canonical gate | contract changes public counts, readiness, pagination, facets, export/CRM inputs, cache invalidation and zero/downward correction; independent scoped review is mandatory, but must not block unrelated Track C work | use the `dc5af51` canonical runner from an independent read-only Codex session after operator config selects the highest supported reasoning effort. Review owner matrix, opaque revision equality, no fallback/max merge, `N/N` vs `C/N`, independent profile/card/capture, downward/zero correction, facet index ownership/cache invalidation, finite-contention pool-max=1 liveness, bounded cancel retry, and the explicit R-019 no-total-lock-budget/queued-command plus R-028 boundaries | valid GO required before D-3 live/W6/manual/product/milestone signoff; non-live Track C work may continue while formal review is pending | implementation=`82d69a1`; results **314/2/4** with both failures clean-`5f14ed8` identical; writer **24+3 subtests**; adapter **62+4**; operation **129**; storage **59**; PG projection/CRM **58+4**; frontend Python **50** + build; workflow smoke **187/1 baseline-identical +3**; lint **58 files**; mypy **81/4**; contract lane **349+2+11+1+2**, `dry_run_ready`. Apify credentials rotated, so no live validation | 2026-07-13 | pending async Codex review |
| R-028 | projection-to-CRM command completion and legacy identity UoW | D-3 fixed selection atomically writes `crm_records` + `crm_engagements` + `crm_events` under the projection publication and sorted person locks. D1j moves Operation `add_to_crm` onto a schema-defined owner-bound projection selection target and revalidates the exact projection snapshot at submit, dispatch, and command-owner time. C2.7 fixed-forward further moves authenticated `update_crm_record` behind one record-row owner lock + version check + three-table UoW; Public-Web promotion now uses record → schema-scoped promotion-id advisory → promotion-row lock order with immutable exact replay; Stage2/cancel use a typed owner/state job-row CAS. Legacy `add_person_to_crm`, other identity writers, and legacy promotion upserts do not all share those locks, while durable cancel follow-on effects, Activity/entity-delta recording, and command terminal/effect completion remain outside one transaction | the bounded D-3/D1j/C2.7 UoWs close selection revision TOCTOU, Operation add-to-CRM schema-less caller drift, authenticated PATCH partial writes, promotion-id overwrite, and owner/terminal job read→write races, not all CRM mutation or workflow-command cross-table atomicity. A command effect can still interleave with cancel after its pre-effect check, an unfenced legacy writer can race selection/promotion insertion, and `(workspace_id, person_identity_key)` is not yet a unique PG constraint | in the CRM Repository/command-completion batch, move all remaining record/engagement/event and promotion mutations behind shared identity/idempotency locks, clean existing duplicate person rows then add the unique constraint, bind command status/lease generation to the effect boundary or add an explicit committed-effect recovery state, and delete the temporary Store selection facade. Keep R-019 causality/post-sync limits explicit | before any CRM mutation live/manual signoff, the next remaining CRM writer or workflow-command completion change, or any new CRM mutation caller | D-3 author evidence proves missing/stale/duplicate selection zero-write, mid-window A→B recheck, injected event-table rollback, exact replay, and cross-revision reselect；D1j author evidence proves full Operation runtime `137 passed`, add-to-CRM owner-bound target happy path, stale dispatch reselection, forged command target zero CRM write, and D1 action request surface `6 passed`; C2.7 adds permanent isolated-PG owner/state interleavings, cross-workspace/record/signal/action promotion collision tests, exact replay, blank-owner compatibility, and HTTP `409` transport regressions, but independent fixed-forward reviews remain pending where noted | 2026-07-16 | pending remediation |
| R-029 | Track D D1 loose action-request schema bridge | D1f activates `set_crm_stage`, `add_crm_note`, and `create_crm_task`; D1h activates `enrich_person_public_web`; D1i activates `start_acquisition_run`; D1j activates `add_to_crm`; D1k activates `export_candidates`; D1l activates `search_projection` and `filter_projection`; D1m candidate activates `refresh_company_public_web_assets`. The other **5/15** production actions remain schema-less and API-submittable through the compatibility path. Their action/run pins remain the explicit empty/empty pair; brownfield empty-pin rows for the ten activated actions fail closed. Served Agent tool population remains zero. | D1c intentionally landed the shared validator/pin lifecycle before every action owner had explicit semantics. D1f/D1h/D1i/D1j/D1k/D1l/D1m reduce the denominator only for the exact ten actions whose implemented schemas, binders, transport scope, replay identity, and execution preflights are explicit. The other 5 still lack one or more owner/schema decisions; independent review remains a separate served/signoff/deletion gate and does not change the physical denominator. | For every remaining API-submittable action, implement one closed `ActionRequestSpec`, owner-minted `target_ref`, binder, and explicit legacy-input migration/rejection, then obtain the required scope-matched review before serving/signoff. Bump the compatibility-observation epoch for each release window and audit submit replay/approve/retry/dispatch over the full population. Deploy existing-row constraint validation separately. Close only after the complete API-submittable population records zero bridge hits for one release window; served-only evidence is insufficient. | **Before any production action enters the served registry, D1 live/manual/product/milestone signoff, removal of empty/empty or durable hit evidence, validation of the `NOT VALID` checks, or addition of an API-submittable action without a schema/binder decision.** | D1c=`9a0e051`; D1f=`1cb829f`; D1h/D1i evidence remains recorded in their implementation documents and 2026-07-16 ledger annotations. D1j adds an owner-bound projection selection target for Operation `add_to_crm`, closed CRM destination input, submit/dispatch/command-owner projection snapshot revalidation, forged target zero-write coverage, and full Operation runtime `137 passed`. D1k adds an owner-bound projection membership target for Operation `export_candidates`, closed export options input, stale membership fail-closed before command planning, whole-projection compatibility via empty selected-candidate list, targeted D1j/D1k + D1 surface evidence `10 passed`, writer-control regression `6 passed`, full Operation runtime `137 passed`, lint green, and typecheck `81/4`. D1l selects the public reader's closed `shared_canonical_read` classification; adds owner-bound projection-read id+revision targets, exact selector aliases, and strict compact canonical multi-select filters; uses the exact persisted Operation workspace for Operation CRM overlays; authenticated direct/job reads default to the server-derived workspace while explicit `default` remains the pre-auth legacy selector and open mode preserves explicit workspace; acquires Operation dispatch plus projection publication locks against one shared monotonic 5s total acquisition deadline rather than 5s per lock, fails a past deadline before connection acquisition, and holds both through result persistence; commits terminal action+Operation+event writes in one commandless PG UoW; revalidates persisted strict JSON and maps generic changed-during-read/page-read/search-read reasons to stale/reselection with replay-preserved `reselection_required`; routes all four production generic projection-field patch callers through publication-key + native row-lock merge while rejecting the three search-index binding keys; maps missing/non-shared/unprovable submit targets to zero-write HTTP 404, dispatch readiness plus `operation_dispatch_lock_busy` / `projection_publication_lock_busy` to exact-reason HTTP 409, and completed reads to HTTP 200. D1l final stable-tree author evidence is `5 passed + 9 subtests` adversarial, `50 + 87 subtests` API/writer, `133 + 176 subtests` D1 contract, Operation runtime `139`, lint `58 files`, and mypy `81/4`; its dirty-tree non-author re-audit was scope-local advisory `GO 0/0/0/0`, not pinned or formal. Epoch `d1f_r029_20260715_v2` remains current because these batches do not start a release window. `0002` still uses a 5s lock budget + `NOT VALID`; existing-row validation remains separate. D1i fresh pinned review timed out. Combined D1j/D1k pinned `354e979` runner-backed advisory=`NO-GO 0/9/7/1`; those findings require fixed-forward/re-review. D1l pinned review artifact reports `NO-GO 0/4/7/0`; its fixed-forward and fresh re-review remain pending. D1m adds deterministic seed-only canonical input, alias-resolved owner target `workspace_id+company_key`, authenticated/open transport, exact persisted/dispatch/root/source/materialize revalidation, one normalized source-run owner with current-attempt/lease fencing, digest-bound immutable materialization snapshots, and bounded root/source/materialize expired-`claimed` recovery. Guarded Activity start and DB-clock-authoritative exhausted closure lock deterministic ActivityRun/all-through-current ActivityAttempt identities and full-validate the immutable spine. Successful takeover closes superseded prior running execution attempts. Guarded current/future terminal execution Activity/Attempt rows, split/alternate nonterminal rows, and future/malformed resume evidence fail closed, while fully exact prior terminal execution evidence may remain. A succeeded owner-specific resume terminal may coexist only with deterministic resume id/key, generation no greater than current, and full workspace/activity/workflow/command/provider/request-ref/lease plus target/company/boolean-force/nonblank-output-reason semantics. Exhausted closure retains a valid resume-control Attempt and converges an exact current failed owner-loss partial only when error/metadata/output share one nonblank reason, `output.status=skipped`, `error.owner_lost=true`, and `error.deterministic_terminal_failure=false`; the newly returned execution spine must still be exact `running`. Current broad author evidence is D1m `68+15 subtests`, Operation selection `52+61 subtests`, and migration runner `24+65 subtests`; the enclosing commit and fresh pinned review remain pending. Served population is still zero because D1m has no action-specific populated revisioned result spec, result/simulate serializer mapping, or complete served predicate. No provider/model/live authorization. | 2026-07-17 | D1j/D1k advisory NO-GO; D1l pinned NO-GO and fixed-forward pending; D1m stable-tree evidence complete, commit/review pending |
| R-030 | D3c1a demo non-enumerable `raw` compatibility view | Typed demo DTOs retain an own non-enumerable `raw` reference for existing snake-case compatibility consumers. The D3c1a JSON seal snapshots, measures, freezes, and returns only the enumerable DTO graph; direct access to or separate serialization of `dto.raw` is not covered by that final 1 MiB JSON footprint. | Current frontend types and operation/workflow consumers still use `raw` for canonical wire-field access. Removing it inside the projection fixed-forward would broaden the change into a frontend API migration. The compatibility view is already recursively projected and private-field filtered, stays non-enumerable, and is never used as the Response/JSON.stringify root in the sealed paths. | Migrate consumers to typed DTO fields or a separately named, explicitly bounded wire snapshot; then remove `attachDemoRaw`, every `raw` interface member, and the compatibility projection. Do not make `raw` enumerable or silently spread it into public responses. Retain this row, append exact exit evidence, and transition `accepted -> closed`; never delete the audit history. | **Any Response body, cache key, transport serializer, spread/`Object.assign`, or new product consumer starts serializing `raw`; `raw` becomes enumerable; or the frontend workflow DTO compatibility-retirement batch begins.** | Ultra review `20260715T030659Z_Track_D_D3c1a_public_projection_fixed-forward_ec0ad95.md` finding #7 identified the missing ledger owner. Fixed-forward tests prove root/list DTOs serialize the returned trusted snapshot, `raw` remains non-enumerable, admitted typed/raw list counts stay synchronized, and hostile/private fields remain absent. The second valid Ultra review `20260715T034542Z_Track_D_D3c1a_trusted_JSON_snapshot_fixed-forward_f5c33b0.md` finding #4 identified the missing durable removal task. The third valid Ultra review `20260715T041856Z_Track_D_D3c1a_second_Ultra_fixed-forward_46af086.md` finding #2 caught the task's incorrect deletion instruction; `docs/NEXT_TODO.md` now retains the row and requires exact evidence plus `accepted -> closed`. This row does not waive `R-019` bounded-streaming work. | 2026-07-15 | accepted |
| R-031 | authenticated Operation API exact-owner closure | D1g fixed-forward centralizes authenticated Operation authorization on `agent_actions.workspace_id` / `operation_runs.workspace_id`; a run also requires its linked action in the exact workspace. Action/run lists use server scope; detail/provenance and approve/reject/dispatch/retry/resume/cancel preflight the same owner. Nested commands require their physical `operation_id` to join an exact-workspace run+action, nested events exact-filter physical workspace, and planned CRM/export command refs exact-bind the current run before compatibility observation or writes. Valid refs are captured once and still pass request/target/approval guards; all SQL authorization predicates precede `LIMIT`; `actor` remains provenance only. | D1f bound only the exact three CRM submits. D1g addresses the downstream IDOR and later nested-evidence findings without treating actor/user payloads as record ownership, migrating schema-less actions, or changing served=0. Pinned `646e596` advisory passed invalid-reference boundaries but found valid planned replay bypassed persisted request validation and approval; fixed-forward separates those phases and avoids a mutable-ref second read. Pinned `c7d2e24` advisory then re-raised R-019 because an existing-plan sensitive-CRM approval guard wrote action/run/event while its command remained queued; `ebe7ed0` returns that approval requirement read-only. Its fresh pinned non-author scope-local advisory is GO, while formal highest-effort review remains required before closure for hosted/live/product signoff. | Preserve `ebe7ed0`'s exact owner and zero-write contracts while obtaining the formal scope-matched artifact: invalid refs pre-write fail-closed; valid-current replay passes persisted request, target, and approval guards without compatibility writes; authenticated/open positives; canonical owner/source matrix; SQL filters before limit; foreign/missing parity; and R-019/R-028 non-closure. Append formal evidence and transition this row to closed; do not delete its audit history. | **Before hosted/live multi-user Operation exposure, any Operation action enters the served registry, or manual/product/milestone signoff of authenticated Operation controls.** | Current fixed-forward evidence: request-scope + D1g PG **29 passed + 107 subtests** (D1g alone **7 + 35 subtests**), dispatch registry + exact stale characterization probes **25 passed**, combined D1 **117 passed + 198 subtests**, exact adjacent Operation nodes **4 passed**, full Operation runtime **136 passed + 503 subtests**, lint **58 files**, and global mypy **81 errors / 4 files**. Candidate `646e596` pinned advisory=`NO-GO 0/0/1/1`; candidate `c7d2e24` pinned advisory=`NO-GO 0/0/1/0`; commit `ebe7ed0` fresh pinned non-author scope-local advisory=`GO 0/0/0/0`, including full-table zero-write and parent-regression sensitivity. This is not formal GO. No new state mutator or transaction-lock caller; R-019's **26** ratchet is unchanged. R-028 is not triggered. | 2026-07-16 | scope-local advisory GO; formal review pending |
| R-032 | FT2 plan-review location application owner + dormant tagged-clear wire contract | The backend review gate (`plan_review.py` `build_plan_review_gate` `editable_fields`) still authorizes neither `target_locations` nor `exclude_target_locations`, and `apply_plan_review_decision()` still has no location application owner, so reviewed location edits cannot reach the canonical request / filter hints / provider manifest / execution bundle. The frontend serializer (`planReviewDecisionToApiPayload`) already conforms to the tagged tri-state wire contract (values → replace; `[]` → explicit opt-out; `{ op: "clear" }` → restore the absent server-default state; key omitted → not part of this decision) and stays DORMANT: it never serializes either operation while the server-issued gate does not list the field. | The backend owner is being implemented in parallel by the sibling lane `f6-backend-plan-review` (touching `plan_review.py` + the orchestrator apply path); both lanes coordinate through the pinned contract entry `docs/COHORT_SELECTION_CONTRACT.md` §"Plan-review location decision wire contract (dormant pending backend owner)" (wire shape, owner, activation condition, consumers, rollout/mixed-version behavior, exact tripwire, removal condition), so the frontend does not guess the backend shape. Accepting the dormancy with a documented contract is honest; shipping an unowned write path would not be. | Backend gate lists both location fields independently; `apply_plan_review_decision()` merges replace/`[]`/clear into the canonical request, rebuilds filter hints/provider manifest/execution bundle, and an end-to-end regression verifies the launched workflow executes with the reviewed values; wire-shape parity check against the contract entry in the same pass; then transition this row `accepted -> closed` (never delete the audit history). | **The day the backend gate lists either location field OR `apply_plan_review_decision()` gains a location branch — this row must close in the same pass or the lane is red; also any frontend serializer change to the tagged shape before activation.** | rerun4 Ultra review `20260719T203000Z_Track_D_TML_FT2_frontend_targeting_and_facet_consumption_rerun4.md` finding 5 (F6 not a valid tracked residual) — pinned `plan_review.py` inspection confirms the gate list and the missing apply owner; r4 handoff `.coord/handoffs/tml-ft2-ff-r4-v1.md` §4; frontend conformance pinned by `test_location_fields_round_trip_through_real_request_paths` tagged-clear assertions. **R-032 closed 2026-07-20:** tripwire fired — backend owner landed `fa435a4` (gate lists `target_locations`/`exclude_target_locations` independently; `apply_plan_review_decision()` merges replace/`[]`/clear into the canonical request and rebuilds filter hints/provider manifest/execution bundle; 19+15 tests incl. 3 real-orchestrator launched-workflow PG regressions); frontend serializer `0c4d337` conforms and activates on the server-issued gate; wire-shape parity verified against the contract entry, whose title moved `dormant pending backend owner` -> `ACTIVE since fa435a4` in the same pass. Audit history retained. | 2026-07-20 | closed (exit evidence appended) |
| R-033 | PG simulate-lineage incident residuals (jobs `27f7e8a9df32`/`88f84960e381` + workers 27/38 + orphan workers 3-6) | 两条 jobs 行的 `summary_json` 保留 simulate 占位内容（jobs 无隔离字段）；workers 27/38 的原始 07-19/20 运行本身即 simulate；workers 3-6 引用无 jobs 行的孤儿 job `tml_full_fetch_20260719`。已修复部分：4 条 simulate shard 行已隔离（status=cancelled + governance_quarantine 标记）、3 条真实 former 行 source_path 已回正 canonical、6 条 07-22 复活 worker 行已标注、hot-cache 27 个 simulate 文件已隔离到 `runtime/quarantine_simulate_20260722/`、写入栅栏已落地（`tests/test_live_schema_write_fence.py`）。机制：test-env jobs 共存 live schema + 无三重门的 daemon resume + fail-closed simulate 物化（原始 07-19/20 执行本就是 simulate，无真实前态可恢复）。**勘误：commit 2856b14 声称 daemon 重启首 tick 干净，但 PG 显示 05:49:45 UTC google simulate 写入发生在该 commit 之前 13 分钟——首波 02:22-02:28 UTC 无 daemon 日志覆盖，归因为推断** | 真实 roster lineage 从 salvage 快照经 `scripts/backfill_acquisition_shard_query_families.py --rebuild-from-assets` 前向重建；jobs 行等终态老化；孤儿 run `tml_full_fetch_20260719` 的 8 条 stalled running workflow_current_state 行待 daemon-ops 终态化 | **任何消费者读这两个 job 的 summary_json、或 stalled running 行被 orphan-admission 复活、或 R5(b) recovery park 栅栏落地时** | salvage-closeout-audit 工作流（2026-07-22，pgdebt agent + google 对抗复核 agrees=true）；修复 SQL 与 rowcount（4/3/6）在会话记录 | 2026-07-22 | partially repaired; residuals accepted |
| R-034 | `tests/test_request_scoped_roster_shards.py` 整模块 collection error | 模块 import 了 src 中不存在的 `build_large_org_keyword_probe_shard_policy`（Kimi commit 233a31a『unify roster lane contract — no org-size forks』的部分落地：测试引用了未 land 的规划函数），整文件 ~37 测试无法收集 | 基线对照：stash 前后同败（HEAD 69b7423 clean tree 亦败），pre-existing | 策略统一 Step 1（death-code/doc cleanup）裁决该函数：要么补实现（scoped keyword probe 并入 shard planner，即统一 Step 4 的一部分），要么删除引用并重写受影响测试到现行 contract | **策略统一批次触碰 `company_shard_planning.py` 或本测试文件时必须同批收口** | 本会话 stash 对照实验 2026-07-22；`git log --oneline -1 -- tests/test_request_scoped_roster_shards.py` = 233a31a | 2026-07-22 | closed (exit evidence 2026-07-22: strategy Step 1 收口——模块从整体不可收集修复到 52/52 全绿。根因三层：①import 了未 land 的 `build_large_org_keyword_probe_shard_policy`；②执行类绕开 repo PG fixture 惯例（手拼 DSN 无 schema → limiter 建表 InvalidSchemaName 全数 blocked），已改用 `PGControlPlaneStoreTestMixin`；③17 个测试 pin 了 233a31a 中间态合同（unsharded 默认/每 shard 一 manifest lane/cohort 走 legacy 元数据），已按批准合同重写为 fn-sharded 默认断言（实测校准）。墓碑（tombstones，行为归属）：`test_keyword_policy_subdivides_only_over_cap_function_roots`、`test_keyword_probe_policy_composes_request_axes_and_function_expansion`、`test_keyword_probe_policy_without_cohort_keeps_priority_function_axis` → Step 4 keyword_union 验收草图（设计意图存 233a31a + master plan WS1 Step 4）；`test_plan_builds_request_shards_for_cohort_role_request`、`test_plan_manifest_parity_for_cohort_role_request` → cohort lane 归 CohortProviderCompiler 套件（test_cohort_*.py），归一见 Step 5。顺手修复：manifest lane 对存量 no-policy 形状丢位置轴 → base_filters 回退（planning.py）。) |
| R-035 | `test_hosted_workflow_smoke.py::test_hosted_simulate_smoke_matrix_completes_across_small_medium_large_orgs` ×1 | hosted simulate 全链路完成后 final `stage_summaries` 为空（`'linkedin_stage_1' not found in {}` @:1024），三案（skild/humansand/openai_reasoning）全空，job_status=completed 但 stage summary 物化缺失 | **归因修正 2026-07-22**：worktree+资产软链对照证明 69b7423（重构续章基线 pin）与 8a83bad 均同败——先于 B3 Step 3/Block (a)/harvest 拆分全部工作（bcdb035 提交信息中『attributed to B3 Step 3』系误判，以本行为准）；更早控制点（f544954）因 PG schema 世代漂移不可测。该 focus 仅在 changed-path 映射中触发，疑自 B2 期某提交起静默断裂 | 诊断 stage summary 物化为何在 simulate 完成路径缺失（对照 d97d17c 前后 hosted final 载荷）；按结论修管线或翻冒烟期望 | **B3 里程碑签核前收口；触碰 hosted smoke 或 stage summary 物化时同批** | 本会话 worktree 对照实验 2026-07-22（69b7423/8a83bad 同败,资产软链法） | 2026-07-22 | closed (exit evidence 2026-07-22: 根因 = 冒烟 harness pin 了两个已退役面——①legacy `/api/jobs/{id}/results` 投影切断后载荷不再携带 `workflow_stage_summaries`（410→投影路由；该键现归 progress/dashboard 面），修复 = workflow_smoke 终态装配处单缝回填（results 缺键时取 final progress 快照，stage digest/raw/wall-clock 三个下游统一受益）；②`public_web_stage_2` 为 two_stage/require_stage2_confirmation 显式 opt-in（planning._should_include_public_web_stage_2），矩阵案未请求即按合同缺席，断言改条件式（在场必 completed）；同家族第二腿：history round-trip 测试裸打 legacy /results 410，改走 `_fetch_smoke_results_payload` 跟随 cutover。管线无回归——stage summaries 一直正确记录在 progress 面。focus 四件 4/4 绿) |

## Active candidate annotations

- **R-019 / D1f bounded read-only preflight (2026-07-15):** D1f adds schema-pin/owner/version verification before a
  new CRM command plan and again before an existing CRM writer is invoked. It adds no state-update call site, no
  transaction-lock caller, no command-claim/terminal mutation, and does not change the **26** production state-sync
  caller ratchet. This is the same narrow pre-write-validation class already allowed for D1c, not an R-019 atomicity
  remediation: approval/plan/event writes and command effect/terminal/Operation sync remain outside one UoW. The next
  actual retry/dispatch/completion state mutation, lock caller, or live/manual signoff still triggers R-019 in full.
  In particular, repository upsert still does not return inserted-vs-existing provenance: two same-state concurrent
  first submissions can both miss preflight, so the loser may remain fresh-compatible `202/idempotent_replay=false`.
  D1f deliberately limits its `200/true` claim to preflight-observed or non-fresh committed replay; closing this marker
  ambiguity requires the atomic repository outcome, not another status heuristic. Conditional non-required
  approval/cancel/retry also preserves existing approval metadata and deterministic parent-run status ownership.

- **R-028 / D1f bounded read-only preflight (2026-07-15):** D1f carries the four-field owner snapshot into the exact
  three existing-record CRM command payloads and rejects a canonical action/command/target mismatch before invoking
  the existing writer. It adds no CRM mutation caller and does not move the CRM effect, Activity/Attempt, EntityDelta,
  command terminal state, or linked Operation completion into one transaction. The read-only check can still race a
  later effect and partial-effect recovery remains outside this slice, so R-028 stays pending and its next-writer /
  effect-boundary / live-signoff tripwire is unchanged.

- **R-029 / R-031 / D1f (2026-07-15):** exact three CRM actions leave the schema-less numerator, reducing it from 15
  to 12 while served tools remain zero. Their input envelopes are presence-selected and single-carrier; this does not
  migrate the other 12 R-029 actions. Epoch `d1f_r029_20260715_v2` gives action-scoped submit-replay evidence a new
  durable key instead of mutating v1 run-carried evidence. Authenticated submit is exact-owner fenced; downstream Operation
  list/detail/provenance/control is separately remediated by the review-pending D1g candidate under R-031. Local
  advisory findings and author tests are fixed-forward evidence only. Pinned `5677a59` advisory=`NO-GO 0/0/1/0` found
  list/object raw statuses could raise HTTP 500; current string-first replay/fresh enum guard returns stable HTTP 400
  with exact evidence `25+72`. Commit `0740a36` fresh pinned non-author advisory=`GO 0/0/0/0`; it is not formal GO.

- **R-019 / R-028 / R-031 / D1g (2026-07-16):** D1g fixed-forward exact-scopes authenticated action/run
  lists, detail, provenance, and every Operation control to the server-derived workspace; an authenticated run also
  requires a same-workspace linked action. Foreign/missing ids share one generic not-found body per resource and the
  denied matrix leaves action/run/event/command/Activity/EntityDelta/CRM state unchanged. Open-mode compatibility is
  retained. Shared-workflow commands and malformed events are now filtered by linked owner/physical workspace before
  SQL limit, and planned CRM/export command refs exact-bind the current run before any compatibility event/write.
  This is authorization-only: no new state mutator or transaction-lock caller is added, the R-019 caller
  ratchet remains **26**, and no CRM effect/UoW boundary changes, so R-019 stays open and R-028 is not triggered. R-031
  invalid-reference boundary passed pinned `646e596` review, but the advisory returned `NO-GO 0/0/1/1`: a valid
  planned replay bypassed persisted request validation/approval, and D1g checkpoint/current R-029 wording drifted.
  Candidate `c7d2e24` re-review=`NO-GO 0/0/1/0` because the captured-plan CRM approval branch wrote
  action/run/event while leaving its command queued. Current fixed-forward captures the valid ref once, passes
  request/target/approval guards, skips compatibility writes, and returns that existing-plan approval requirement
  read-only under a full-table zero-write regression; P3 wording is checkpoint/current explicit. Commit `ebe7ed0`
  fresh pinned non-author scope-local advisory=`GO 0/0/0/0`; R-031 remains open pending formal highest-effort review. No hosted/live/provider/
  served authorization follows from author or advisory evidence.

- **R-019 / R-028 / R-029 / R-031 / D1h (2026-07-16):** `enrich_person_public_web` leaves the schema-less
  numerator, reducing it from 12 to 11 while served tools remain zero. Authenticated submit binds every selected CRM
  record to exact server workspace+user; missing/foreign/mixed batches are one generic not-found and pre-submit
  full-surface zero-write. Dispatch revalidates before a new plan. The queue owner revalidates again before batch/run
  materialization, but only after its existing claim/running transition; a rejection terminalizes the command and may
  synchronize the Operation, so its guarantee is limited to zero batch/run/EntityDelta writes. These checks do not put
  approval, plan, claim, effect, terminal, or Operation sync in one UoW and do not close R-019/R-028. R-031 has a
  scope-local D1g advisory GO but remains formal-review pending. D1h author evidence and any advisory review are not formal `GO`; no
  provider/model/live/served authorization follows. Pinned `a36333b` advisory=`NO-GO 0/1/2/1`; pinned `6742130`
  advisory=`NO-GO 0/3/1/0`. The current fixed-forward now makes selector aliases presence-safe and target-exclusive,
  attributes batch requests to the stable queue service, rejects generic-principal/overlapping/cross-workspace attached
  batch-run authority and deterministic-job collisions before materialization, and exact-binds all 1–1000 admitted
  records plus a surplus sentinel to the canonical batch, persisted runs, and persisted job without a mutable
  action-payload fallback. A strict-PG lease-fenced checkpoint freezes job status while re-deriving immutable linkage,
  so expired-lease recovery survives batch progress without duplicate batch/run writes; post-start PG rereads fail
  closed before phase links. Fresh pinned review of the enclosing commit remains required.

- **R-019 / R-029 / D1i (2026-07-16):** `start_acquisition_run` leaves the schema-less numerator, reducing it from
  11 to 10 while served tools remain zero. Canonical company/query input and the server-owned workspace target are
  revalidated through submit, approve/retry/resume, dispatch, and the acquisition root owner. The root owner requires
  exact OperationRun→AgentAction, canonical payload/envelope/source/causality, and exact current claim owner/attempt/
  unexpired lease, using the PG repository clock and UTC interpretation for naive persisted timestamps. Strict JSON
  container/scalar/schema checks apply to the persisted root, event, child, causality, payload, and result. The
  root-plan event, deterministic intent child, and root terminal now commit or roll back in one PG transaction; exact
  succeeded replay validates the full persisted tuple before repairing current-state/recovery wakeup/Operation sync.
  The D1i fixed-forward additionally writes the physical downstream edge in that UoW, treats retry scheduling as
  mutable lifecycle state, reconciles successful-COMMIT driver ambiguity only through fresh authoritative exact replay,
  and installs actual-root-scoped child shape/uniqueness via trigger plus parent identity advisory locking; the
  constraint blocks unknown-owner/cross-workflow alternate children and wrong-type children under an
  `acquisition.run.create` root, while non-root fan-out remains legal. Locked conflict reread accepts only the
  canonical winner. This is not a global parent-generation fence for other command families.
  R-019 remains open because Operation/action preflight is outside that UoW, the repair path is post-commit, and a
  committed failure CAS can lose its acknowledgement; the 26-call ratchet must not rise. Original pinned advisory=
  `NO-GO 0/2/1/0`; `dce094e` fresh pinned advisory=`NO-GO 0/2/2/0` for commit acknowledgement, parent uniqueness,
  physical causality, and mutable retry state. The current fixed-forward locally reconciles those four findings; fresh
  review of its new commit is still pending. Local evidence is not formal
  `GO`, and no served/provider/model/live authorization follows.

- **R-019 / R-029 / D1l (2026-07-16):** `search_projection` and `filter_projection` leave the schema-less numerator,
  reducing it from 8 to 6 while served tools remain zero. Projection identity stays shared-canonical rather than
  tenant-owned. Operation dispatch uses the exact persisted Operation workspace for CRM overlays; authenticated
  direct/job reads default to the server-derived workspace, an explicit `default` remains the pre-auth legacy selector,
  and open mode preserves its explicit workspace; direct candidates/search transport tests pin all three modes.
  Persisted schema-defined action input/target must be strict JSON, so tuple and other Python-only containers fail
  before transition writes. Generic changed-during-read/page-read/search-read reasons normalize to stale/reselection,
  and replay preserves `reselection_required=true` without a duplicate event. D1l is a bounded commandless R-019
  specialization: terminal action+Operation+event writes commit in one dedicated PG UoW, and the session
  Operation-dispatch/publication locks consume one shared monotonic 5s total acquisition deadline rather than 5s each.
  A past deadline fails before connection acquisition; budget exhaustion returns `operation_dispatch_lock_busy` or
  `projection_publication_lock_busy`, both mapped to HTTP 409. Four production generic projection-field patch callers
  now use `patch_publication_fields_under_lock`; production raw projection `upsert` is statically rejected. The helper
  holds the publication session key, then native `SELECT ... FOR UPDATE` merges the current row, and rejects
  `projection_person_search_index_build_generation`, `projection_person_search_index_build_input_revision`, and
  `projection_person_search_index_input_revision`, so public count/readiness patches cannot bypass D1l read/result
  exclusion. Because no workflow command exists, command generation/lease fencing is inapplicable; D1l adds no direct
  state-sync caller and the ratchet remains 26. This does not close R-019 for command-producing, retry, completion, or
  other Operation paths. R-028 remains unchanged。Final stable author evidence=adversarial `5+9 subtests`、API+writer
  `50+87 subtests`、D1 contract `133+176 subtests`、Operation runtime `139`、lint `58 files`、mypy `81/4`、compile/diff
  clean；fresh dirty-tree non-author read-only re-audit=`GO 0/0/0/0`，但未 pin、不是 formal `GO`。后续 hash-bound
  independent artifact `runtime/reviews/20260716T144526Z_Track_D_D1l_projection_read_action_schema_activation.md`
  对 unchanged-from-`fc5d603` 的 35-path scope 返回 **NO-GO 0/4/7/0**，另保留 R-019/R-028/R-029；其十一项
  new finding 已分成 workspace/fallback/filter carrier、replay/failure/retry/lock order、index-owner/ready-empty、
  bounded SQL/lock/bytes 四批。No served/provider/model/live authorization follows.

- **R-019 / R-029 / D1m (2026-07-17):** D1m candidate moves only `refresh_company_public_web_assets` out of the
  schema-less numerator, reducing the candidate partition from 6 to 5 while served tools remain zero. The request is
  deterministic `seed_url_only`: explicit company/source-family/seed-URL input, canonical defaults, and
  `refresh_nonce` iff force. The owner derives exact `workspace_id + company_key` through the canonical alias resolver;
  authenticated transport uses server scope and open mode preserves explicit operator workspace. Persisted action/run,
  dispatch, root refresh, source collection, and asset materialization each reload and exact-compare the canonical
  request/target plus OperationRun/AgentAction and recomputed expected idempotency/command/event causality before their
  new effect. Source collection normalizes one effective idempotency key, holds sorted key/run advisory locks, rejects
  split identity, and binds physical command id/current attempt/lease ownership to reclaim and owner-CAS terminalization;
  stale attempts return `owner_lost` with zero source-run write. A later current retry attempt may reclaim running/failed
  work, while ActivityRun remains `retry_wait` between attempts and converges to `succeeded` with the successful attempt.
  Root/source/materialize drains opt into bounded expired-`claimed` recovery without changing the shared default.
  Guarded start uses the PostgreSQL clock and locks the deterministic ActivityRun plus all deterministic ActivityAttempts
  through the current command attempt. It full-validates primary ids/keys, workspace, command/activity/workflow/
  operation/type/owner/provider links, attempt/lease, and owner metadata. Split identities, alternate nonterminal rows,
  current/future terminal execution Activity/Attempt rows, and future/malformed resume evidence fail closed before writes; fully
  exact prior terminal execution evidence may remain. A succeeded owner-specific resume Attempt may coexist only under
  deterministic resume id/key, generation no greater than current, and exact workspace/activity/workflow/command/
  provider/request-ref/lease plus target/company/boolean-force/nonblank-output-reason semantics. Successful takeover
  closes all exact superseded prior running execution attempts before the returned exact current Activity/Attempt
  `running` spine is accepted. Exhausted-final-source closure uses the DB clock as sole expiry authority and atomically
  fails the exact Command/Activity/Attempts. It retains a valid resume-control Attempt and converges an exact current
  failed owner-loss partial only when `error.reason == metadata.owner_lost_reason == output.reason` is nonblank,
  `output.status=skipped`, `error.owner_lost=true`, and `error.deterministic_terminal_failure=false`; an active lease or
  any future/malformed/identity/semantic conflict is zero-write.
  Materialization consumes the bounded immutable `company_public_web_run_snapshot_v3` frozen on the completed run; its
  digest binds discovered assets, summary, artifact paths/publication digest, source revision/completion time, and both
  run timestamps, so mutable source rows or a later
  clock read are not authority. Positive logical revision owns PG/memory latest ordering; brownfield fallback is
  explicit. Exact-claim asset/evidence writes share one PG transaction and monotonic `updated_at`. Typed completed
  repair derives mandatory deferred sync from the authenticated source owner instead of mutable run policy. Operation
  workspace remains control-plane authority and canonical company facts retain shared `workspace_id=default`. A source
  effect whose pure child planning fails retains artifact refs for retry but writes no plan event, child, source-run
  EntityDelta, or source terminal CAS. After snapshot freeze, the exact plan event, deterministic materialize child,
  one source-run EntityDelta, physical downstream edge, and source terminal result share one PG UoW; takeover,
  final-CAS failure, and identity collision roll that bundle back, while commit-ack loss succeeds only after exact bundle
  validation. Source rows/artifacts still commit before that bundle and linked Activity/Operation synchronization remains
  post-commit. This is bounded pre-effect/owner fencing plus a source-completion UoW, not a global submit/effect/
  Operation-sync UoW, command-generation fence, or exactly-once closure; R-019 remains open. Current direct state-sync
  ratchet is **24**; prior 26-call D-3/D1i/D1l checkpoints remain historical evidence. R-029's complete
  API-submittable-population release-window zero-hit and
  separate constraint-validation deletion gates are unchanged. Pre-D1m writers also remain revisionless and can use
  the old generic canonical writer, so hosted rollout requires either a quiesced single-version cutover or a separately
  reviewed dual-write/compatibility bridge with exact old/new convergence evidence; this is an explicit R-019 rollout
  gate, not a property of the current non-live candidate. Latest broad author evidence=`D1m 68+15 subtests /
  Operation 52+61 / migration runner 24+65 / mypy 81/4`; earlier combined D1 `219+209` is predecessor evidence only;
  fresh pinned review remains pending. Served population remains zero because this action has no populated
  action-specific revisioned result spec, result/simulate serializer mapping, or complete served predicate. No
  provider/model/live authorization follows.

- **R-019 / D3c2i (2026-07-15):** decision-only parent lock ratifies one future plan-review-owner aggregate:
  `PlanReviewAuthorityRepository` owns immutable `plan_review_gate_authority_versions` plus
  `identity_search_budget_grants` and immutable `identity_search_budget_consumptions`. The 32/37/22-column manifests,
  full PFX, historical seven-pin exposure parent unique/FK, Tier-2 issuance parent unique/FK, four-dimensional balance,
  pre-transport debit CAS, terminal-no-revive and exact supersede-with-transfer lifecycle are fixed; legacy JSON
  (`plan_json`, `gate_json`, `decision_json`) is explicitly non-authoritative and is never a strict-D3 backfill source.
  The same owner persists and recovers `human_transition_pending` through an eight-attempt due path that remains
  fail-closed. Exact new boundary is 25 structural constraints/16 FKs, 40 local checks, 4 indexes/5 access paths and 11
  CAS methods; it expands the future combined D3 evidence boundary to 10 relations/77 constraints/45 FKs/15 indexes,
  19 forward and 18 rollback actions. This advances only the Plan §6 item 6/7 and OB-1.1/OB-4.1/OB-9.1/OB-10.2
  *decisions* to `decision_locked_not_implemented`: all three tables/repository/runtime remain physically absent, author
  evidence is not formal `GO`, and D3c2h1 plus D3c2i matching pinned reviews remain prerequisites for dormant migration.
  R-019's status and ratchet remain open; R-023, R-027, R-028, R-029, action-root, other OB gates, served=0,
  provider/live, and Migration A-D are unchanged.

- **R-019 / R-029 / D1n S1e1 (2026-07-18):** decision-only, zero-migration start-authority lock chooses the existing
  operation-event payload as the physical owner rather than adding a table. Specialized submit will create the exact
  v2 pending Action plus ActionApprovalRequired sequence 1. Specialized create will atomically persist ActionApproved
  sequence 2 with the complete `acquisition_confirmation_receipt.v1` payload, exact Action/Operation projections,
  WorkflowStarted/CommandPlanRequested sequences 1/2, one root command, exact `workflow_current_state`, and
  OperationCommandPlanned sequence 1 as the
  command-acceptance winner. The same immutable receipt/digest owns the five-field acquisition parent-budget admission
  envelope; it is not D3 `cost_reservations`/`dispatch_exposures` and has no release/consume CAS. Result prepare remains
  read-only and shared accept owns attempt/slot/journal. Scope is only `isolated_local_canary + simulate|scripted` with
  runtime_outbox delta zero; generic approve/dispatch, live/replay, provider/model calls, and serving do not expand.
  This annotation changes neither row status nor R-019's current ratchet, and does not close R-029's 5/15 bridge,
  Plan §6#6, OB-2.2/10.3/10.4, or served=0. S1e2 product implementation and fresh pinned review remain pending.

- **R-019 / R-029 / D1n S1e2a (2026-07-18):** the first product slice implements only the specialized pending-submit
  aggregate selected by S1e1. It revalidates the current local-canary occurrence, mode, pins, and complete bound root
  before adapter access; then exact-locks the Action stream, Action identities, reserved pending result slot, and
  immutable preview under one deadline, rebinds the locked preview using one DB-owned timestamp, and atomically
  inserts or exact-replays the `approval_required` Action plus `ActionApprovalRequired` sequence 1. Fault and
  lost-ack paths preserve one aggregate; eight-way concurrency produces one submit plus seven exact replays. A bounded
  local audit's successor-replay, deadline, and PG-authority findings were fixed-forward and its re-audit was advisory
  `GO 0/0/0/0`, not a formal verdict. Submit creates
  no Operation, workflow event/command/current state, result attempt/journal, outbox, Activity, AcquisitionRun,
  provider/model, or domain row. This implementation advances neither the R-019 direct state-sync ratchet nor R-029's
  population/observation epoch: the partition remains 10/5, public/default served population remains zero, and
  S1e2b/S1e2c plus fresh pinned review remain pending. Plan §6#6, OB-2.2/10.3/10.4, provider/live, and all other
  residuals remain open.

- **R-019 / D3c2h1 fixed-forward (2026-07-15):** the first pinned highest-effort non-author review of
  `1c4a2d9177dcb3470117700086b12fd533898bb7` returned formal `NO-GO 0/3/3/0`. The author repair addresses all six
  findings without SQL/runtime: classification now has nonterminal `current_pending_apply` plus a fresh normal-terminal
  continuation; failure-first/retry-first/distinct-response races preserve immutable cost and can only stale+quarantine;
  attempt 8 transient/lease expiry converges terminally; response/quarantine artifact refs reject blank/whitespace; and
  D3c2g's two cost tables plus D3c2h1's five evidence tables share exact 13 upstream constraints and 52 seven-table
  constraints including 29 FKs. The later Ultra attempt against `f0a0069...` failed closed as `invalid_transport`, so its
  substantive `0/1/2/0` output is advisory only. The second author repair now exact-compares every relation/index/access/
  race/DAG tuple, shortens the sole over-limit identifier below PostgreSQL's 63-byte ceiling, gives both pending `<8`
  claim and pending `=8` convergence admitted access through the same pending-state index, rejects nullable timestamp
  `CHECK` `UNKNOWN`, and defines an executable 17-action create/attach plus 16-action rollback DAG with all six internal
  forward/cycle FKs attached only after their targets exist. D3c2i now separately ratifies the typed plan/review/gate and
  OB-10.2 Tier-2 grant parent decisions; matching pinned non-author `GO` artifacts for this repair and D3c2i remain hard
  predecessors, so no dormant migration is authorized. Pinned `af4db419...` fresh scope-local advisory=
  `NO-GO 0/0/1/0`; its sole P2 found that the document told a future migration to add
  `workflow_commands.workspace_id` even though migration `0003` already installs `TEXT DEFAULT '' NOT NULL` plus its
  named `NOT VALID` check. The current fixed-forward requires adopt/validate, preserves the empty-string brownfield
  sentinel, forbids add/drop/rewrite/reinterpret, and makes the oracle read `0003` directly. `fdb3b792...` then received
  a fresh exact-object non-author local advisory `GO 0/0/0/0`, recorded at
  `runtime/reviews/20260715T215450Z_Track_D_D3c2h1_fixed-forward_local_advisory.md`; it closes the P2 re-raise but is not
  formal highest-effort `GO`. This annotation
  changes neither R-019's pending state nor its 26-call ratchet, and it does not alter or claim closure of R-027, R-023,
  R-028, R-029, action-root, served=0, provider/live, Migration A-D, or any other residual.

- **R-019 / D3c2g (2026-07-15):** decision-only cost-ledger/dispatch-exposure lock ratifies the sole future
  `CostLedgerRepository` aggregate and its exact parent/child `21/75` manifests, keys/indexes/checks, immutable
  pricing/reconciliation registry shapes, eight-method CAS surface, source-dependent terminal mapping, and parent-first
  settlement order. Strict-D3 live/simulate/scripted all require a durable exposure so D3b receipts remain reachable;
  live money is positive, simulate/scripted money is zero, and replay now waits for its schema revision.
  OB-2.2 and OB-10.3 advance only to `decision_locked_not_implemented`; the current `83` baseline
  tables and `41` descriptors still contain neither physical table, and no future owner/registry/store wiring exists.
  D0f now provides the exact TIMESTAMPTZ codec, durable envelope owner/ref grammar, and one envelope-specific immutable
  insert/replay path. Exact Decimal plus cost/receipt table-specific insert-once/CAS primitives remain explicit
  implementation prerequisites; generic replace-all upsert is forbidden.
  This candidate authorizes no SQL, descriptor, repository, runtime writer, settlement daemon, provider/model path,
  live/W6/manual signoff, rollout step 3, or served tool. D3c2h0 now separately ratifies receipt/quarantine cross-contract
  identity/order, but verification-intent and exact receipt/quarantine manifests, upstream FKs, Migration B-D, adoption,
  and fresh pinned/formal review remain later gates. R-019, R-023,
  R-027, R-029, action-root, the remaining OB gates, and served=0 retain their existing status.

- **R-019 / D3c2h0 (2026-07-15):** decision-only cross-contract ratification supersedes D3b's scope-digest-only
  receipt/quarantine sketches with the complete
  `(runtime_namespace, provider_mode, workspace_id, scope_digest, coordination_plan_review_id)` PFX. Both response and
  failure receipts exact-copy the exposure's post-claim `command_attempt`; response-only quarantine forbids
  `workflow_run_id` and `reconciled_no_call`, so its cost axis is only pending→confirmed|uncertain and no-call has no
  receipt/quarantine. This active annotation supersedes D3b's exposure-first optional-quarantine sketch with two PG
  compositions: pure exposure-first is exposure lock→receipt→exposure terminalization with quarantine permission=0;
  response-classification may add optional response quarantine only after `d3-dispatch-v2` locks and validates the
  complete global owner-row prefix and derives current/stale from stored current state. Caller flags, callbacks, and stale
  `ClaimReceipt` values have no classifier authority, and neither path may return to runtime/domain owners after exposure.
  Durable pending response-classification owner/state, retry/recovery, exact replay, and idempotency remain D3c2h1 work.
  Current `ModelInvocationEnvelopeV1` remains the sole canonical shape, and D0f now supplies its sole durable full-PFX
  ref/persistence owner; placeholder refs/second schemas remain forbidden. Initial v1 admits only `model_tool_v1`
  live/simulate/scripted; simulate/scripted zero-cost evidence remains
  reachable, replay is zero-write, and Harvest/provider-search cannot masquerade as model transport. Oracle=`9 passed`.
  D3c2h0 ratifies no ordered manifest/count, SQL, descriptor, repository, runtime, or live path. D0f is now complete;
  next is D3c2h1 exact intent/receipt/quarantine manifests after Decimal plus table-specific specialized insert-once/CAS
  prerequisites. R-019/R-023/R-027/R-029, action-root, remaining OB gates, rollout step 3, and served=0 remain open.

- **R-019 / D0f (2026-07-15):** D0f closes only the durable `ModelInvocationEnvelopeV1` full-PFX ref/persistence and
  TIMESTAMPTZ substrate prerequisites. It adds one immutable PG evidence table with exact replay/collision and a fixed
  retained-to-tombstone CAS; it does not accept/consume logical results, authorize effects or sends, implement cost or
  receipt/quarantine owners, close any Migration A-D aggregate, or activate live/served paths. Fresh formal review is
  pending. D3c2h1, Decimal/cost tables, R-019/R-023/R-027/R-029, action-root, remaining OB gates, rollout step 3, and
  served=0 remain open.

- **R-019 / D3c2f (2026-07-15):** the event-core Migration-A fragment adds only the dormant `workflow_events`
  eleven-column scope/claim/terminal-outcome shape plus eleven `NOT VALID` local checks. The 5s one-table timeout,
  populated/current-writer sentinel, malformed-new-write, descriptor invisibility, ledger rollback, exact-once recovery,
  and no-op proofs are migration-local only. The 17-column descriptor and current explicit writer remain closed to the
  new fields. Transport provenance, verification intent, durable dispatch exposure, response/failure receipts, late
  quarantine, terminal UoW, registries/manifests/factory, Migration B-D, action-root, OB-10.1/10.2/10.3/10.4, and served
  population remain open. This candidate changes neither R-019 status nor the 26-call ratchet and authorizes no
  runtime/live path. Commit `47a7f7db...` received a fresh pinned medium-effort advisory `NO-GO 0/0/2/0`; the migration
  semantics passed, and its two contract-evidence findings (stale `0006` absence wording and incomplete zero-PG exact
  DDL oracle) were fixed-forwarded. Commit `316741a...` re-review closed those two but returned medium advisory
  `NO-GO 0/0/1/0` because a twelfth validating `ADD CONSTRAINT` escaped the `CHECK ... NOT VALID`-only extractor; the
  every-constraint ordered-name/total exact assertion is now fixed-forwarded with migration/runtime unchanged and a
  fresh re-review pending. Commit `68c901a...` re-review returned medium advisory `NO-GO 0/0/1/0` because quoted-name
  or alternate-whitespace constraints still escaped the extractor; independent total counts for every `ADD` and
  `CONSTRAINT` token now precede whitespace-tolerant exact name and full-predicate comparisons, with migration/runtime
  still unchanged. Commit `e7db34e...` fresh pinned medium re-review returned advisory `GO 0/0/0/0`, including five
  twelfth-constraint mutation variants; formal highest-effort review remains pending. This does not change any residual
  status.

- **R-019 / D3c2e (2026-07-15):** decision-only batch ratifies the exact WorkflowEvent terminal-lineage core as
  **11 columns + 11 local `NOT VALID` checks**, reuses existing operation/command/attempt links, and forbids redundant
  event-side `operation_run_id`/`source_*`/`source_command_attempt` aliases. D3c2e-D1..D10 explicitly retain transport
  provenance, verification-intent/receipt/exposure/quarantine physical ownership, indexes/FKs/population checks,
  adoption, and the terminal command/attempt/event/source-intent one-PG UoW. D3c2f may install only the dormant core;
  commit `1fb052fe...` has a fresh pinned medium-effort non-author **ADVISORY GO** with P0-P3=`0/0/0/0`, not a formal
  highest-effort verdict. This annotation does not change R-019, R-023, R-027, R-029, action-root, OB-10.1-10.4, or
  served=0.

- **R-019 / D3c1a (2026-07-15):** the row's D3c1a summary includes the five direct findings from the invalid D3c1
  artifact. The later `20260714T215839Z` artifact also remains **invalid/advisory** because
  `final_response_item_exact=false`; its substantive #1/#2/#4-#11 findings are fixed-forwarded by an exact-built-in
  bounded JSON copier, one budgeted traversal in each backend/frontend boundary, once-sanitized strict control/outcome
  envelopes, numeric-record parity, the canonical producer-owned policy-field registry, current ActivityRun descriptor plus linked WorkflowCommand
  control-target evidence, and constructor-only `CommandKernel` ownership. A legitimate planned ActivityRun may have a
  different `activity_type`/`owner` from its source command, so those two owner domains are verified independently rather
  than forced equal. The prior pinned `4cfd1916` author evidence remains recorded. Current stable evidence is
  `18/63/129/60/51`, frontend `81 modules`, public mapper `85/34`, Ruff/diff clean, and mypy `81/4`; fresh dirty-tree
  non-author advisory is `0/0/0/0 CLEAN`. The later `20260714T231243Z_*` artifact is also invalid solely at
  `final_response_item_exact=false`; its `0/4/4/1` is advisory only. Its actionable evidence is fixed by one canonical
  frontend outcome/budget manifest, exact action matrices, once-captured shared wrapper traversal, bounded/cycle-safe
  demo traversal, true bounded PG batch provenance reads, the exact-empty operation-sync bit, and capture-once Activity
  provenance. Final integration audit additionally closes nonempty projected-empty frontend sync, memoized DAG alias
  output amplification, foreign workspace/run provenance, and whitespace-wrapped outcomes. Focused evidence is
  `18+38+6(+3 subtests)+2+60+60`, frontend build `82 modules`, Ruff/diff clean, mypy `81/4`, and dirty-tree non-author
  advisory `0/0/0/0 CLEAN`. Commit `4919990...` then received valid exact-object medium pinned advisories:
  backend `NO-GO 0/0/2/1`, frontend `NO-GO 0/0/5/2`. Their findings are fixed-forwarded by real-PG exact-500 and
  independent lineage-mismatch evidence plus bounded descriptor-first frontend projection, O(1) alias charging,
  single-serialized demo DTOs, phantom-empty omission, literal action return types, and exact-empty fail-closed rules.
  Current evidence is `119 + 19 + PG 6/500 subtests + 501 guard 1`, frontend `82 modules`, Ruff/diff clean, mypy
  `81/4`; absent-Content-Length read-before-allocation remains a streaming-reader residual. Commit `b54ef9c...` then
  received valid isolated-home `gpt-5.6-sol / ultra / priority` formal **NO-GO 0/0/5/2**. Its five P2 frontend
  findings are fixed-forwarded by the enclosing trusted-snapshot candidate; its two P3 findings remain `R-019` and
  `R-030`. Commit `f5c33b0...` then received formal **NO-GO 0/0/4/4** and commit `46af086...` received formal
  **NO-GO 0/0/2/2**; the latter confirmed both exact-cap fixes and left only recursive TypeScript readonly plus the
  R-030 audit-transition instruction as P2. Both are fixed-forwarded in the current candidate. Fresh formal pinned
  re-review of the new enclosing commit is still **pending**. No invalid artifact is a formal `NO-GO` or `GO`. This
  annotation does not change R-019's status, tripwire, 26-call ratchet, claim-fence obligations, or `served=0`.

- **R-019 / D3c2b (2026-07-15):** the scoped-root Migration-A fragment adds only the exact dormant
  `plan_review_sessions` 11-column and `operation_runs` 5-column sentinel shapes plus sixteen `NOT VALID` local checks.
  A two-table timeout test proves the pending session alteration, blocked OperationRun alteration, and `0004` ledger row
  roll back together; populated sentinel, malformed-new-write, mapper invisibility, single-apply, and no-op evidence are
  local migration evidence only. No scoped-session repository, OperationRun exact-copy writer, authority/claim/dispatch
  path, FK/index/unique, action-root gate, or OB-10.1/10.2/10.3/10.4 owner is implemented. Activity/event/receipt/
  quarantine Migration-A fragments remain open, so rollout step 3 registries/manifests/factory cannot start. This
  advances physical shape only and does not change R-019's pending status, 26-call ratchet, or `served=0`. Exact commit
  `0aa253c7d7e5324f5c0021570e2980f358ea3922` has a **VALID GO** pinned review at
  `runtime/reviews/20260714T222746Z_Track_D_D3c2b_scoped_root_migration_retry_1.md`, scope digest
  `7988dd50814ba1c2cc4a3c5efa7ec40ac7eb47b80cbce901ecc8c04a83a92685`; that verdict covers only this dormant
  nine-file migration slice and does not close the named residuals.

- **R-019 / D3c2d (2026-07-15):** the Activity-chain Migration-A fragment adds only the dormant
  `workflow_activity_runs` six-column and `workflow_activity_attempts` ten-column scope/coordination/claim-identity
  shapes plus eighteen `NOT VALID` local checks. It keeps existing 20/22-column descriptors and all characterized
  runtime writers closed to the new fields; current `attempt_number` remains retry accounting and is not future
  post-claim `command_attempt`. The two-table timeout/recovery, populated sentinel, malformed-new-write, and descriptor
  invisibility proofs are migration-local only. Workflow-event/intent/receipt/quarantine fragments, terminal UoW,
  registries/manifests/factory, Migration B-D, action-root, OB-10.1/10.2/10.3/10.4, and served population remain open.
  Therefore this candidate changes neither R-019 status nor the 26-call ratchet, and it authorizes no runtime/live path;
  its exact implementation commit and fresh pinned review are recorded only after final validation.

- **R-029 / D1n S1e2b (2026-07-18):** S1e2a/S1e2b add only isolated `start_acquisition_run` v2 submit/create
  authority. They do not add an API-submittable production action, change the `10 schema-defined / 5 schema-less`
  denominator, start an R-029 observation window, or populate the default/public Agent registry. Result acceptance,
  action-specific served evidence, and the complete 15-action release-window deletion condition remain open; current
  author PG evidence and any future scope-local review do not close this row.

- **R-019 / R-029 / D1n S1e2b formal-review response (2026-07-18):** the response to the S1e2b formal `NO-GO`
  hardens only the isolated start-v2 create boundary: held-root generic command controls/claim/ready-list and direct PG
  cancel/retry/resume mutators are zero-write, mixed v2 Action/Operation controls fail closed, partial-v2 pending
  approve/reject controls fail closed, OperationRun control-state affordances are hidden when the same preflight rejects
  the control, replay uses raw physical row identity plus strict JSON equality, and approval actor/policy validation
  happens before adapter/PG access. This does not add a direct
  `update_action_state`/`update_operation_state` caller, does not change the current R-019 direct state-sync ratchet,
  does not start the R-029 observation epoch, and does not populate served/default Agent tools. S1e2c has a separate
  formal `NO-GO`, so result acceptance, release replay after downstream progress, shared lock topology, central
  contract/preflight wiring, and live/signoff remain blocked.

- **R-019 / R-029 / D1n S1e2c partial response (2026-07-18):** the S1e2c response rejects forged
  serialized-result/error terminals before writes, exact-loads slot and preview owner rows, refreshes prepare
  lock/statement timeout from the remaining deadline, and adds stable owner-result proof for accepted replay plus
  late-result quarantine after downstream root-command progress. The central pre-agent matrix/preflight now includes
  the start-v2 result hold/release owner entry. Start-v2 create and result acceptance now share the same advisory lock
  topology and idempotency keys. The shared result-acceptance native boundary now fails closed unless all required tables
  are both read-routed and authoritative. Result acceptance also reuses the canonical locked-preview binder during
  base-owner load, so PG-admissible nested company drift, expired row+payload timestamps, and start-schema pin drift fail
  closed with zero result-slot/journal/command-release writes. It does not close the S1e2c formal `NO-GO`: broader
  completion/control topology remains under R-019, and no provider/model/live/served path is authorized.

## 使用方式

- **批验收**：suite 失败 ⊆ 本台账未 closed 行 = 绿（green-modulo-ledger）；任何不在台账内的失败必须现场 worktree 归因，
  归因为 pre-existing 才可新增行（带全部列），否则就是你的回归。
- **评审引用**：跨模型评审（`INDEPENDENT_REVIEW_GATE.md`）把 finding 分类为 `residual` 时必须引用行 id；
  无 id 的 residual 分类无效。
- **到期巡检**：每批 step 6 文档收尾时扫一遍 tripwire 列，触发的行升级为当批工作项或决策卡。
