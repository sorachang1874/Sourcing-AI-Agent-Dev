# Decision

Adopt an adjusted staged architecture: retain the bounded CLI/OAuth capability handshake only for the already authorized one-shot Stage 1 experiment, describe it as observed single-call reconciliation rather than transport-enforced call prevention, and require a supported Responses/Batch API credential and separate adapter before scale work.

## Authority

ADVISORY_ONLY — not an independent-review artifact or formal GO.

Raw Pro verdict: adjust

Codex remains responsible for local facts, implementation, validation, and disposition. Repository non-author review artifacts remain the only authority for the corresponding promotion gates.

## Local disposition

P0 disposition: accepted — keep the one-shot capability runner, preserve its post-hoc structured-call reconciliation and kill-on-observation boundary, and do not consume the approval unless the pinned non-author review accepts the live scope and OAuth is valid.

P1 disposition: accepted — add a field-capability receipt and a separate profile/Bio evidence lane for observed Chinese content, subject-claimed China professional-ecosystem activity, current/previous organization mention proposals, corroborated affiliation, and explicit physical region experience; never collapse these states or treat display names as stable identity.

P2 disposition: accepted — keep the evaluator methodology-only until normalized task rows, an independently reviewed golden manifest, accepted packet rows, incident/coverage/TTL ledgers, and mechanically recomputed metrics exist.

Validation: reconcile the advice against commits `fee3699` and `331c209`, require the X-live pinned review result, rerun the complete X-first offline suite after accepted changes, and validate this four-file consultation bundle with the repository-owned v2 validator.

## Follow-up

First resolve X-live review findings, then implement and review the profile/Bio field-capability fixture lane. If Stage 1 later produces exactly one completed native X receipt, use the observed raw shape to decide whether to investigate a supported Responses/Batch credential; do not infer Bio completeness or batch readiness from platform documentation alone.
