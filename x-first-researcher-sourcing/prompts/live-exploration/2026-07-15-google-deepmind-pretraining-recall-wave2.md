You are executing the historical, publication, and project-linkage challenger wave of a read-only, high-recall
researcher-sourcing experiment with Grok's native X tools.

Target: novel public X accounts missed by the prior Google DeepMind broad-discovery wave that may belong to people
with current or historical Google DeepMind affiliation and current or historical pre-training, base-model, or
foundation-model training experience. Prior handles are exclusions except when materially new evidence corrects a
temporal conflict. Do not return famous accounts merely because they are convenient seeds.

Complete these materially different strategies:

1. DeepMind-era and post-merger technical reports, model cards, papers, projects, releases, talks, and conference
   discussions involving large language, multimodal, speech, vision, robotics, world, or generative base models;
2. low-visibility authors and research engineers identified through public acknowledgements, team replies, quotes,
   contributor threads, recruiting posts, welcomes, farewells, and one-hop professional graph edges;
3. training functions that may omit the word `pretrain`: data curation/mixtures/quality, tokenization, scaling,
   architecture, optimization, training stability, distributed systems, accelerators, checkpointing, compute,
   reliability, and training-time evaluation or safety;
4. historical paths through DeepMind, Google DeepMind, and Google Brain, plus alumni transitions to academia, other
   labs, or startups. Google Brain evidence alone remains ambiguous for target-lab affiliation unless a separate
   source explicitly binds the person to DeepMind or post-merger Google DeepMind.

Treat Bio as affiliation/profile evidence, not the only source of technical relevance. Search first-party Posts and
Replies for pretraining evidence. For promising unresolved retained handles, issue distinct exact single-handle
`from:<handle> ... -filter:replies` and `from:<handle> ... filter:replies` searches where time permits, and preserve
the authoritative schema's `thread_relation` plus typed temporal support claims. A list, paper authorship, project
mention, or third-party statement may seed a lead but cannot alone confirm employment or experience.

Use only native X keyword, semantic, user, and thread tools. No generic web search/fetch, browser, filesystem, shell,
connectors, memory, subagents, mutations, or contact. Use public professional evidence only; never query or infer
protected identity.

Do not impose a candidate, observation, or native-X-call business cap. Complete all four strategy families, then
continue materially different expansions until three consecutive expansions add no novel evidence-bearing handle or
material temporal correction. The external operator owns the deadline, turn ceiling, and kill switch. Deduplicate
case-insensitively and keep lab affiliation temporality independent from pretraining temporality.

Return exactly one JSON object matching the authoritative result schema appended by the operator. Emit no Markdown,
progress prose, prefix, or suffix. Never invent IDs, Bios, timestamps, URLs, excerpts, relationships, or claims.
