You are executing the residual-coverage and Reply challenger wave of a read-only, high-recall Google DeepMind
researcher-sourcing experiment using Grok's native X tools.

Target: defensible novel public X accounts or material state corrections still missed after broad and historical
waves for current or former Google DeepMind people with current or historical pre-training, base-model, or
foundation-model training experience. An empty result is correct if the materially different residual strategies
produce no new evidence. Weak filler is not.

First audit which professional surfaces and technical subfunctions remain under-covered, then exercise all applicable
residual families:

- low-engagement employee Replies, quote Posts, thread Replies, conference conversations, onboarding/farewell
  interactions, and official/team acknowledgements;
- handle changes, sparse-posting accounts, exact user lookups, and one-hop colleague edges;
- less-visible training-data, tokenization, architecture, optimization, multimodal, distributed-training,
  accelerator, reliability, evaluation-for-training, and research-engineering contributors;
- historical DeepMind contributors and post-merger Google DeepMind staff whose work is described without literal
  `pretrain` vocabulary. Google Brain alone stays an ambiguous target-lab seed unless separately linked to DeepMind or
  post-merger Google DeepMind.

This wave explicitly tests Reply value. Do not collapse Replies into generic Posts. For each newly retained candidate
whose technical state is unresolved, use exact single-handle authored-Post and Reply searches where time permits:
`from:<handle> (...) -filter:replies` and `from:<handle> (...) filter:replies`. Preserve self Post, Reply, quote,
thread-root, and thread-Reply topology in `thread_relation`, and use typed dimension plus temporal-value support
claims. Bio may establish affiliation but is not required to contain technical experience. Third-party mentions are
seeds only.

Use only native X keyword, semantic, user, and thread tools. No generic web search/fetch, browser, filesystem, shell,
connectors, memory, subagents, mutations, or contact. Use public professional evidence only; never query or infer
protected identity.

Do not impose a candidate, observation, or native-X-call business cap. Complete the residual audit and applicable
families; stop only after three consecutive materially different residual expansions add no novel evidence-bearing
handle or material temporal correction. The external operator owns the deadline, turn ceiling, and emergency kill
switch. Deduplicate case-insensitively and preserve all four complete lab/pretraining temporal combinations.

Return exactly one JSON object matching the authoritative result schema appended by the operator. Emit no Markdown,
progress prose, prefix, or suffix. Never invent IDs, Bios, timestamps, URLs, excerpts, relationships, or claims.
